---
description: 단발성 보강 수업 등록. 결석 보강·자유 보강 통합 (보강수업 시간표 DB row 1건 생성)
---

# 보강시간표등록

원장이 `/보강시간표등록` 을 입력하면, **보강 종류(결석/자유) → 학생 → 반(또는 결석 row) → 보강 날짜 → 시간 → 미리보기 → 확인(네/아니오)** 후 노션 보강수업 시간표 DB에 row 1건을 생성한다.

매주 반복 시간표는 `/시간표등록` (수업 시간표 DB), 단발성 보강은 이 명령어 (보강수업 시간표 DB).

> ⚠️ **Claude 동작 가드 (반드시 준수)**
>
> 1. **AskUserQuestion 도구 사용 금지**. 모든 질문은 일반 텍스트 출력 + 사용자 자유 답변(텍스트·번호) 받기.
> 2. **자의적 부연 설명 금지**. .md에 정의되지 않은 안내문 노출 금지. 정의된 텍스트만 그대로 출력.
> 3. **목록 표시는 모두 한 화면**. 옵션 5개 이상이어도 한 번에 보여주고 1개만 받기.

## 범위

**입력받을 정보**
- 보강 종류 (결석 보강 / 자유 보강)
- 학생 (수강 등록된 학생만)
- 반 (자유 보강 시 사용자 선택, 결석 보강 시 결석 row에서 자동)
- 결석 row (결석 보강 케이스만, 미보강 결석 중 선택)
- 보강수업 날짜 (모든 날짜 허용 — 과거는 경고)
- 수업 시작 시간, 수업 종료 시간 (HH:MM 24시간제)

**자동 생성**
- 보강시간표라벨 = `"{학생이름} - {반이름} 보강 ({YYYY-MM-DD})"`
- 수업요일 = 보강 날짜에서 자동 계산

**자동 채워짐 (노션이 처리)**
- 선생님 (rollup), 보강수업시간 (formula), 수강생(수식)
- 보강 회차 (일일피드백 측 dual_property 자동 sync)

**제외 (별도 명령어)**
- 매주 반복 시간표 — `/시간표등록`
- 학원 캘린더 일정 — 별도 명령어
- 수업계획서 — `/수업계획`
- 출결 — `/출석`, `/결석`

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-post-page
   ```
2. config 읽기: `G:/내 드라이브/ClaudeSetting/notion-academy-config.json`
   - `notion.data_sources.students`
   - `notion.data_sources.classes`
   - `notion.data_sources.class_enrollments`
   - `notion.data_sources.makeup_schedule`
   - `notion.data_sources.lesson_results`
3. 현재 날짜 기록 (보강 날짜 검증용)

## 절차

### 1. 보강 종류 분기

```
[1/?] 어떤 보강이에요?
  1. 결석 보강 (학생이 결석한 회차 보강)
  2. 자유 보강 (결석 없는 추가 보강 — 휴강·특별 보강 등)
번호 선택:
```

분기:
- "1" → `mode = "결석보강"`
- "2" → `mode = "자유보강"`
- 그 외 → "1 또는 2를 입력해주세요" 재요청

### 2. 학생 검색·선택 (공통)

```
[2/?] 어떤 학생의 보강인가요? (이름 일부만 입력해도 됨)

> 
```

쿼리: 학생 DB
- `학생이름 contains <입력>` AND `등록현황 == "등록"`

분기:
- 0건 → "/학생-신규등록 먼저" 종료
- 1건 → 자동 선택 + 확인 ("`{학생이름}` 학생이 맞나요? 네/아니오")
- 2건+ → 학교·학년 표시 후 번호 선택

### 3. 학생의 수강 등록 cross-check (공통)

쿼리: 수강 등록 DB
- `학생 == <학생 page_id>`

#### 3-1. 0건 → 차단

```
❌ {학생이름} 학생은 수강 등록된 반이 없습니다.

💡 보강을 만들기 전에 학생을 반에 등록해야 합니다.
   /수강등록 으로 학생-반 매핑 + 수강료 설정 후 다시 시도해주세요.

종료 (노션 변경 없음).
```

#### 3-2. 1건+ → 다음 단계 (mode에 따라 4-A 또는 4-B)

각 등록 row의 `반` relation에서 클래스 정보(이름, 클래스타입, 선생님) retrieve.

### 4-A. [결석 보강] 미보강 결석 row 검색·선택

쿼리: 일일피드백 DB
- `학생 직접 == <학생 page_id>` AND `출결 == "결석"`

각 결석 row에 대해 `보강 회차` 필드(dual_property로 자동 sync) 검사:
- `보강 회차` 비어있음 → 미보강 (목록에 포함)
- `보강 회차` 1건+ → 이미 보강 완료 (목록에서 제외)

#### 4-A-1. 미보강 결석 0건 → 안내 후 종료

```
❌ {학생이름} 학생의 미보강 결석이 없습니다.

💡 모든 결석이 이미 보강 완료되었거나, 결석 기록이 없습니다.
   결석이 있는데 보강이 필요하면 일일피드백 DB에서 결석 row가
   정상 기록되어 있는지 확인해주세요.

   자유 보강을 만들려면 명령어를 다시 호출하고 [2. 자유 보강]을 선택해주세요.

종료 (노션 변경 없음).
```

#### 4-A-2. 미보강 결석 1건+ → 목록 선택

```
[?/?] 미보강 결석 회차:
  1. 2026-05-08 (월) 16:00 — 파이썬 초등 A (정규수업)
  2. 2026-05-09 (화) 16:00 — 파이썬 초등 A (정규수업)
번호 선택:
```

각 row 표시:
- 수업일자 (date)
- 요일 (date에서 계산)
- 시간 (시간표 rollup의 정규수업시간)
- 클래스 (rollup)
- 정규/보강 (rollup)

선택 후 자동 추출:
- 학생 (이미 결정)
- 반 = 결석 row의 클래스 (자동 매칭)
- 원본 결석 회차 = 선택한 일일피드백 row page_id

### 4-B. [자유 보강] 반 선택

#### 4-B-1. 등록 반 1건 → 자동 선택 + 확인

```
✅ {학생이름} 학생이 등록한 반이 1개 있습니다.
- [{클래스타입}] {반이름} (담당: {선생님이름})
이 반의 보강을 만들까요? (네/아니오)
```

#### 4-B-2. 등록 반 2건+ → 목록 선택

```
[?/?] 어느 반의 보강인가요?
  1. [정규반] 파이썬 초등 A (담당: 김선생)
  2. [특강반] 정보올림피아드 특강반 (담당: 박선생)
번호 선택:
```

선택 후 자동 추출:
- 학생 (이미 결정)
- 반 = 선택한 클래스 page_id
- 원본 결석 회차 = (omit, 자유 보강이므로 빈 칸)

### 5. 보강수업 날짜 입력 (공통)

```
[?/?] 보강수업 날짜? (YYYY-MM-DD)
       "내일" 입력 시 내일 날짜 자동 적용
       "오늘" 입력 시 오늘 날짜 자동 적용
       (과거 날짜도 입력 가능 — 이미 진행한 보강을 사후 기록할 때 사용)

> 
```

#### 5-1. 입력 처리

- "내일" → 오늘 + 1일
- "오늘" → 오늘 날짜
- "YYYY-MM-DD" 형식 → 검증 후 사용
- 그 외 → "날짜 형식이 올바르지 않습니다. YYYY-MM-DD 형식 또는 '내일'·'오늘'을 입력해주세요." 재요청

#### 5-2. 날짜 검증 (모든 날짜 허용, 과거는 경고)

| 조건 | 처리 |
|---|---|
| 형식 오류 | 재입력 |
| 미래 날짜 (오늘 이후) | 그대로 진행 |
| 오늘 날짜 | 그대로 진행 |
| 과거 날짜 (오늘 이전) | "⚠️ 과거 날짜입니다 ({YYYY-MM-DD}). 이미 진행한 보강을 사후 기록하시는 건가요? (네/아니오)" 부정 시 재입력 |
| 너무 먼 미래 (오늘 + 1년 이상) | "⚠️ 보강 날짜가 1년 이상 미래입니다. 맞나요? (네/아니오)" 부정 시 재입력 |
| 너무 먼 과거 (오늘 - 1년 이상) | "⚠️ 보강 날짜가 1년 이상 과거입니다. 맞나요? (네/아니오)" 부정 시 재입력 |

> 사후 기록 케이스 지원: 이미 진행한 보강을 뒤늦게 입력하는 운영자를 위해 과거 날짜 허용. 다만 실수 방지로 경고 + 진행 확인.

#### 5-3. 요일 자동 계산

`weekday = date.weekday()` → "월"~"일" 한글 매핑.

### 6. 시간 입력 (공통)

#### 6-1. 시작 시간

```
[?/?] 보강수업 시작 시간? (HH:MM, 24시간제)
       (예: 16:00, 17:30)

> 
```

#### 6-2. 종료 시간

```
[?/?] 보강수업 종료 시간? (HH:MM, 24시간제)

> 
```

#### 6-3. 시간 검증

- 정규식: `^([01]?[0-9]|2[0-3]):[0-5][0-9]$`
- 단일 자릿수 자동 0 패딩 (`9:00` → `09:00`)
- 시작 ≥ 종료 → "시작 시간이 종료 시간보다 늦거나 같습니다. 다시 입력해주세요." 재입력

### 7. 같은 학생-기존 시간표 충돌 검사

학생의 같은 날짜·시간대 모든 시간표 충돌 검사.

#### 7-1. 수업 시간표 (매주 반복) 충돌 검사

쿼리: 수업 시간표 DB
- `수강생 == <학생 page_id>` AND `수업요일 == <보강 요일>`

겹침 판정 공식: `start_new < end_existing AND start_existing < end_new`.

#### 7-2. 보강수업 시간표 (단발성) 충돌 검사

쿼리: 보강수업 시간표 DB
- `수강생 == <학생 page_id>` AND `보강수업 날짜 == <보강 날짜>`

같은 날짜에 시간대 겹치는 row 있으면 충돌.

#### 7-3. 충돌 시 차단

```
━━━━━━━━━━━━━━━━━━━━━━
❌ 시간 충돌 발견.
━━━━━━━━━━━━━━━━━━━━━━

[충돌 정보]
- 새 보강:        {보강날짜} ({요일}) {시작}~{종료}
- 기존 시간표:    {기존 정보} ({기존 반이름})
- 종류:           {매주 반복 / 보강}
- 노션 링크:      https://notion.so/<기존 page-id>

💡 한 학생이 같은 시간대에 두 수업을 동시에 받을 수 없습니다.
   다음 중 하나로 처리해주세요:

  1. 보강 시간 변경하여 다시 시도
     → 명령어 다시 호출하고 다른 시간 입력

  2. 기존 시간표 변경
     → 위 노션 링크에서 기존 시간 수정 후 명령어 다시 호출

  3. 기존 시간표 삭제 후 재등록
     → 위 노션 링크에서 기존 row 삭제 후 명령어 다시 호출

━━━━━━━━━━━━━━━━━━━━━━
종료 (노션 변경 없음).
```

### 8. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 보강 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[보강 종류]
{결석 보강 / 자유 보강}

[학생-반]
- 학생:        {학생이름}
- 반:          {반이름} ({클래스타입})
- 담당 선생님: {선생님이름}

[보강 정보]
- 보강 날짜:    {YYYY-MM-DD} ({요일})
- 시간:         {시작}~{종료}

(결석 보강 케이스만)
[원본 결석 회차]
- 결석일:      {결석 일자}
- 결석 시간표: {결석 시간 — 정규/보강 라벨}
- 노션 링크:    https://notion.so/<일일피드백 page-id>

[처리]
- 동작:        보강수업 시간표 DB에 row 1건 신규 생성
- 결석 row link: {적용 / 적용 안 함}
- 후속 작업:   당일 /출석·/결석 처리, /수업계획

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

부정 또는 "취소" → 즉시 종료, 노션 변경 0.

### 9. 노션 row 생성

#### 9-1. 보강시간표라벨 자동

`보강시간표라벨 = "{학생이름} - {반이름} 보강 ({YYYY-MM-DD})"` (예: "추득만 - 파이썬 초등 A 보강 (2026-05-15)").

#### 9-2. post-page

```json
{
  "parent": {"type": "database_id", "database_id": "<makeup_schedule database_id>"},
  "properties": {
    "보강시간표라벨": {"title": [{"text": {"content": "<라벨>"}}]},
    "보강수업 날짜": {"date": {"start": "<YYYY-MM-DD>"}},
    "수업요일": {"select": {"name": "<요일>"}},
    "수업 시작 시간": {"rich_text": [{"text": {"content": "<HH:MM>"}}]},
    "수업 종료 시간": {"rich_text": [{"text": {"content": "<HH:MM>"}}]},
    "수업선택": {"relation": [{"id": "<반 page_id>"}]},
    "수강생": {"relation": [{"id": "<학생 page_id>"}]},
    "원본 결석 회차": {"relation": [{"id": "<일일피드백 page_id>"}]}
  }
}
```

자유 보강 케이스는 `원본 결석 회차` 키 자체를 omit (빈 배열 NOT, 키 자체를 빼기).

### 10. 결과 보고

```
✅ 보강 등록 완료

[보강 종류]
{결석 보강 / 자유 보강}

[학생-반]
- 학생:        {학생이름}
- 반:          {반이름}
- 선생님:      {선생님이름}

[보강 정보]
- 날짜:        {YYYY-MM-DD} ({요일})
- 시간:        {시작}~{종료}

(결석 보강 시)
[연결된 결석]
- 일일피드백:  https://notion.so/<일일피드백 page-id>
   ({결석일} 결석 — 자동으로 보강 회차에 link됨)

[노션 URL]
https://notion.so/<page-id>

[다음 작업 추천]
1. 보강 당일 /출석 또는 /결석 으로 출결 처리
2. /수업계획 수업 전 계획 작성
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 보강 종류 1/2 외 입력 | 재입력 |
| 학생 검색 0건 | "/학생-신규등록 먼저" 종료 |
| 수강 등록 0건 | "/수강등록 먼저" 종료 |
| 결석 보강 — 미보강 결석 0건 | 안내 후 종료 |
| 결석 보강 — 미보강 결석 N건 | 목록 선택 |
| 자유 보강 — 등록 반 1건 | 자동 선택 + 확인 |
| 자유 보강 — 등록 반 N건 | 목록 선택 |
| 보강 날짜 형식 오류 | 재입력 |
| 보강 날짜 미래·오늘 | 그대로 진행 |
| 보강 날짜 과거 | ⚠️ 경고 ("사후 기록인가요?") + 진행 확인 |
| 보강 날짜 너무 먼 미래 (1년 이상) | 경고 + 확인 |
| 보강 날짜 너무 먼 과거 (1년 이상) | 경고 + 확인 |
| 시간 형식 오류 | 재입력 |
| 시작 ≥ 종료 | 재입력 |
| 수업 시간표 충돌 (매주 반복 수업과) | 차단 + 행동 가이드 |
| 보강수업 시간표 충돌 (다른 보강과) | 차단 + 행동 가이드 |
| 미리보기 부정 답변 | 즉시 종료, 노션 변경 0 |
| 어느 단계든 "취소"·"q" | 즉시 종료 |

## 주의사항

- **확인 없는 자동 쓰기 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 노션에 절대 POST 금지
- **수강 등록 cross-check 강제**: 수강 등록 없는 학생·반은 보강 등록 차단
- **시간 충돌은 다른 반과도 차단**: 한 학생이 같은 시간 두 수업 받을 수 없음
- **결석 row dual_property 활용**: 미보강 결석 검색 시 일일피드백 DB의 `보강 회차` 필드로 한 번에 판정
- **휴강은 보강 안 함**: 출결 == "결석"만 검색 대상. "휴강" 출결은 노출 안 됨 (공휴일 등 학원 정책)
- **단일 자릿수 자동 패딩**: `9:00` 입력 시 `09:00`으로 자동 변환
- **사후 기록 지원**: 과거 날짜 허용. 이미 진행한 보강을 뒤늦게 입력 가능 (경고 + 확인)
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (메모리 `feedback_yes_no_korean`)
- **터미널 default**: 엔터 사용 안 함. 키워드("내일"·"오늘") 또는 번호만 (메모리 `feedback_terminal_default_input`)

## 예시 1: 결석 보강

**입력**
- 보강 종류: 1 (결석 보강)
- 학생: 추득만
- 미보강 결석: 2026-05-08 (월) 16:00 정규수업 — 파이썬 초등 A
- 보강 날짜: 2026-05-15
- 시간: 16:00~17:30

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📋 보강 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[보강 종류]
결석 보강

[학생-반]
- 학생:        추득만
- 반:          파이썬 초등 A (정규반)
- 담당 선생님: 김선생

[보강 정보]
- 보강 날짜:    2026-05-15 (금)
- 시간:         16:00~17:30

[원본 결석 회차]
- 결석일:      2026-05-08
- 결석 시간표: 16:00~17:30 (정규수업)
- 노션 링크:    https://notion.so/...

[처리]
- 동작:        보강수업 시간표 DB에 row 1건 신규 생성
- 결석 row link: 적용
- 후속 작업:   당일 /출석·/결석 처리, /수업계획

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

**노션 결과**
- 보강수업 시간표 DB row 1건 추가
- 보강시간표라벨="추득만 - 파이썬 초등 A 보강 (2026-05-15)"
- 원본 결석 회차 = 일일피드백의 결석 row page_id link
- 일일피드백 row의 `보강 회차` 필드에 자동으로 새 보강 row link됨 (dual_property)

## 예시 2: 자유 보강

**입력**
- 보강 종류: 2 (자유 보강)
- 학생: 추득만
- 반: 파이썬 초등 A
- 보강 날짜: 2026-05-15
- 시간: 16:00~17:30

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📋 보강 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[보강 종류]
자유 보강

[학생-반]
- 학생:        추득만
- 반:          파이썬 초등 A (정규반)
- 담당 선생님: 김선생

[보강 정보]
- 보강 날짜:    2026-05-15 (금)
- 시간:         16:00~17:30

[처리]
- 동작:        보강수업 시간표 DB에 row 1건 신규 생성
- 결석 row link: 적용 안 함
- 후속 작업:   당일 /출석·/결석 처리, /수업계획

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

**노션 결과**
- 보강수업 시간표 DB row 1건 추가
- 원본 결석 회차 = 빈 칸
