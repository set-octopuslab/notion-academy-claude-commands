---
description: 수업 전 수업계획·수업요약 DB 입력. 클래스 단위 일괄 처리 (같은 반 N명 동일 내용)
---

# 수업계획

원장이 `/수업계획` 을 입력하면, **날짜 → 클래스 → 수강생 자동 추출 → 보호 판정 → 수업내용 입력 → 미리보기 → 확인(네/아니오)** 후 수업계획·수업요약 DB에 학생별 row N건을 일괄 생성/patch한다.

같은 반 학생 N명에게 **동일한 수업내용**을 N개 row로 일괄 적용. `/출석`·`/결석`이 자동 생성한 빈 수업계획서 row가 있으면 patch, 없으면 신규 생성.

> ⚠️ **Claude 동작 가드 (반드시 준수)**
>
> 1. **AskUserQuestion 도구 사용 금지**. 모든 질문은 일반 텍스트 출력 + 사용자 자유 답변(텍스트·번호) 받기.
> 2. **자의적 부연 설명 금지**. .md에 정의되지 않은 안내문 노출 금지.
> 3. **목록은 모두 한 화면**. 클래스·학생 목록은 한 번에 노출하고 1개 번호만 받기.
> 4. **수업내용 입력은 단일 메시지**. 사용자가 보낸 한 메시지 텍스트(여러 줄 포함) 전체를 수업내용 raw로 사용. 별도 종료 신호("끝"·빈 줄) 사용 안 함.
> 5. **수업내용 다듬기는 학부모 친근체**. 학부모가 읽는다는 전제로 친근하고 따뜻한 톤. 학습 내용·용어는 정확히 유지. 영어 용어 옆에 한글 설명 자연스럽게 추가. 과도한 미사여구 금지.
> 6. **다듬기 수정 무제한**. 사용자가 "이대로" 또는 "원본 그대로" 선택할 때까지 다듬기 반복.

## 범위

**입력받을 정보**
- 대상 날짜 (오늘/내일/직접 입력)
- 클래스 (그 날짜에 수업 있는 반 중 1개)
- 수업내용 (rich_text, 한 번 입력 → N명에게 일괄 적용)

**자동 결정**
- 수업타입 (정규수업/보강수업) — 시간표 매칭 결과
- 수강생 N명 — 시간표 row의 수강생 자동 추출
- 수업명 (title) 자동 명명: `"{YYYY년 M월 D일} ({요일})\n{학생이름} {클래스이름} 수업계획"`
- 수업 요일 (select) — 날짜에서 자동

**제외 (별도 명령어)**
- 수업녹음파일, 수업내용 요약AI 등 — 노션 직접 또는 별도 명령어
- 학생별 차별화 수업내용 — `/수업결과`에서 학생별 피드백으로 처리

## 사전 조건

- 클래스 DB에 클래스 row 존재
- 수업 시간표 DB에 그 클래스의 수강생 link 존재 (정규반 매주 반복)
- 또는 보강수업 시간표 DB에 그 날짜·그 클래스의 row 존재

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-post-page,mcp__notionApi__API-patch-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.lesson_plans`
   - `notion.data_sources.regular_schedule` (수업 시간표)
   - `notion.data_sources.makeup_schedule` (보강수업 시간표)
   - `notion.data_sources.classes`
   - `notion.data_sources.students`
3. 현재 날짜·요일 기록

## 절차

### 1. 날짜 선택

```
[1/?] 어떤 날짜의 수업계획을 작성할까요?
  1. 오늘 ({YYYY-MM-DD} {요일})
  2. 내일 ({YYYY-MM-DD} {요일})
  3. 다른 날짜 직접 입력
번호 선택:
```

분기:
- "1" → `target_date = today`
- "2" → `target_date = tomorrow`
- "3" → 추가 입력 단계로
- 그 외 → "1·2·3 중 입력해주세요" 재요청

#### 1-1. 직접 입력 시

```
[?/?] 날짜를 입력해주세요. (YYYY-MM-DD)

> 
```

- 형식 오류 → 재입력
- 너무 먼 미래/과거 (±1년 이상) → "맞나요? (네/아니오)" 확인

`target_weekday` = `target_date.weekday()` → "월"~"일" 한글 매핑.

### 2. 클래스 선택

#### 2-1. 그 날짜에 수업 있는 클래스 목록 추출

쿼리 1: 수업 시간표 DB
- `수업요일 == <target_weekday>`
- 결과의 `수업선택` (클래스 relation) → unique 클래스 page_id 목록 + 수업타입="정규수업"

쿼리 2: 보강수업 시간표 DB
- `보강수업 날짜 == <target_date>`
- 결과의 `수업선택` (클래스 relation) → unique 클래스 page_id 목록 + 수업타입="보강수업"

두 결과 합산 + 중복 제거. 같은 클래스가 정규+보강 둘 다 있으면 둘 다 표시 (수업타입 라벨로 구분).

#### 2-2. 분기

| 결과 | 처리 |
|---|---|
| 0건 | "❌ {target_date} ({target_weekday})에 수업이 있는 클래스가 없습니다" 종료 |
| 1건 | 자동 선택 + 확인 ("`{클래스이름}` ({수업타입}) 맞나요? 네/아니오") |
| 2건+ | 목록 번호 매겨 선택 |

표시 예 (2건+):
```
[2/?] {target_date} ({target_weekday}) 수업 클래스:
  1. 파이썬 초등 A (정규수업)
  2. 정보올림피아드 특강반 (정규수업)
  3. C언어 중등 B (보강수업)
번호 선택:
```

선택 후 `target_class`, `target_type` 결정.

### 3. 수강생 추출

선택한 클래스 + 날짜 + 수업타입 기준으로 그 반 수강생 N명 추출.

#### 3-1. 정규수업 케이스

쿼리: 수업 시간표 DB
- `수업선택 contains <target_class page_id>` AND `수업요일 == <target_weekday>`
- 각 row의 `수강생` (학생 single_property) 수집 → unique 학생 목록

#### 3-2. 보강수업 케이스

쿼리: 보강수업 시간표 DB
- `수업선택 contains <target_class page_id>` AND `보강수업 날짜 == <target_date>`
- 각 row의 `수강생` 수집 → unique 학생 목록

#### 3-3. 분기

| 결과 | 처리 |
|---|---|
| 0건 | "❌ 그 클래스에 등록된 수강생이 없습니다" 종료 |
| 1건+ | 학생 이름 목록 표시 후 다음 단계 |

표시 예:
```
✅ 수강생 N명:
- 추득만 (목동초 5학년)
- 김철수 (목동초 6학년)
- 이영희 (목동초 5학년)
```

각 학생의 `학교`·`학년` 정보 함께 표시 (1단계에서 학생 page retrieve 후 추출).

### 4. 학생별 기존 수업계획 row 검색

각 학생 × `target_date` × `수업타입` 으로 수업계획 DB 조회.

쿼리: 수업계획·수업요약 DB
- `학생 == <학생 page_id>` AND `수업 날짜 == <target_date>` AND `수업타입 == <정규수업/보강수업>`

학생별로 분기 결과 분류:

| 분류 | 조건 | 처리 예정 |
|---|---|---|
| 신규 | row 0건 | post-page 신규 생성 |
| Patch | row 1건 + 수업내용 빈 칸 | patch (수업내용 채움) |
| 보호 (덮어쓰기 확인 필요) | row 1건 + 수업내용 채워짐 | 5단계 보호 판정 |
| 다중 (드문 케이스) | row 2건+ | 가장 최근 1건 사용 + 알림 |

### 5. 보호 판정

기존 row의 `수업내용` 필드가 **이미 채워져 있는** 학생이 있으면 보호 발동:

```
⚠️ 다음 학생은 이미 수업내용이 채워져 있습니다:
  - 이영희: "변수와 자료형 기본 개념..."
  - 박철수: "for 반복문 응용..."

이 학생들도 새 내용으로 덮어쓸까요?
  1. 덮어쓰기 (모두 새 내용으로)
  2. 그 학생들은 건드리지 않고 빈 학생만 채우기 (추천)
  3. 취소
번호 선택:
```

분기:
- "1" → `protected_action = "overwrite"` (보호 학생 포함 모두 patch)
- "2" → `protected_action = "skip"` (보호 학생 제외, 신규+빈 row만 처리) — 추천
- "3" → 즉시 종료, 노션 변경 0
- 그 외 → 재입력

보호 학생 0명이면 이 단계 건너뜀.

### 6. 수업내용 입력 (학부모 친근체로 다듬기)

> 수업계획.수업내용은 일일피드백 DB의 `수업내용` rollup으로 그대로 학부모에게 발송됨. 따라서 학부모가 읽기 좋은 친근한 톤으로 다듬어 저장.

#### 6-1. raw 입력

```
[?/?] 수업내용을 입력해주세요.
       대략적으로 적어주시면 학부모 발송에 적합한 친근한 문체로 다듬어드립니다.
       (한 메시지로 여러 줄 가능)

> 
```

#### 6-2. Claude가 학부모 친근체로 다듬어 제시

다듬기 가이드라인:
- **학부모가 읽는다는 전제** — 친근하고 따뜻한 톤
- 학습 내용·용어는 정확히 유지 (변경 금지)
- 영어 용어 옆에 한글 설명 자연스럽게 추가 (예: `print` → `화면에 글자 출력하기(print)`)
- 자연스러운 문장 흐름 (조사·접속사 보강)
- 과도한 미사여구 금지 — 정보 전달 우선
- 길이는 원본과 비슷한 수준

```
📝 다듬은 버전 (학부모용 친근체):
{다듬은 텍스트}

이대로 진행할까요?
  1. 네, 이 버전으로 저장
  2. 수정할 부분이 있어요 (의견 입력)
  3. 원본 그대로 저장 (다듬기 안 함)
번호 선택:
```

#### 6-3. 분기

- "1" → 다듬은 버전 채택, 7단계로
- "2" → 수정 의견 입력 (6-4)
- "3" → 원본 채택, 7단계로
- 그 외 → 재입력

#### 6-4. 수정 의견 입력 (선택지 2)

```
[?/?] 어떤 부분을 수정할까요?
       (예: "더 짧게", "특정 단어 빼주세요", "이 부분 추가해주세요", "더 격식있게")

> 
```

수정 의견 받고 그에 맞춰 다시 다듬어 6-2 형식으로 제시 → 분기 반복.

수정 횟수 **무제한**. 사용자가 1·3 선택할 때까지 반복.

#### 6-5. 원본 빈 칸 입력 시 (raw 자체 비어있음)

```
⚠️ 수업내용이 비어있습니다.
   그래도 진행할까요? (네/아니오)
```

긍정 → 빈 칸 저장 (다듬기 단계 건너뜀). 부정 → 재입력.

### 7. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 수업계획 작성 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[수업]
- 날짜:      {target_date} ({target_weekday})
- 수업타입:  {정규수업/보강수업}
- 클래스:    {클래스이름}

[대상 학생 N명]
  ✏️ patch  추득만 (기존 빈 row에 수업내용 채움)
  ✨ 신규   김철수 (신규 row 생성)
  ⚠️ 덮어  이영희 (기존 내용 덮어쓰기) — 1번 선택 시
  ⏭️ skip   이영희 (이미 작성됨) — 2번 선택 시

[수업내용 — {다듬은 버전 / 원본}]
{최종 채택된 텍스트}

[처리]
- 동작:        수업계획·수업요약 DB에 row {N}건 처리
- 다듬기:      {적용 ({수정 N회) / 적용 안 함 (원본 유지)}
- 후속 작업:   /출석·/결석으로 수업계획 자동 link, 학부모 발송 시 이 내용 본문 사용
━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

부정 또는 "취소" → 즉시 종료, 노션 변경 0.

### 8. 노션 일괄 처리

각 학생별로 분류에 따라 처리.

#### 8-1. 신규 row 생성 (`API-post-page`)

```json
{
  "parent": {"type": "database_id", "database_id": "<lesson_plans database_id>"},
  "properties": {
    "수업명": {"title": [{"text": {"content": "{YYYY년 M월 D일} ({요일})\n{학생이름} {클래스이름} 수업계획"}}]},
    "수업 날짜": {"date": {"start": "<target_date>"}},
    "수업 요일": {"select": {"name": "<target_weekday>"}},
    "수업타입": {"select": {"name": "<정규수업/보강수업>"}},
    "학생": {"relation": [{"id": "<학생 page_id>"}]},
    "클래스": {"relation": [{"id": "<target_class page_id>"}]},
    "수업내용": {"rich_text": [{"text": {"content": "<수업내용>"}}]}
  }
}
```

#### 8-2. 기존 row patch (`API-patch-page`)

```json
{
  "page_id": "<기존 수업계획 page_id>",
  "properties": {
    "수업내용": {"rich_text": [{"text": {"content": "<수업내용>"}}]}
  }
}
```

#### 8-3. skip 학생

`protected_action = "skip"` 이고 보호 학생이면 처리 안 함 (스킵).

### 9. 결과 보고

```
✅ 수업계획 작성 완료
- 날짜:    {target_date} ({target_weekday})
- 클래스:  {클래스이름} ({수업타입})
- 처리:    신규 {신규수}명 / patch {patch수}명 / skip {skip수}명

[처리된 row]
- 추득만:  https://notion.so/<page-id>
- 김철수:  https://notion.so/<page-id>

[다음 작업 추천]
1. 수업 시작 후 /출석 또는 /결석 으로 출결 처리 (수업계획 자동 link됨)
2. 수업 끝난 뒤 /수업결과 로 학생별 피드백 입력
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 날짜 1·2·3 외 입력 | 재입력 |
| 직접 입력 날짜 형식 오류 | 재입력 |
| 직접 입력 너무 먼 미래/과거 (±1년 이상) | 경고 + 확인 |
| 그 날짜 수업 클래스 0건 | "수업 없음" 종료 |
| 클래스 1건 자동 선택 부정 | 종료 |
| 클래스에 수강생 0건 | "수강생 없음" 종료 |
| 학생별 기존 row 2건+ (드문 케이스) | 가장 최근 row 사용 + 사용자 알림 |
| 보호 학생 N명 — 1 (덮어쓰기) | 보호 학생 포함 모두 patch |
| 보호 학생 N명 — 2 (skip) | 보호 학생 제외 처리 |
| 보호 학생 N명 — 3 (취소) | 즉시 종료 |
| 수업내용 빈 칸 | "비었음, 진행?" 확인 |
| 미리보기 부정 답변 | 즉시 종료, 노션 변경 0 |
| 어느 단계든 "취소"·"q" | 즉시 종료 |

## 주의사항

- **확인 없는 자동 쓰기 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 노션에 절대 POST 금지
- **같은 반 = 동일 수업내용**: v1에서는 학생별 차별화 안 함. 학생별 피드백은 `/수업결과`로 별도
- **수업타입 자동 결정**: 시간표 매칭 결과 따름. 정규반·보강수업 모두 같은 명령어로 처리
- **학생별 row 1건씩**: 같은 클래스라도 학생마다 분리된 row (출결 설계 메모리)
- **수업내용 다듬기 → 학부모 본문**: 수업계획.수업내용은 일일피드백 DB의 `수업내용` rollup으로 학부모 발송 본문에 그대로 사용됨. 따라서 학부모 친근체로 다듬기 필수. 사용자가 "원본 그대로" 선택 시에만 raw 그대로 저장
- **다듬기 수정 무제한**: 사용자가 "이대로" 또는 "원본 그대로" 선택할 때까지 반복
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (메모리 `feedback_yes_no_korean`)
- **터미널 default**: 엔터 사용 안 함. 번호 선택만 (메모리 `feedback_terminal_default_input`)

## 예시

**입력**
- 날짜: 2 (내일 — 2026-05-11 월)
- 클래스: 초등 코딩반(이승환) (정규수업)
- 수강생 자동 추출: 허경민 (1명)
- 기존 row: 허경민 0건 (신규)

**raw 입력**
```
파이썬 첫 수업인데 print로 출력하기 연산자 자료형 input으로 입력받기를 배울예정
```

**Claude 다듬기 (학부모 친근체)**
```
📝 다듬은 버전 (학부모용 친근체):
오늘은 파이썬 첫 시간이에요. 화면에 글자 출력하기(print), 더하기·빼기 같은 연산자,
숫자·글자 같은 자료형, 그리고 키보드로 입력 받기(input)를 함께 배울 예정입니다.

이대로 진행할까요?
  1. 네, 이 버전으로 저장
  2. 수정할 부분이 있어요 (의견 입력)
  3. 원본 그대로 저장 (다듬기 안 함)
번호 선택:
```

**사용자**: 1 (이 버전 채택)

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📋 수업계획 작성 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[수업]
- 날짜:      2026-05-11 (월)
- 수업타입:  정규수업
- 클래스:    초등 코딩반(이승환)

[대상 학생 1명]
  ✨ 신규   허경민 (신규 row 생성)

[수업내용 — 다듬은 버전]
오늘은 파이썬 첫 시간이에요. 화면에 글자 출력하기(print), 더하기·빼기 같은 연산자,
숫자·글자 같은 자료형, 그리고 키보드로 입력 받기(input)를 함께 배울 예정입니다.

[처리]
- 동작:        수업계획·수업요약 DB에 row 1건 신규 생성
- 다듬기:      적용 (수정 0회)
- 후속 작업:   /출석·/결석으로 수업계획 자동 link, 학부모 발송 시 이 내용 본문 사용

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

**노션 결과**
- 수업계획·수업요약 DB에 허경민 row 1건 추가 (수업내용=다듬은 버전)
- 일일피드백 DB의 `수업내용` rollup이 자동으로 이 내용을 학부모 발송 본문에 가져옴
