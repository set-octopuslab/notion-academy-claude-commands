---
description: 학원관리 config를 수동 입력으로 작성·보강 (자동 매칭에 실패한 항목용 수동 fallback)
---

# 노션 학원관리 초기 설정 (수동 입력)

사용자가 `/노션-초기설정` 을 실행하면, 학원관리 config 파일에 들어갈 값들(노션 DB ID 17개 + 노션 Integration Secret + 학원 고유값 + 디스코드 웹훅)을 **하나씩 직접 입력**받아 저장한다.

> 본 명령어는 `/노션-템플릿연결` 의 **수동 fallback** 입니다. 자동 매칭(`/노션-템플릿연결`)이 일부 또는 전체 DB 매칭에 실패하거나, 사용자가 ID·웹훅을 직접 채우고 싶을 때 사용하세요. 일반적인 첫 셋업은 `/노션-템플릿연결` 을 권장합니다.

> 출력 config 구조는 `/노션-템플릿연결` 과 100% 동일합니다 (`_version: 5`, 영문 키, `notion.data_sources.*` 구조). 21개 일반 명령어가 모두 같은 키를 참조합니다.

## 저장 위치

- **Config 파일 기본 위치**: `~/.claude/notion-academy-config.json`
  - Windows: `C:\Users\<사용자>\.claude\notion-academy-config.json`
  - macOS·Linux: `$HOME/.claude/notion-academy-config.json`
- 이 파일이 이후 모든 학원관리 커맨드의 진입점.
- 기존 파일이 있으면 **백업** (`notion-academy-config.json.bak.YYYYMMDD-HHMM`) 후 누락분만 보강하거나 전체 덮어쓰기. 시작 시 사용자에게 선택받음.

## 절차

### 1. 사전 확인

- 필요한 MCP 도구 로드:
  ```
  select:mcp__notionApi__API-get-self
  ```
- `mcp__notionApi__API-get-self` 호출해 노션 MCP 연결 확인. 실패 시 "노션 MCP 연결이 필요합니다. setup/setup-notion.md 또는 노션 통합 설정을 먼저 진행해주세요." 안내 후 중단.
- 기존 config 파일 존재 여부 확인:
  - 없음 → 신규 작성 모드
  - 있음 → "기존 파일이 있습니다. (1) 누락 항목만 보강 (2) 전체 다시 입력 (3) 취소" 사용자에게 선택받음. 어느 모드든 진행 전 백업 1부 생성.

### 2. 노션 DB ID 17개 입력

`/노션-템플릿연결` 의 자동 매칭이 실패한 키에 대해 사용자에게 직접 ID를 받음. 한 번에 한 키씩.

**대상 키 17개 (영문 키, 21개 명령어가 모두 참조)**:

| config 키 | 노션 DB 제목 예시 |
|---|---|
| `students` | 학생 |
| `parents` | 학부모 |
| `teachers` | 선생님 |
| `classes` | 클래스관리 |
| `class_enrollments` | 수강 등록 |
| `regular_schedule` | 정규수업 시간표 |
| `makeup_schedule` | 보강수업 시간표 |
| `lesson_results` | 개인 수업결과 (또는 일일피드백) |
| `lesson_plans` | 수업계획·수업요약 |
| `weekly_monthly_feedback` | 주간 or 월간피드백 |
| `monthly_tuition` | 월간 수강료 |
| `tuition_lines` | 월간 수강료 데이터 |
| `consultations_booking` | 상담예약 |
| `consultations` | 상담결과 |
| `academy_calendar` | 학원 캘린더 |
| `refunds` | 환불 |
| `supplies` | 운영 물품구매 |
| `boards` | 게시판 |

각 키마다 사용자에게 묻기:

```
[키 입력] notion.data_sources.<키> (<DB 제목 예시>)
사용자의 노션 워크스페이스에서 해당 DB의 data_source_id 를 알려주세요.
(노션 UI: 해당 DB → 우상단 ··· → "데이터 원본 ID 복사" 또는 URL 끝의 32자 hex)
모르거나 사용 안 하면 "없음" 입력 (해당 DB를 쓰는 명령어만 동작 안 함)
```

- 입력값을 dash 형식(`8-4-4-4-12`)으로 정규화
- "없음" / 빈 값 → `null` 로 저장
- 형식 검증 실패 시 재입력

### 2-1. 게시판 database_id 추가 수집 (선택)

`/노션-게시판` 명령어는 일부 호출에 data_source_id 대신 database_id 가 필요합니다. `boards` 입력 시 자동으로 한 번 더 묻기:

```
게시판 DB의 database_id 도 알려주세요. (data_source_id 와 별도)
모르면 "없음" — /노션-게시판 의 일부 기능만 동작 안 함
```

→ `notion.boards_database_id` 에 저장.

### 3. 노션 API token (Integration Secret)

```
노션 Internal Integration Secret (ntn_ 으로 시작) 을 알려주세요.
- 사진 자동 첨부(/주간피드백·/월간피드백 file_upload)에 필요
- MCP 만 쓰는 경우 비워둬도 OK ("없음" 입력)
- 발급 방법: https://notion.so/profile/integrations
```

빈 값·"없음" → `""` 저장.

### 4. 학원 고유값 입력 (한 번에 한 질문)

#### 4-1. 학원명

```
학원명을 알려주세요. (예: 행복코딩학원)
이 값은 메시지·게시판 안내에 노출될 수 있습니다.
```

→ `academy.name` 에 저장.

#### 4-2. 매월 결제 기준일

```
매월 며칠을 결제 안내일로 표시할까요? (1~31, 기본 1)
- /결제선생 메시지에 "매월 X일에 결제 부탁드립니다" 같이 들어갑니다
```

→ `tuition._매월_결제기준일` 에 저장. 1~31 범위 검증.

#### 4-3. 학부모 안내 디스코드 웹훅

```
학부모 안내(수강료·일일피드백) 발송용 디스코드 채널 웹훅 URL을 알려주세요.
- 디스코드 채널 설정 → 연동 → 웹훅 → 새 웹훅 → URL 복사
- 안 쓰면 "없음" (디스코드 발송 기능 비활성)
```

`https://discord.com/api/webhooks/...` 형식 가볍게 검증. 빈 값·"없음" → `""` 저장. → `discord.parent_webhook`.

#### 4-4. 디스코드 세션 저장 웹훅 (선택)

```
세션 저장 알림용 디스코드 채널 웹훅 URL을 알려주세요 (선택).
- /디코저장 명령어가 이 채널로 재개 메시지를 보냅니다
- 학부모 채널과 반드시 분리하세요 (학부모 메시지가 잘못 가지 않게)
- 안 쓰면 "없음"
```

→ `discord.session_save_webhook` 에 저장.

### 5. 수강료 패턴·월별 인사말 (기본값 자동 채움)

수강료 패턴 4종과 월별 인사말 12개는 기본값으로 자동 채움. 사용자가 따로 수정할 수도 있다고 안내:

```
수강료 패턴 4종 (초주1·초주2·중고주1·중고주2)과 월별 인사말 12개는 기본값으로 설정됩니다.
나중에 config 파일을 직접 수정해도 됩니다.
이대로 사용할까요? (네 / 직접 입력)
```

"직접 입력" 선택 시 4개 패턴(`월수강료·월기준·회당단가·할인버퍼`)을 한 패턴씩 묻기. 보통은 "네"로 진행.

### 6. Config 파일 저장

최종 구조 (`/노션-템플릿연결` 과 동일):

```json
{
  "_version": 5,
  "_workspace_root": "<선택, 비워둠 가능>",
  "academy": { "name": "<4-1>" },
  "discord": {
    "parent_webhook": "<4-3>",
    "session_save_webhook": "<4-4>",
    "username": "결제선생"
  },
  "notion": {
    "api_token": "<3>",
    "boards_database_id": "<2-1>",
    "data_sources": {
      "students": "<2>",
      "parents": "<2>",
      "teachers": "<2>",
      "classes": "<2>",
      "class_enrollments": "<2>",
      "regular_schedule": "<2>",
      "makeup_schedule": "<2>",
      "lesson_results": "<2>",
      "lesson_plans": "<2>",
      "weekly_monthly_feedback": "<2>",
      "monthly_tuition": "<2>",
      "tuition_lines": "<2>",
      "consultations_booking": "<2>",
      "consultations": "<2>",
      "academy_calendar": "<2>",
      "refunds": "<2>",
      "supplies": "<2>",
      "boards": "<2>"
    }
  },
  "tuition": {
    "patterns": {
      "초주1": { "월수강료": 200000, "월기준": 4, "회당단가": 50000, "할인버퍼": 0 },
      "초주2": { "월수강료": 350000, "월기준": 8, "회당단가": 50000, "할인버퍼": 1 },
      "중고주1": { "월수강료": 250000, "월기준": 4, "회당단가": 62500, "할인버퍼": 0 },
      "중고주2": { "월수강료": 400000, "월기준": 8, "회당단가": 50000, "할인버퍼": 0 }
    },
    "_매월_결제기준일": <4-2>,
    "_천원_절삭": true
  },
  "month_closing": { "<1~12>": "<기본 인사말>" }
}
```

- Write 도구로 저장. 디렉토리 없으면 만들고 저장.
- 저장 후 경로와 주요 통계(입력된 DB 17개 중 n개, 웹훅 등록 여부, API token 설정 여부)를 사용자에게 요약

### 7. 완료 안내

```
✅ 수동 셋업 완료

📁 Config 파일: ~/.claude/notion-academy-config.json
🏫 학원명: <이름>
📚 입력된 DB: <n>/17
🔔 디스코드 학부모 채널: 연결됨 ✓ / 미설정
🔔 디스코드 세션 저장 채널: 연결됨 ✓ / 미설정
🔑 노션 API token: 설정됨 ✓ / 비어있음 (file_upload 기능 비활성)

이제 다음 명령어를 사용할 수 있습니다:
- /학생-신규등록 — 신규 학생 등록
- /결제선생 --dryrun — 수강료 메시지 미리보기 (실제 발송 X)
- /노션-게시판 — 게시판 글 작성

먼저 /결제선생 --dryrun 으로 수강료 계산이 정상 동작하는지 확인해보세요.
```

## 주의사항

- **`/노션-템플릿연결` 과 결과 파일이 동일**: `_version: 5`, 영문 키, `notion.data_sources.*` 구조. 두 명령어는 같은 파일을 쓰며, 21개 일반 명령어가 모두 이 구조를 참조합니다.
- **민감값 보호**: 노션 API token, 디스코드 웹훅 URL은 응답·로그·메모리 어디에도 출력하지 말 것 (파일에 저장만).
- **Git 커밋 금지**: Config 파일은 민감값(api_token, webhook URL)을 포함하므로 절대 Git에 커밋하지 말 것.
- **누락 DB**: 사용 안 하는 DB(예: 환불·게시판)는 `null` 로 둬도 됨. 해당 DB를 쓰는 명령어만 동작 안 함.
- **중간 중단**: 실행 중 사용자가 중단하고 싶어하면 "지금까지 수집된 값만 저장할게요" 로 제안 후 저장 가능. 누락 항목은 다음 실행 시 보강.
