---
description: 노션 학원관리 워크스페이스 DB ID·디스코드 웹훅을 config에 수동 입력으로 저장 (최초 1회 — 자동 매핑은 /노션-템플릿연결)
---

# 노션 학원관리 초기 설정 (수동 입력)

사용자가 `/노션-초기설정` 을 실행하면, 아래 절차로 워크스페이스 DB ID를 해석해 config 파일에 저장하고 디스코드 웹훅 URL을 등록한다.

> 수강료 체계(v4, 옵션 B)는 **학생 DB의 `초,중,고` 필드 + 정규수업 시간표 개수**로 자동 판별하므로, 반별 설정 주입 단계는 없다.

## 저장 위치

- **Config 파일**: `~/.claude/notion-academy-config.json` (Windows는 `C:\Users\<사용자>\.claude\notion-academy-config.json`)
- 이 파일이 이후 모든 학원관리 커맨드의 진입점. 존재하지 않으면 새로 만들고, 있으면 기존 값 유지하며 누락분만 보강.

## 절차

### 1. 사전 확인

- 필요한 MCP 도구가 로드되지 않았으면 먼저 `ToolSearch` 로 로드:
  - `select:mcp__notionApi__API-get-self,mcp__notionApi__API-post-search`
- `mcp__notionApi__API-get-self` 호출해 연결 확인. 실패 시 `setup/setup-notion.md` 참고 안내 후 중단.

### 2. DB ID 자동 해석

아래 17개 DB를 **이름 기반**으로 `API-post-search` 호출해 찾는다. 검색 파라미터:
```json
{ "query": "<DB이름>", "filter": { "value": "data_source", "property": "object" } }
```

대상 DB 목록:
```
학생, 선생님, 클래스관리,
월간 수강료, 월간 수강료 데이터,
특강 수강료, 특강 수강료 데이터,
개인 수업결과, 일일피드백,
수업계획, 수업요약,
주간 or 월간피드백,
정규수업 시간표, 보강수업 시간표,
상담예약, 상담결과,
게시판, 운영 물품구매, 학원블로그 발행관리
```

처리 규칙:
- 검색 결과가 1건이면 `id` 를 채택
- 여러 건이면 **사용자에게 목록 보여주고 선택받기**
- 0건이면 사용자에게 알리고 해당 항목은 `null` 로 두되 계속 진행
- 결과를 **미리보기로 한 번에** 보여주고 "맞는지" 확인받음

### 3. 디스코드 웹훅 URL 수집

- 이미 config에 `discord_webhook` 이 있으면 "기존 값을 유지/변경/삭제" 중 선택
- 없으면 다음 안내 후 URL 입력받기:
  > 디스코드 서버 → 채널 설정 → 연동 → 웹훅 → "새 웹훅" → URL 복사 → 여기에 붙여넣기
- URL 포맷 검증: `https://discord.com/api/webhooks/...` 패턴인지 가볍게 확인

### 4. Config 파일 저장

최종 구조:
```json
{
  "version": 1,
  "updated_at": "<ISO 8601>",
  "databases": {
    "학생": "...",
    "선생님": "...",
    "클래스관리": "...",
    "월간 수강료": "...",
    "월간 수강료 데이터": "...",
    "특강 수강료": "...",
    "특강 수강료 데이터": "...",
    "개인 수업결과, 일일피드백": "...",
    "수업계획, 수업요약": "...",
    "주간 or 월간피드백": "...",
    "정규수업 시간표": "...",
    "보강수업 시간표": "...",
    "상담예약": "...",
    "상담결과": "...",
    "게시판": "...",
    "운영 물품구매": "...",
    "학원블로그 발행관리": "..."
  },
  "discord_webhook": "https://discord.com/api/webhooks/...",
  "tuition": {
    "version": "v4",
    "round_to": 10000,
    "patterns": {
      "초주1": { "월수강료": 200000, "월기준": 4, "회당단가": 50000, "할인버퍼": 0 },
      "초주2": { "월수강료": 350000, "월기준": 8, "회당단가": 50000, "할인버퍼": 1 },
      "중고주1": { "월수강료": 250000, "월기준": 4, "회당단가": 62500, "할인버퍼": 0 },
      "중고주2": { "월수강료": 400000, "월기준": 8, "회당단가": 50000, "할인버퍼": 0 }
    }
  }
}
```

- Write 도구로 기록. 디렉토리 없으면 만들고 저장.
- 저장 후 경로와 주요 통계(17개 DB 중 n개 해석됨, 웹훅 등록 여부)를 사용자에게 요약

### 5. 완료 안내

- "설정 완료. 이제 `/결제선생-발송 --dryrun` 으로 계산 검증해보세요."
- 수강료 패턴은 학생 DB의 `초,중,고` + 정규수업 시간표에서 자동 판별되므로 별도 입력 불필요

## 주의사항

- `API-post-search` 는 페이지·데이터베이스를 함께 검색하므로 결과에서 `object == "data_source"` 인 것만 필터
- Config 파일은 민감한 웹훅 URL을 포함하므로 **Git 커밋 금지**
- 실행 중 사용자가 중단하고 싶어하면 "지금까지 수집된 값만 저장할게요" 로 제안 후 저장 가능
