---
description: 재원·휴원 학생을 퇴원 상태로 변경 + (선택) 미납·환불 검토 안내. 데이터 일체 보존
---

# 학생-퇴원처리

원장이 `/학생-퇴원처리` 를 입력하면, 학생 이름을 검색해서 1명 선택하고, **사유·퇴원일을 받은 뒤 미리보기 → 확인(네/아니오)**, 그 학생의 학생 DB row 1개를 `등록현황 = 퇴원` 으로 patch한다. 그 후 **미납·환불 정산도 검토할지 묻고**, 긍정이면 월간 수강료 미납 현황 + 이번달 환불 검토 결과(두 가지 정산식)를 표시한다.

학생 row 1개 patch만 실제 변경. 시간표·청구서·피드백·환불 기록 등 연결된 모든 데이터는 그대로 보존. 미납·환불의 실제 처리는 사용자가 노션 UI에서.

집/학원/서버 어디서든 실행 가능.

## 범위

**포함 (실제 patch)**
- 학생 검색 (등록 + 휴원)
- `등록현황` → "퇴원" patch
- (선택) `이탈일` patch
- (선택) `이탈사유` patch (rich_text)

**포함 (조회·안내만)**
- 미납 청구서 조회 (월간 수강료만)
- 환불 검토 대상이면 두 가지 정산 결과 표시 (남은 횟수 / 학원법 시행령 별표 4)
- 진행 횟수 산정 (일일피드백 + 보강수업 시간표)
- 할인 적용 청구서는 자동 계산 생략, 별도 계산 안내만

**제외 (사용자 직접 처리)**
- 시간표 row 수정
- 청구서 row patch (미납 회수, 독촉 단계 변경 등)
- 환불 관리 DB row 생성
- 학부모 알림 발송
- 특강 수강료 검토 (특강 중 퇴원 케이스 없음)

## 사전 조건

- 학생 DB의 `이탈사유` 필드가 **rich_text** (텍스트 타입). select 타입이면 사용자에게 노션 UI에서 텍스트로 변경 안내

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-patch-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.students` 학생 DB ID
   - `notion.data_sources.monthly_tuition` 월간 수강료 종합 DB ID
   - `notion.data_sources.tuition_lines` 월간 과정별 수강료 DB ID
   - `notion.data_sources.lesson_results` 일일피드백 DB ID
   - `notion.data_sources.makeup_schedule` 보강수업 시간표 DB ID

## 절차

### 1. 학생 검색

```
[1/3] 퇴원 처리할 학생 이름은? (일부만 입력해도 됨)
```

쿼리:
- `학생이름 contains <입력>` AND `등록현황 in (등록, 휴원)`

분기:
- 0건 → `❌ 등록·휴원 중에 '<입력>' 이름 없음` 종료
- 1건 → 자동 선택 + 정보 표시:
  ```
  ✅ 1건 찾음
  - {학생이름} ({학교} {학년}학년, 등록현황: {등록 or 휴원})
  맞나요? (네/아니오)
  ```
  답변 인식: 메모리 `feedback_yes_no_korean` 규칙 따름
- 2건+ → 목록 번호 매겨 보여주고 선택받음

### 2. 퇴원 사유

```
[2/3] 퇴원 사유? (자유 입력, 모르거나 안 받았으면 엔터)
```

- 자유 입력. rich_text 필드라 어떤 텍스트도 OK
- 엔터/빈 값이면 필드 비움

### 3. 퇴원일

```
[3/3] 퇴원일? (YYYY-MM-DD, 엔터 시 오늘)
```

- "오늘"·엔터 → 오늘 날짜 자동 적용
- "내일"·"다음주 월요일" 같은 자연어 → YYYY-MM-DD로 정규화 후 사용자 확인
- 형식 오류 시 재입력
- 미래 날짜 OK
- 과거 날짜는 경고 후 확인

### 4. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📌 퇴원 처리 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:      {학생이름}
- 학교/학년: {학교} {학년}학년
- 현재 상태: {등록 or 휴원}

[변경 사항]
- 등록현황:  {등록·휴원} → 퇴원
- 이탈일:    {퇴원일 or "(없음)"}
- 이탈사유:  {사유 or "(없음)"}

(학생 row 1개만 patch. 시간표·청구서·피드백·환불 기록 등 연결 데이터는 일체 보존)
━━━━━━━━━━━━━━━━━━━━━━

⚠️ 노션 학생 DB의 row 1개가 업데이트됩니다.
진행할까요? (네/아니오)
```

### 5. 노션 patch

`API-patch-page` 1회 호출:

```json
{
  "page_id": "<학생 page_id>",
  "properties": {
    "등록현황": {"select": {"name": "퇴원"}},
    "이탈일": {"date": {"start": "<퇴원일>"}},                                    // 입력 있을 때만
    "이탈사유": {"rich_text": [{"text": {"content": "<사유>"}}]}                  // 입력 있을 때만
  }
}
```

빈 값은 properties에서 키 omit.

### 6. 1차 결과 보고 + 미납·환불 검토 분기

```
✅ 퇴원 처리 완료
- 학생: {학생이름} (https://notion.so/<page-id>)
- 등록현황: {이전} → 퇴원
- 이탈일: {퇴원일 or "(없음)"}
- 이탈사유: {사유 or "(없음)"}

📊 미납·환불 정산도 검토할까요? (네/아니오)
```

부정 응답 → 종료.
긍정 응답 → 7~9단계 진행.

### 7. 미납 검토 (월간 수강료만)

쿼리: 월간 수강료 종합 DB
- `학생 relation contains <학생 page_id>`
- 정렬: `수강료 년/월` asc

각 row 분석:
- `미납금액` formula 값 확인
  - `> 0` → 미납 (그 금액)
  - `== 0` → 결제 완료
  - `< 0` → 과납 (환불 검토 대상에 합산)

특강 수강료 DB는 조회하지 않음 (특강 중 퇴원 케이스 없음).

청구서 0건이면 → "청구서 없음" 표시.

### 8. 환불 검토 (이번달 청구서만)

이번달 = 퇴원일이 속한 월.

#### 8-1. 검토 대상 식별

이번달 월간 수강료 종합 row 1개 (있으면).

대상 조건:
- `수강료 년/월 == 퇴원일이 속한 월` AND
- `미납금액 == 0` (결제 완료)

미납이 있는 청구서는 환불 대상 아님. 미납 회수 안내만.

**할인 적용 청구서 처리**:
- 헤더 `할인금액 > 0` 또는
- 라인 중 어느 하나라도 `추가 할인 금액 > 0` 또는
- `할인 사유 != "없음"` 이면
- 자동 계산 **하지 않음**. "할인 적용됨, 별도 계산 필요" 안내 + 진행 횟수만 표시

#### 8-2. 진행 횟수 산정

이번달 월간 수강료 종합의 `라인 합계` 가 있는 경우, 라인별로 진행 횟수 계산.

각 라인(월간 과정별 수강료 row)의 `반` relation → 그 반의 진행 횟수:

```
이번달 진행 횟수 =
  ① 이번달 정규 출석 수
     일일피드백 DB 쿼리:
       학생 직접 == <학생 page_id>
       AND 수업일자 in [이번달 1일 ~ 퇴원일]
       AND 정규/보강 == "정규"
       AND 출결 == "출석"
       (가능하면 시간표 → 클래스 == 라인의 반 까지)
     → row 수

  + ② 이번달 결석의 이번달 내 보강 출석 수
     일일피드백 DB 쿼리:
       학생 직접 == <학생 page_id>
       AND 수업일자 in [이번달 1일 ~ 퇴원일]
       AND 정규/보강 == "보강"
       AND 출결 == "출석"
     각 row마다:
       - 보강수업 시간표 DB에서 매칭 (수강생 == 학생 AND 보강수업 날짜 == 일일피드백 수업일자)
       - 매칭된 row의 `원본 결석 회차` relation → 일일피드백 row 조회
       - 그 원본 회차의 `수업일자`가 이번달 안이면 카운트
       - 매칭 안되거나 원본 비었으면 안전상 카운트 (지난달 보강 아니라고 가정)
```

#### 8-3. 두 가지 정산식 계산

라인별로:

```
잔여 회차 = 수업 예정 횟수 - 진행 횟수 (음수면 0)
```

**남은 횟수 기준**:
```
환불액(횟수) = 잔여 회차 × 회당 단가
```

**학원법 시행령 별표 4 기준** (1개월 이내 가정 — 월간 청구 기준):
```
진행률 = 진행 횟수 / 수업 예정 횟수

if 진행률 == 0:    환불 100% (= 라인 단가 전액)
elif 진행률 < 1/3: 환불 = 라인 단가 × 2/3
elif 진행률 < 1/2: 환불 = 라인 단가 × 1/2
else:              환불 0
```

라인별 결과 합산 → 청구서 단위 환불 가능액.

(할인 적용 청구서는 위 자동 계산 건너뛰고 "별도 계산 필요" 안내만 표시)

### 9. 정산 결과 표시

**표준 케이스** (미납·환불 둘 다 있음):
```
━━━━━━━━━━━━━━━━━━━━━━
💰 수강료 정산 결과
━━━━━━━━━━━━━━━━━━━━━━

[월간 수강료 — 미납 현황]
- 2026-04: 결제완료 ✅
- 2026-05: 미납 350,000원 ⚠️

[합계]
- 총 미납: 350,000원 (학부모와 별도 회수 협의 필요)

[환불 검토 — 이번달(2026-05) 청구서, 결제완료 350,000원]

▶ 라인 1: 초등반 주 1회 수업
   - 수업 예정 8회, 진행 5회 (정규 4 + 이번달 결석 보강 1)
   - 잔여 3회

   • 남은 횟수 기준
     잔여 3회 × 50,000원 = 150,000원

   • 학원법 기준 (시행령 별표 4)
     진행률 62.5% (1/2 초과) → 0원

→ 환불액 결정 후 환불 관리 DB에 row 직접 추가
   (학원법 정산식 필드에 적용 공식 기재 권장)

(노션 변경은 학생 row만. 청구서·환불 row는 사용자가 직접 처리)
━━━━━━━━━━━━━━━━━━━━━━
```

**할인 적용 청구서 케이스**:
```
[환불 검토 — 이번달(2026-05) 청구서, 결제완료 320,000원]

⚠️ 할인 적용된 청구서입니다 (할인사유: 형제할인, 할인금액: 30,000원)
   자동 계산 생략. 할인 정책 따라 별도 계산 필요.
   참고용으로 진행 횟수만 표시:
   - 라인 1: 수업 예정 8회, 진행 5회, 잔여 3회
```

**검토 대상 없음 케이스** (청구서 0건):
```
━━━━━━━━━━━━━━━━━━━━━━
💰 수강료 정산 결과
━━━━━━━━━━━━━━━━━━━━━━

[월간 수강료 — 미납 현황]
청구서 없음

[환불 검토 — 이번달({YYYY-MM}))]
이번달 청구서 없음 → 환불 검토 대상 없음

→ 정산 완료, 추가 처리 없음
   (학생 상태만 퇴원으로 변경됨)
━━━━━━━━━━━━━━━━━━━━━━
```

미납 청구서가 있는데 환불 대상이 아닌 경우 → "미납 회수 안내"만 표시.
환불 대상도 미납도 없으면 → "정산 완료, 추가 처리 없음" 표시.

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 학생 못 찾음 | "등록·휴원 중에 '<입력>' 이름 없음" 안내 후 종료 |
| 동명이인 (등록·휴원 2건+) | 목록 번호 매겨 선택 |
| 이미 퇴원/졸업 상태 | 검색 필터(`등록현황 in (등록, 휴원)`)에서 제외되어 안 잡힘 |
| 이번달 청구서 자체가 없음 | "이번달 청구서 없음 → 환불 검토 대상 없음" 안내 |
| 청구서 모두 미납 | 환불 대상 0, 미납 회수 안내만 |
| 할인 적용 청구서 (헤더 할인금액 > 0 또는 라인 추가 할인 > 0 또는 할인 사유 ≠ "없음") | 자동 계산 생략. "별도 계산 필요" 안내 + 진행 횟수만 표시 |
| 일일피드백 보강 row와 보강수업 시간표 매칭 실패 | 안전상 카운트 (지난달 보강 아니라고 가정) |
| 진행 횟수가 수업 예정 횟수 초과 | 잔여 0회 처리, 환불 0원 |
| 퇴원일이 미래 | 진행 횟수는 퇴원일 기준이 아닌 오늘 기준으로 계산 (사용자에게 안내) |
| 퇴원일 형식 오류 | 재입력 |
| 자연어 날짜 입력 ("오늘"·"내일") | YYYY-MM-DD 정규화 후 사용자 확인 |
| "취소"·"q" 또는 부정 답변 | 즉시 종료, 노션 변경 0 |

## 답변 인식 (네/아니오)

메모리 규칙 `feedback_yes_no_korean` 적용:
- 긍정: 네·예·응·맞아·맞습니다·이대로·진행·오케이·OK·y·Y·yes·좋아·좋습니다·해줘
- 부정: 아니오·아니·아뇨·아냐·잘못·취소·다시·n·N·no·안할래·취소해·중지
- 모호하면 한 번 더 명시적으로 묻기

## 주의사항

- **확인 없는 자동 patch 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 노션에 절대 PATCH 금지
- **빈 값 처리**: properties에서 키 omit
- **이탈사유 필드 타입**: 본 명령어는 rich_text 가정. select 타입이면 사용자에게 노션 UI 변경 안내 (스키마 변경은 사용자 직접)
- **퇴원 ≠ 휴원**: 등록현황만 "퇴원". 시간표·청구서는 그대로 보존 (`/학생-휴원처리`와 동일 정책)
- **미납·환불은 안내만**: 청구서 patch·환불 관리 DB row 생성은 사용자가 직접. 본 명령어는 검토 결과 표시까지만
- **할인 적용 청구서**: 자동 계산 생략. 학원의 할인 정책마다 환불 시 처리 방식이 달라 일률 계산 위험
- **결제선생·일일피드백 영향**: 퇴원 처리 즉시 다음 발송부터 자동 제외됨 (등록현황 필터)

## 예시

**입력**
- 학생 이름: 추득만
- 퇴원 사유: 전학으로 인한 퇴원 멀리이사감
- 퇴원일: 2026-05-05
- 미납·환불 검토: 네

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📌 퇴원 처리 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:      추득만
- 학교/학년: 목동초등학교 6학년
- 현재 상태: 휴원

[변경 사항]
- 등록현황:  휴원 → 퇴원
- 이탈일:    2026-05-05
- 이탈사유:  전학으로 인한 퇴원 멀리이사감

(학생 row 1개만 patch. 시간표·청구서·피드백·환불 기록 등 연결 데이터는 일체 보존)
━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

**1차 결과**
```
✅ 퇴원 처리 완료
- 학생: 추득만 (https://notion.so/358ca39da84381c49105f7987951bb75)
- 등록현황: 휴원 → 퇴원
- 이탈일: 2026-05-05
- 이탈사유: 전학으로 인한 퇴원 멀리이사감

📊 미납·환불 정산도 검토할까요? (네/아니오)
```

**정산 결과 (검토 대상 없음 케이스)**
```
━━━━━━━━━━━━━━━━━━━━━━
💰 수강료 정산 결과
━━━━━━━━━━━━━━━━━━━━━━

[월간 수강료 — 미납 현황]
청구서 없음

[환불 검토 — 이번달(2026-05))]
이번달 청구서 없음 → 환불 검토 대상 없음

→ 정산 완료, 추가 처리 없음
   (학생 상태만 퇴원으로 변경됨)
━━━━━━━━━━━━━━━━━━━━━━
```

**노션 결과**
- 학생 row 1개 patch
- `등록현황` → 퇴원 (빨간색)
- `이탈일` → 2026-05-05
- `이탈사유` → "전학으로 인한 퇴원 멀리이사감"
- 청구서·시간표·피드백 row 변경 없음
