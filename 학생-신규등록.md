---
description: 신규 학생을 학생 DB·학부모 DB에 한 번에 등록 (대화식, 미리보기·확인 후 생성)
---

# 학생-신규등록

원장이 `/학생-신규등록` 을 입력하면, 학생 정보와 학부모 정보(1~2명)를 **대화식 1개씩 질문**으로 받고, **미리보기 → 확인(네/아니오)** 후 노션 학생 DB·학부모 DB에 row를 생성하고 학부모-학생 relation을 자동 link한다.

집/학원/서버 어디서든 실행 가능 (학원 설정 파일에만 접근 가능하면 됨).

## 범위

**포함**
- 학생 row 1개 + 학부모 row 1~2개 생성
- 학생.학부모 dual relation 자동 연결
- `등록현황` 자동 "등록"

**제외**
- 반 배정 (`수강중인 클래스` relation): 별도 명령어
- 상담 연결: 신규/재원생 상담 분리 정책으로 미연결
- 동의서 3필드 (`법정대리인 동의 확인`·`동의서 수집일`·`동의 버전`): 미사용
- 첫 수업일이 미정이면 빈 값으로 둠 (사후 별도 갱신)

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-retrieve-a-data-source,mcp__notionApi__API-query-data-source,mcp__notionApi__API-post-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.students` ID 확인
   - `notion.data_sources.parents` ID 확인 (없으면 즉시 중단 + `/노션-초기설정` 안내)

## 절차

### 1. 학생 기본 정보 11개 질문

순서대로 1개씩 질문 → 답변 → 다음. 답변 끝까지 받기 전에는 노션에 아무것도 쓰지 않음.

| # | 질문 | 형식 | 필수 | 비고 |
|---|---|---|---|---|
| 1 | 학생 이름은? | 자유 입력 | ✅ | title |
| 2 | 학교는? (예: 목동초/양천중) | 자유 입력 | ✅ | rich_text |
| 3 | 초/중/고/대 중 어디? | 초·중·고·대 select | ✅ | 학교명에 "초"·"중"·"고" 키워드 있으면 자동 추정 → 사용자 확인 후 진행 |
| 4 | 학년은? (1~6) | 1~6 select | ✅ | |
| 5 | 성별? (남/여, 모르면 엔터) | 남/여 select | 선택 | |
| 6 | 학생 전화번호? | phone_number | 선택 | "없음"·"모름"·엔터 시 비움 |
| 7 | 유입경로? | select | ✅ | 네이버·당근·인스타·블로그·추천·기타·알수없음 (없는 옵션은 노션 API 첫 사용 시 자동 생성) |
| 8 | 실력레벨? | 입문·기본·심화·경시 select | ✅ | 매번 명시적으로 묻기 |
| 9 | 진로희망? | select | 선택 | SW개발자·게임·AI/데이터·로봇·디자인·창업·기타·미정. 모르면 엔터로 비움 |
| 10 | 매월 결제일은? (1~31) | number | ✅ | **기본 1일**, 엔터 시 1 적용 |
| 11 | 첫 수업일 알아요? (YYYY-MM-DD) | date | 선택 | 모르면 엔터로 비움. 미리보기엔 "미정" 표시, 노션엔 빈 값 |

자동 채움:
- `등록현황` = "등록"

### 2. 학부모 정보 입력

학부모 이름은 받지 않음. row의 title은 자동 명명: `"{학생이름} {관계}"` (예: "이서연 어머니").

**1번째 학부모 (필수)**

| # | 질문 | 형식 | 필수 |
|---|---|---|---|
| 1 | 관계? | 어머니·아버지·조모·조부·기타 select | ✅ |
| 2 | 전화번호? | phone_number | ✅ |
| 3 | 주 연락처로 설정? (네/아니오, 엔터 네) | checkbox | 선택 (기본 true) |

**2번째 학부모 (선택)**

```
"두 번째 학부모 입력하시겠어요? (네/아니오)"
```
y → 1번째와 동일한 3개 질문 반복. n → 다음 단계로.

**엣지케이스**
- 두 명 다 주 연락처 y → "두 명 다 주연락처는 안 됩니다. 1번째만 주연락처로 설정합니다." 안내 후 1번째만 true로
- 1번째 학부모 전화번호 비어있음 → "최소 1명 필수, 전화번호 입력 부탁" 재요청

### 3. 동명이인 검사

학생 row 만들기 전에 학생 DB에서 같은 이름 검색:

쿼리: `학생이름 = "<입력 이름>"`

결과 1건 이상이면:
```
⚠️ 같은 이름의 학생이 이미 있습니다.
- 이서연 (목동초 5학년, 등록현황: 등록)
그래도 새 row로 등록할까요? (네/아니오)
```

n → 종료. y → 진행.

### 4. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 신규 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:        {학생이름}
- 학교/학년:   {학교} {학년}학년
- 초/중/고:    {초중고}
- 성별:        {성별 or "(없음)"}
- 학생 전번:   {전번 or "(없음)"}
- 유입경로:    {유입경로}
- 실력레벨:    {실력레벨}
- 진로희망:    {진로 or "(없음)"}
- 매월 결제일: {결제일}일
- 첫 수업일:   {첫수업일 or "미정"}
- 등록현황:    등록 (자동)

[학부모 1] {⭐ 주 연락처 표시}
- {학생이름} {관계} / {전번}

[학부모 2] (선택 입력 시)
- {학생이름} {관계} / {전번}
━━━━━━━━━━━━━━━━━━━━━━

⚠️ 노션 학생/학부모 DB에 실제 row가 생성됩니다.
이대로 등록할까요? (네/아니오)
```

n → "어떤 항목 수정할까요? (1~14)" 또는 "취소"로 종료.

### 5. 노션 row 생성

순서가 중요. 학부모 먼저 → 학생.

**5-1. 학부모 row 생성 (1~2회)**

```json
{
  "parent": {"type": "database_id", "database_id": "<학부모 database_id>"},
  "properties": {
    "학부모이름": {"title": [{"text": {"content": "{학생이름} {관계}"}}]},
    "학생과의 관계": {"select": {"name": "{관계}"}},
    "학부모님 전화번호": {"phone_number": "{전번}"},
    "주 연락처면 ": {"checkbox": <bool>}
  }
}
```

> ⚠️ **필드명 주의**: 학부모 DB의 실제 필드명은
> - `학생과의 관계` (단순 "관계" 아님)
> - `학부모님 전화번호` (단순 "전화번호" 아님)
> - `주 연락처면 ` (trailing space 1칸 포함 — Notion API에 정확히 그대로 전송 필요)

응답에서 `id` 추출.

**5-2. 학생 row 생성 (1회, 학부모 ID 포함)**

```json
{
  "parent": {"type": "database_id", "database_id": "<학생 database_id>"},
  "properties": {
    "학생이름": {"title": [{"text": {"content": "{학생이름}"}}]},
    "학교": {"rich_text": [{"text": {"content": "{학교}"}}]},
    "학년": {"select": {"name": "{학년}"}},
    "초,중,고": {"select": {"name": "{초중고}"}},
    "성별": {"select": {"name": "{성별}"}},                 // 비우려면 omit
    "학생 전화번호": {"phone_number": "{전번}"},            // 비우려면 omit
    "유입경로": {"select": {"name": "{유입경로}"}},
    "실력레벨": {"select": {"name": "{실력레벨}"}},
    "진로희망": {"select": {"name": "{진로}"}},             // 비우려면 omit
    "매월 결제일": {"number": <결제일>},
    "최초등록일": {"date": {"start": "{첫수업일}"}},        // 비우려면 omit
    "등록현황": {"select": {"name": "등록"}},
    "학부모": {"relation": [{"id": "<학부모1_id>"}, {"id": "<학부모2_id>"}]}
  }
}
```

빈 값은 properties에서 키 자체를 omit (null/빈 string 넣지 말 것).

학부모는 dual_property relation이라 학생 쪽에만 link 넣으면 학부모.자녀 자동 sync.

### 6. 결과 보고

```
✅ 등록 완료

[학생]
- 이름: {학생이름}
- URL: https://www.notion.so/{학생 page_id}

[학부모]
- {학생이름} {관계1}
- {학생이름} {관계2}  (있는 경우)

[자동 검증]
✅ 학교 학년(수식) → "{학교} {학년}학년"
✅ 학부모 연락처 (rollup) → "{관계1} : {전번1}\n{관계2} : {전번2}"
✅ 학부모-학생 양방향 relation 정상

다음 권장 작업: 반 배정 (별도 명령어, 미구현)
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 학교명 → 초/중/고 자동 추정 실패 | 명시적 질문 |
| 동명이인 (학생 DB에 같은 이름 row 존재) | 경고 + 기존 row 정보(학교·학년·등록현황) 보여주고 "그래도 등록?" 확인 |
| 전화번호 형식 오류 | 정규식 검증 (`010-XXXX-XXXX` 또는 11자리 숫자), 실패 시 재입력 |
| 결제일 1~31 범위 외 | 재입력 |
| 첫 수업일 과거 날짜 | "과거 날짜인데 맞나요?" 경고 후 확인 |
| 학부모 둘 다 주연락처 y | 1번째만 주연락처, 2번째 false로 강제 |
| 학부모 1명도 전번 비어있음 | "최소 1명 필수" 재요청 |
| 어느 단계든 "취소"·"q" 입력 | 즉시 종료, 노션 변경 0 |

## 주의사항

- **확인 없는 자동 쓰기 금지**: 미리보기 후 사용자 명시적 y 응답 없이 노션에 절대 POST 금지
- **학부모 → 학생 순서**: 학생.학부모 relation에 학부모 ID 넣어야 하므로 반드시 학부모 먼저 생성
- **빈 값 처리**: properties에서 키 omit (null·빈 string 안 됨)
- **테스트용 데이터**: 사용자가 "테스트" 접두사 등을 명시하면 그대로 사용. 사후 정리는 사용자 노션 UI에서
- **유입경로 "알수없음" 옵션**: 학생 DB select 옵션에 없으면 첫 호출 시 노션 API가 자동 생성. 별도 사전 작업 불필요
- **이름 자동 명명 학부모 row**: 한 학생에게 어머니 2명·아버지 2명 같은 케이스는 발생 안 함 가정. 예외 발생 시 사용자가 직접 학부모 DB에서 title 수정

## 예시

**입력**
- 학생: 이서연, 목동초등학교, 초 6학년, 여, 학생 전번 없음, 네이버, 입문, 미정, 결제일 1, 첫 수업일 모름
- 학부모 1: 어머니, 010-1234-0000, 주연락처 y
- 학부모 2 없음

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📋 신규 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:        이서연
- 학교/학년:   목동초등학교 6학년
- 초/중/고:    초
- 성별:        여
- 학생 전번:   (없음)
- 유입경로:    네이버
- 실력레벨:    입문
- 진로희망:    미정
- 매월 결제일: 1일
- 첫 수업일:   미정
- 등록현황:    등록 (자동)

[학부모 1] ⭐ 주 연락처
- 이서연 어머니 / 010-1234-0000
━━━━━━━━━━━━━━━━━━━━━━
이대로 등록할까요? (네/아니오)
```

**노션 결과**
- 학생 row 1개 + 학부모 row 1개 + 양방향 relation
- `학교 학년(수식)` → "목동초등학교 6학년"
- `학부모 연락처` (rollup) → "어머니 : 010-1234-0000"
