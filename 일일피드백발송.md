---
description: 선택한 선생님·날짜의 일일피드백 row를 학부모 디스코드 채널로 일괄 발송 (미입력 수정 통합)
---

# 일일피드백발송

원장이 `/일일피드백발송` 을 입력하면, **선생님 → 날짜 → 발송 대상 분류 → 분기 선택(N건만/수정후 N+M건/취소) → (선택) 미입력 수정 → 미리보기 → 디스코드 발송 → 발송 여부 자동 체크** 순서로 처리한다.

수업 라이프사이클의 마지막 단계 (입학 → 수강등록 → 시간표 → 수업계획 → 출결 → 수업결과 → **일일피드백발송**).

> ⚠️ **Claude 동작 가드 (반드시 준수)**
>
> 1. **AskUserQuestion 도구 사용 금지**. 모든 질문은 일반 텍스트 출력 + 자유 답변 받기.
> 2. **자의적 부연 설명 금지**. .md에 정의되지 않은 안내문 노출 금지.
> 3. **session_save_webhook 절대 사용 금지**. `discord.parent_webhook`만 사용.
> 4. **메시지에 학부모 전화번호 포함 금지** (개인정보 보호).
> 5. **의견 3개 필드 다듬기 패턴 강제** (미입력 수정 시 적용). 다듬기 후 사용자 확인 없이 다음 단계 넘어가지 말 것.
> 6. **다듬기 수정 무제한**.
> 7. **필수는 2개만** — 수업태도 + 수업 내용 선생님 의견. 과제완수도·테스트 점수 등은 옵션 (과제·테스트 없는 날엔 빈 칸이 정상).
> 8. **미입력 수정 시 의견은 raw + AI 둘 다 patch**. 일일피드백 발송용 formula가 AI 필드를 참조하므로 AI 필드를 반드시 채워야 발송 본문이 정상 출력됨. /수업결과와 동일 정책.
>
> ⚠️ **formula 결과 처리 가드 (반드시 준수, 자주 발생하는 오류 방지)**
>
> 9. **formula 결과를 그대로 신뢰**. 노션 `일일피드백 발송용` formula의 `string` 값을 그대로 디스코드 발송 본문으로 사용. 자의적 "null 체크"·"빈 본문 검사"·"본문 짧음 경고" 추가 금지. 명령어 .md에 정의되지 않은 검증 단계 추가 절대 금지.
> 10. **매번 노션 query로 최신 formula 확보**. 발송 직전(7단계 미리보기 단계)에 반드시 query 또는 retrieve로 최신 `일일피드백 발송용` formula 값을 다시 가져올 것. 이전 호출 결과·이전 시도 결과·LLM 메모리 캐시 절대 사용 금지. 노션 데이터는 사용자가 그 사이에 수정했을 수 있음.
> 11. **formula 결과는 절대 null이 될 수 없음**. formula 정의가 `"안녕하세요😊\n○○학원입니다.\n\n" + ...` (학원명 부분은 각 학원이 노션 formula에서 직접 설정) 같은 형태로 시작하므로 항상 인사말 + 본문 + 마무리가 출력됨. 만약 query 결과에서 formula `string` 값이 정말 null이라면 그것은 명령어 측에서 노션 데이터를 잘못 파싱한 것 — 다시 query 호출 권장. 절대 발송 차단 안 함.
> 12. **본문이 짧다고 느껴도 정상**. 노션 row의 일부 필드(과제·테스트 등)가 비어있으면 formula의 if(empty) 분기로 해당 섹션이 출력 안 됨. 이건 정상 동작. 본문에 "▶ 과제" 섹션이 없다고 해서 발송 차단·경고 추가 금지.
> 13. **formula `string` 값을 한 글자도 수정 금지**. 디스코드 발송 시 `일일피드백 발송용` formula의 `property.formula.string` 값을 그대로 메시지 content로 전달. 날짜 표기·따옴표·줄바꿈·이모지·언어(한/영) 등 어떤 형식 변환도 자의적 추가 금지. 본문 형식은 노션 formula 정의로 통제 (운영자가 노션에서 직접 수정).
> 14. **포맷 변경은 노션 측에서**. 학부모 안내 본문의 톤·표기를 바꾸고 싶으면 명령어가 아니라 노션 `일일피드백 발송용` formula expression을 수정해야 함. 예: 영어 날짜 → 한글 날짜는 formula의 `formatDate(prop("수업일자"), "YYYY년 M월 D일")` 으로 처리. 명령어는 노션이 출력한 결과를 단순 전달자 역할만.

## 범위

**입력받을 정보**
- 선생님 (재직 중 목록에서 1명)
- 날짜 (오늘 또는 직접 입력)
- 발송 분기 (1·2·3 선택)
- (선택) 미입력 row M명의 피드백 (수업태도·과제완수도·수업 의견 + 옵션 분기)

**자동 처리**
- 메시지 본문 = 노션 `일일피드백 발송용` formula 그대로 사용
- 발송 성공 시 `발송 여부` checkbox = true patch

**제외**
- 결석·휴강 → `/오늘결석` 별도 명령어
- 주간·월간 → `/주간피드백`·`/월간피드백`

## 사전 조건

- 선생님 DB에 재직 중 선생님 row 존재
- 일일피드백 DB에 발송 대상 날짜 row 존재 (`/출석`·`/결석` 생성)
- config에 `discord.parent_webhook` 설정 완료

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-patch-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.lesson_results`
   - `notion.data_sources.teachers`
   - `discord.parent_webhook`
   - `discord.username`
3. 현재 날짜 기록

## 절차

### 1. 선생님 선택

쿼리: 선생님 DB
- `재직상태 == "재직"`

```
[1/?] 어느 선생님의 일일피드백 발송이에요?
  1. 이승환 (재직, 수업과목: 파이썬, 코딩)
  2. 김선생 (재직, 수업과목: C언어, 자바)
번호 선택:
```

분기:
- 0건 → "재직 중인 선생님이 없습니다" 종료
- 1건 → 자동 선택 + 확인
- 2건+ → 목록 선택. 동명이인 시 전화번호 마스킹·입사일 추가

`target_teacher_id`, `target_teacher_name` 결정.

### 2. 날짜 선택

```
[2/?] 어느 날짜의 발송이에요?
  1. 오늘 ({YYYY-MM-DD})
  2. 직접 입력 (YYYY-MM-DD)
번호 선택:
```

#### 2-1. 직접 입력

```
[?/?] 발송 날짜를 입력해주세요. (YYYY-MM-DD)

> 
```

- 형식 오류 → 재입력
- 미래 날짜 → "⚠️ 미래 날짜인데 맞나요? (네/아니오)" 확인

`target_date` 결정.

### 3. 발송 대상 검색·분류

쿼리: 일일피드백 DB
- `수업일자 == <target_date>` AND `발송 여부 == false`

각 row의 추가 검증 (rollup·select 값 확인):
- `선생님` rollup 이름 == `target_teacher_name`
- `출결` ∈ {"출석", "지각", "조퇴"} (결석·휴강 자동 제외)

#### 3-1. 발송 가능 / 불가 분류

| 분류 | 조건 |
|---|---|
| 발송 가능 (N건) | 위 조건 통과 + 필수 2개 채워짐 (`수업태도` not null AND `수업 내용 선생님 의견` 비어있지 않음) |
| 발송 불가 (M건) | 위 조건 통과 + 필수 2개 중 하나라도 비어있음 |

> **과제완수도·테스트 점수·테스트주제 등은 발송 조건에서 제외**. 과제·테스트가 없는 날엔 빈 칸이 정상. 필수는 수업태도·수업 의견 2개만.

#### 3-2. 0건 처리

N == 0 AND M == 0:
```
❌ {선생님이름} 선생님의 {target_date} 발송 대상이 없습니다.

가능한 원인:
- 그 날짜에 수업이 없었거나
- 출결 처리 안 됨 (/출석·/결석 호출 필요)
- 출결이 결석·휴강 (별도 /오늘결석으로 처리)
- 이미 모두 발송 완료 (재발송은 노션 "발송 여부" 수동 해제 후 다시 호출)

종료 (노션 변경 없음).
```

#### 3-3. 결과 표시

```
[3/?] 검색 결과

✅ 발송 가능 N건 (필수 2개 채워짐):
  1. 추득만 (목동초 5학년, 파이썬 초등 A, 출결: 출석)
  2. 김철수 (목동초 6학년, 파이썬 초등 A, 출결: 지각)
  ...

⚠️ 발송 불가 M건 (필수 미입력):
  - 이영희 (출결: 출석) — 수업태도 비어있음
  - 박철수 (출결: 출석) — 수업 의견 비어있음
```

각 행 끝에 어떤 필수 필드가 비어있는지 표시 (사용자가 어디 입력해야 할지 즉시 파악).

### 4. 발송 분기 선택

```
[?/?] 어떻게 진행할까요?
  1. N건만 발송 (미입력 M건은 다음 기회에)
  2. 미입력 M건 먼저 수정 → 전체 N+M건 발송
  3. 취소
번호 선택:
```

분기:
- "1" → 7단계 (미리보기)로
- "2" → 5단계 (미입력 수정)로
- "3" → 즉시 종료

조건부 노출:
- M == 0 이면 2번 선택지 노출 안 함
- N == 0 이면 1번 선택지 비활성 (2·3만 표시)

### 5. 미입력 row 수정 (분기 2번 선택 시)

M명을 1명씩 차례로 처리. `/수업결과`의 절차를 그대로 적용 (보호 판정 단계 생략 — 비어있는 게 확실).

각 학생 (K = 1, 2, ..., M):

```
━━━ {K}/{M} : {학생이름} (출결: {출결}) ━━━
```

#### 5-1. 수업태도 (필수 1/2)

```
[필수 1/2] 수업태도?
  1. 상  2. 중  3. 하
번호 선택:
```

분기: "1"→상, "2"→중, "3"→하

> 과제완수도는 필수 아님. 과제 분기로 이동 (5-3 참고).

#### 5-2. 수업 내용 선생님 의견 (필수 2/2) — 학부모 친근체 다듬기 강제

##### 5-2-1. raw 입력

```
[필수 2/2] 수업 내용 선생님 의견을 입력해주세요.
       대략적으로 적어주시거나 "수업계획 보고 작성해줘" 같이 요청하시면
       학부모 발송에 적합한 친근한 문체로 다듬어/작성해드립니다.

> 
```

##### 5-2-2. Claude 다듬기 (학부모 친근체)

```
📝 다듬은 버전 (학부모용 친근체):
{다듬은/작성된 텍스트}

이대로 진행할까요?
  1. 네, 이 버전으로 저장
  2. 수정할 부분이 있어요 (의견 입력)
  3. 원본 그대로 저장
번호 선택:
```

##### 5-2-3. 분기

- "1" → 다듬은 버전 채택
- "2" → 수정 의견 입력 → 다시 다듬기 → 분기 반복 (무제한)
- "3" → 원본 채택

#### 5-3. 과제 분기 (옵션)

```
[과제] 오늘 과제 있었나요? (네/아니오)
```

긍정 시 5개 필드 입력:
- 과제완수도 (상/중/하 — 번호 선택)
- 과제 점수 (0~100 또는 "없음")
- 과제 제출여부 (네/아니오 → checkbox)
- 지난과제내용 (텍스트 또는 "없음")
- 과제 관련 선생님 의견 (텍스트, 다듬기 패턴 강제)

#### 5-4. 테스트 분기 (옵션)

```
[테스트] 오늘 테스트 있었나요? (네/아니오)
```

긍정 시 3개 필드 입력:
- 테스트 점수 (0~100 또는 "없음")
- 테스트주제 (텍스트 또는 "없음")
- 테스트 관련 선생님 의견 (텍스트, 다듬기 패턴 강제)

#### 5-5. 내부 메모 분기 (옵션)

```
[특이사항] 내부 메모 있나요? (네/아니오)
```

긍정 시 메모 입력.

#### 5-6. K번째 학생 row patch

각 학생 입력 완료 시점에 즉시 노션 patch (`API-patch-page`):
- 입력받은 필드만 properties에 포함
- **의견 3개는 raw + AI 둘 다 patch** (수업 의견·과제 의견·테스트 의견)
  - raw 필드: 사용자 입력 원본
  - AI 필드 (`수업 내용 선생님 의견AI`·`과제 관련 선생님 의견AI`·`테스트 관련 선생님 의견AI`): 최종 채택 텍스트 (다듬은 버전 또는 원본)
- 출결 절대 patch 금지

```json
// 예시 (필수 + 수업 의견 patch 시)
{
  "page_id": "<일일피드백 page_id>",
  "properties": {
    "수업태도": {"select": {"name": "<상/중/하>"}},
    "수업 내용 선생님 의견":   {"rich_text": [{"text": {"content": "<raw 원본>"}}]},
    "수업 내용 선생님 의견AI": {"rich_text": [{"text": {"content": "<최종 채택>"}}]}
    // 과제 분기 시 과제 6개 필드 (raw + AI 포함) 추가
    // 테스트 분기 시 테스트 4개 필드 (raw + AI 포함) 추가
    // 내부 메모 시 1개 필드 추가
  }
}
```

K+1번째 학생으로.

#### 5-7. 중간 취소 처리

"취소"·"q" 입력 시 → K-1번째까지 patch 유지하고 즉시 종료 (발송 단계로 진행 안 함). 발송은 다음 호출에서.

### 6. 노션 재조회 (분기 1번·2번 모두 적용 — 필수)

> ⚠️ **매번 재조회 필수**. 분기 1번이든 2번이든 이 단계는 건너뛰지 말 것. 3단계 검색 결과를 캐시로 사용하면 사용자가 그 사이에 노션을 수정했을 가능성 누락됨.

7단계 미리보기 직전에 N+M건 row를 노션에서 다시 query.

쿼리: 일일피드백 DB
- `수업일자 == <target_date>` AND `발송 여부 == false`
- (선생님·출결 조건 동일)

각 row의 `일일피드백 발송용` formula 텍스트(`property.formula.string`) 추출 → 발송 본문으로 사용.

#### 6-1. formula 결과 확인 — 정상성 검증

가져온 formula `string` 값은 다음 조건을 만족해야 정상:
- 비어있지 않음 (length > 0)
- "안녕하세요😊"로 시작 (formula 정의상 항상 포함)

만약 formula `string` 값이 `null`·빈 문자열·인사말 누락이라면:
- → 노션 query 결과 파싱 오류 가능성 → 한 번 더 query 재시도
- → 그래도 동일하면 사용자에게 정확한 row page_id + 노션 링크 보여주고 "이 row의 formula를 노션에서 직접 확인해주세요" 안내
- → **절대 자의적으로 발송 차단·"본문 비어있음" 경고 발생시키지 말 것**

### 7. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 일일피드백 발송 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[발송 정보]
- 선생님:    {target_teacher_name}
- 날짜:      {target_date}
- 발송 대상: {총 건수}건

[발송 채널]
- 디스코드:  parent_webhook (학원 학부모 안내 채널)
- 발송자:    {discord.username}

[메시지 미리보기 — 1번째 학생: {학생이름}]
━━━━━━━━━━━━━━━
{일일피드백 발송용 formula 텍스트 그대로}
━━━━━━━━━━━━━━━

... (전체 메시지는 길어지므로 1번째만 미리보기로 표시)
... 총 {총 건수}건의 메시지가 1초 간격으로 순차 발송됩니다.

[발송 후 자동 처리]
- 발송 성공 row의 "발송 여부" checkbox = true 자동 patch
- 발송 실패 시 부분 발송 + 실패 row 보고

ℹ️ 재발송이 필요하면 노션에서 해당 row의 "발송 여부" 체크박스를
   수동으로 해제한 뒤 /일일피드백발송 다시 호출해주세요.

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

부정 또는 "취소" → 즉시 종료 (5단계에서 patch한 미입력 row는 유지).

### 8. 디스코드 발송

#### 8-1. 학생별 순차 발송 (PowerShell)

각 row에 대해 (`config.discord.parent_webhook` 사용):

```powershell
$webhook = $config.discord.parent_webhook
$username = $config.discord.username
$content = $row_formula_text
$payload = @{ content = $content; username = $username }
$json = $payload | ConvertTo-Json -Compress -Depth 5
$bytes = [System.Text.Encoding]::UTF8.GetBytes($json)
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.SecurityProtocolType]::Tls12
try {
    Invoke-RestMethod -Uri $webhook -Method Post -Body $bytes -ContentType 'application/json; charset=utf-8' -TimeoutSec 30 -ErrorAction Stop
    # 성공
    Start-Sleep -Seconds 1  # rate limit 회피
} catch {
    # 실패 — 실패 목록에 추가, 다음 row 계속
}
```

#### 8-2. 발송 실패 처리

webhook 4xx·5xx 응답 또는 timeout:
- 그 row는 `발송 여부` patch 안 함
- 실패 row를 별도 목록에 수집
- 다음 row 계속 (전체 중단 안 함)

429 (rate limit) 발생 시:
- 5초 대기 후 그 row 재시도. 3회 실패하면 그 row 실패 처리

401·403 (인증 오류):
- 즉시 전체 중단. "webhook URL 오류, /노션-초기설정 확인" 안내 후 종료

#### 8-3. 발송 성공 시 patch (`API-patch-page`)

```json
{
  "page_id": "<일일피드백 page_id>",
  "properties": {
    "발송 여부": {"checkbox": true}
  }
}
```

### 9. 결과 보고

```
✅ 일일피드백 발송 완료

[발송 결과]
- 성공:    {성공수}건
- 실패:    {실패수}건 (있다면)

[성공 학생]
1. 추득만 — ✅ 발송됨
2. 김철수 — ✅ 발송됨
...

[실패 학생 (있다면)]
- {학생이름} — ❌ 원인: <에러 메시지 요약>
  → "발송 여부"는 false 유지. 원인 해결 후 다시 호출하면 재시도 가능

[자동 처리]
- 성공 {성공수}건의 "발송 여부" 자동 체크 완료
- 실패 {실패수}건은 체크 안 됨 (재시도 시 자동 포함)

ℹ️ 재발송이 필요하면 노션의 "발송 여부" 체크박스 수동 해제 후
   /일일피드백발송 다시 호출

[다음 작업 추천]
- 결석·휴강 학생 안내: /오늘결석 (향후 구현)
- 주간 요약: /주간피드백 (향후 구현)
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 재직 선생님 0건 | "선생님 먼저 등록" 안내 후 종료 |
| 날짜 형식 오류 | 재입력 |
| 미래 날짜 | 경고 + 확인 |
| 발송 대상 0건 (N=M=0) | 안내 후 종료 |
| 발송 가능만 (M=0) | 2번 선택지 노출 안 함 |
| 발송 불가만 (N=0) | 1번 비활성 (2·3만) |
| 분기 부정 답변 (3번) | 즉시 종료 |
| 미입력 수정 중간 "취소" | K-1번까지 patch 유지, 발송 안 함 |
| 미리보기 부정 | 종료 (5단계 patch 유지) |
| 디스코드 401·403 | "webhook 오류, /노션-초기설정 확인" 종료 |
| 디스코드 429 (rate limit) | 5초 대기 후 재시도. 3회 실패 시 그 row 실패 |
| 디스코드 5xx | 그 row 실패 처리, 다음 계속 |
| 발송 후 노션 patch 실패 | 발송은 됐으나 체크 안 됨 → 사용자에게 강력 경고 (다음 호출 시 중복 발송 위험) |
| 다른 선생님 row 자동 제외 | 선생님 rollup 비교로 자동 처리 |
| 어느 단계든 "취소"·"q" | 즉시 종료 |

## 주의사항

- **확인 없는 자동 발송 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 디스코드 발송 금지
- **session_save_webhook 절대 사용 금지**: `parent_webhook` 만 사용
- **메시지에 전화번호 포함 금지**: 디스코드는 공개 채널이라 개인정보 노출 위험
- **선생님 매번 선택**: 자동 인식 안 함. 다인 운영 안전망
- **결석·휴강 자동 제외**: 일일피드백은 수업 결과 안내 본질. 별도 명령어로 분리
- **재발송 정책**: "발송 여부" true row는 검색에서 자동 제외. 재발송은 사용자가 노션에서 수동 해제 후 호출
- **rate limit 회피**: 1초 간격 발송 (디스코드 webhook 30 req/min)
- **부분 발송 시 무결성**: 성공만 체크. 실패 row는 다음 호출 시 자동 재시도
- **다듬기 패턴**: 의견 3개 필드는 학부모 친근체 다듬기 + 확인/수정/원본 분기 강제
- **formula 결과 절대 신뢰**: 노션 `일일피드백 발송용` formula의 `string` 값을 그대로 발송. 자의적 null 체크·빈 본문 경고 절대 추가 금지. 노션 데이터는 사용자가 수정·patch 가능하므로 매번 query로 최신 값 확보
- **이전 시도 결과 캐시 사용 금지**: 같은 명령어를 여러 번 호출할 때 첫 시도 결과를 기억하지 말 것. 매번 노션 query로 새로 조회. 사용자가 그 사이에 노션을 수정했을 가능성 항상 고려
- **본문 짧음·일부 섹션 없음은 정상**: 과제·테스트 없는 날엔 formula의 if(empty) 분기로 해당 섹션 출력 안 됨. 본문에 "▶ 과제" 섹션이 없어도 발송 차단·경고 추가 금지
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (메모리 `feedback_yes_no_korean`)
- **터미널 default**: 엔터 사용 안 함. 번호 선택 또는 키워드만 (메모리 `feedback_terminal_default_input`)

## 예시 1: 정상 발송 (N건만, 미입력 없음)

**입력**
- 선생님: 이승환
- 날짜: 오늘 (2026-05-10)
- 분기: 1번 (N건만)

**검색 결과**: 발송 가능 3건, 발송 불가 0건

**미리보기 + 발송**: 3건 순차 발송 → 모두 성공 → 발송 여부 자동 체크

**결과 보고**: "성공 3건, 실패 0건"

## 예시 2: 통합 발송 (N+M, 미입력 수정)

**입력**
- 선생님: 이승환
- 날짜: 오늘
- 분기: 2번 (수정 후 전체)

**검색 결과**: 발송 가능 2건, 발송 불가 1건 (이영희)

**미입력 수정**: 이영희에 대해 수업태도·과제완수도·수업 의견(다듬기 적용) 입력 → patch

**미리보기 + 발송**: 3건 (추득만·김철수·이영희) 순차 발송 → 모두 성공

**결과 보고**: "성공 3건, 실패 0건"

## 예시 3: 부분 발송 실패

**입력**: 정상 흐름

**발송 중**: 1·2번 성공, 3번 webhook 429 → 5초 대기 후 재시도 → 또 429 → 3회 실패 → 그 row 실패 처리. 4·5번 계속 발송 → 성공

**결과 보고**: "성공 4건, 실패 1건 (이영희 — rate limit). 발송 여부는 4건만 체크. 다음 호출 시 이영희 자동 재시도"

---

## 관련 명령어

- `/수업결과` — 일일피드백 row의 13개 필드 입력 (다듬기 패턴 참고)
- `/오늘결석` (향후) — 결석·휴강 학생 별도 안내
- `/주간피드백` (향후) — 주간 집계 발송
