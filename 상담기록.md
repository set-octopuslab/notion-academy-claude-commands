---
description: 상담 후 결과 기록 (예약 있음/없음 모두, 입력 방식 3분기 — 메모/녹취록/녹음, AI 다듬기·요약 + 검수, 신규상담 등록/미등록/미정 분기, 디스코드 선생님 검수 발송)
---

# 상담기록

원장이 `/상담기록` 을 입력하면, **학생 검색 → 상담예약 row 조회·분기 → (예약 없는 상담만) 메타데이터 입력 → 입력 방식 분기 (메모/녹취록/녹음) → AI 처리 + 검수 → 신규상담 등록 여부 분기 → 미리보기 → 노션 처리 → 디스코드 검수 발송** 순서로 처리한다.

상담결과 DB 메인 처리. 예약된 상담은 상담예약 row의 `상담진행=완료` patch + dual relation 자동 연결. 예약 없는 재원생 상담은 결과 row만 생성 (relation 빈 채로).

> ⚠️ **Claude 동작 가드 (반드시 준수 — 최우선 정책)**
>
> 1. **학부모 알림톡·카톡·SMS·이메일·기타 어떤 채널로도 학부모 발송 절대 금지**.
> 2. **session_save_webhook 절대 사용 금지**. `parent_webhook` (선생님 내부 검수 채널)만 사용.
> 3. **학부모 디스코드 채널 발송 금지** — parent_webhook은 학원 내부 선생님 채널이므로 OK이지만, 외부 학부모 채널은 절대 X.
> 4. **상담 결과는 학원 내부 정보** — 학부모는 본인이 한 상담이라 따로 통보 불필요. 어떤 형태로도 학부모에게 노출 X.
> 5. AskUserQuestion 도구 사용 금지. 모든 질문은 일반 텍스트.
> 6. 자의적 부연 설명 금지. .md에 정의되지 않은 안내문 노출 금지.
> 7. **학원캘린더 DB 등록 절대 시도 금지** (상담은 1:1, 캘린더는 학원 전체).
> 8. **신규 학생 직접 등록 시도 금지**. 학생 DB에 없는 학생은 /상담예약 먼저 호출 권유.
> 9. **상담내용·상담결과AI 두 필드에 동일 내용 저장** 강제. 하나만 채우거나 다른 내용 저장 금지.
> 10. **AI 처리 검수 단계 강제**. AI 결과를 사용자 확인 없이 노션 patch 절대 금지 (/수업결과의 흐름 문제 회피).
> 11. **음성 파일 자동 첨부 시도 금지**. 노션 API files 필드는 외부 URL만 받음. 로컬 파일은 사용자가 노션에서 직접 첨부.
> 12. **상담예약 row의 신규/재원생 변경 금지**. 결과 row의 신규/재원생은 예약 row와 일치 (예약 없는 상담은 항상 재원생상담).

## 범위

**상담결과 DB row 생성 (공통)**
- 상담라벨 (title), 학생이름 (relation), 신규/재원생 (select)
- 상담일자 (date + time), 상담유형, 상담진행한 선생님 (relation)
- 상담내용 (rich_text) + 상담결과AI (rich_text) — **둘 다 동일 내용** (검수 최종본)
- 등록 여부 (checkbox, 신규상담만)
- 미등록 사유 (select, 신규상담 미등록 시)
- 등록일 (date, 신규상담 등록 시)
- 재접촉 예정일 (date, 신규상담 미등록/미정 시)
- 상담예약 연결 (relation, 예약 있을 때만 / 없을 때 빈 채로)

**상담예약 DB row patch (예약 있을 때만)**
- 상담진행: "예약" → "완료" (이미 "완료"면 skip)

**제외 (별도 처리)**
- 학부모 발송 일체 (학원 내부 정보)
- 학원캘린더 등록
- 음성 파일 노션 자동 첨부 (사용자가 노션에서 직접)
- 신규 학생 등록 — /상담예약 책임
- 등록 후 수강 등록 — /수강등록 책임

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-post-page,mcp__notionApi__API-patch-page
   ```
2. config 읽기: `G:/내 드라이브/ClaudeSetting/notion-academy-config.json`
   - `notion.data_sources.consultations` (상담결과 DB)
   - `notion.data_sources.consultations_booking` (상담예약 DB)
   - `notion.data_sources.students`
   - `notion.data_sources.teachers`
   - `notion.data_sources.classes`
   - `notion.data_sources.class_enrollments`
   - `discord.parent_webhook`
   - `discord.username`
3. 현재 날짜·시간 기록

## 절차

### 1. 학생 검색

```
[1/?] 어떤 학생의 상담 기록인가요? (이름 일부)

> 
```

쿼리: 학생 DB
- `학생이름 contains <입력>` AND `등록현황 IN ("등록", "휴원", "예비")`
- "예비" 포함 이유: /상담예약으로 신규상담 row 만든 학생 (예약된 신규상담 결과 기록)

분기:
- 0건 → 안내 후 종료:
  ```
  ❌ 검색 결과 없음.

  💡 처리 옵션:
    - 신규 학생이면 /상담예약 먼저 호출 (학생 DB 자동 생성 포함)
    - 퇴원·졸업 학생 재상담이면 노션에서 등록현황 먼저 변경 후 재호출
  종료.
  ```
- 1건 → 자동 선택 + 확인
- 2건+ → 학교·학년 표시 후 선택

`selected_student` 저장 (page_id, 학생이름, 학교, 학년, 등록현황).

### 2. 상담예약 row 조회 + 분기

쿼리: 상담예약 DB
- `학생이름 == <selected_student>`
- `상담진행 IN ("예약", "완료")` (취소·노쇼 제외)
- 정렬: `상담 예약일자` 최근 순, 최대 5건

#### 2-A. 0건 (예약 없음)

```
💡 이 학생의 상담예약 기록이 없습니다.
   예약 없이 진행한 상담 기록으로 처리됩니다.
   계속할까요? (네/아니오)
```

- 부정 → 안내 후 종료:
  ```
  💡 /상담예약 먼저 호출하시거나, 그대로 진행하려면 명령어 다시 호출하고 "네" 응답.
  종료.
  ```
- 긍정 → `booking_row = null`, `consultation_type = "재원생상담"` 자동 → 3단계로

#### 2-B. 1건+

```
[?/?] 이 상담의 결과 기록인가요, 새 상담 기록인가요?

[기존 예약 목록 — 최근 5건]
  1. {YYYY-MM-DD HH:MM} ({요일}) / {상담유형} / {신규/재원생} / 진행: {예약/완료}
  2. ...
  
  {N+1}. 새 상담 기록 (예약과 별개)

번호 선택:
```

- 1~N → `booking_row` = 선택된 page_id, 예약 row 메타데이터 활용
  - `consultation_type` = 예약 row의 신규/재원생
  - `appointment_datetime` = 예약 row의 상담 예약일자
  - `consultation_method` = 예약 row의 상담유형
  - `consultation_purpose` = 예약 row의 상담 목적
  - `selected_teacher` = 예약 row의 상담하시는 선생님
  - 4단계로 이동 (메타데이터 입력 skip)
- N+1 → `booking_row = null`, `consultation_type = "재원생상담"` 자동 → 3단계로

### 3. 메타데이터 입력 (예약 없는 상담만, `booking_row = null`)

> ⚠️ 예약 있는 케이스는 이 단계 스킵 (예약 row에서 자동 가져옴).
> ⚠️ 예약 없는 상담은 항상 재원생상담 (신규는 /상담예약 먼저 권유).

#### 3-1. 상담 일시

```
[?/?] 상담 일시를 입력해주세요.

   📌 사후 기록 (과거): "지금", "방금", "1시간 전", "어제 15:00"
   📌 예정 상담 (미래): "내일 오후 2시", "2026-05-15 14:00"

> 
```

Claude 해석 절차:
1. 자연어 → ISO 8601 (`2026-05-13T13:30:00`)
   - "지금"·"방금" → 현재 시각
   - "5분 전"·"1시간 전"·"조금 전" → 현재 - 해당 시간
2. 모호 시 재확인:
   - 시간 미상 ("내일") → "⚠️ 시간이 빠져있어요" 재입력
   - 오전·오후 모호 ("내일 2시") → "오후 2시(14:00)로 처리할까요? (네/아니오/오전)"
3. 너무 오래된 과거 (1개월 이상 전) → "⚠️ 오래된 과거 일시인데 맞나요? (네/아니오)" 확인
4. 너무 먼 미래 (6개월+) → 경고 + 확인

`appointment_datetime` 저장.

#### 3-2. 상담유형

```
[?/?] 상담유형?
  1. 전화상담
  2. 방문상담
  3. 화상상담
번호 선택:
```

`consultation_method` 저장.

#### 3-3. 상담 목적

```
[?/?] 상담 목적?
  1. 모름 — 추천
  2. 진학
  3. 학업
  4. 태도
  5. 수강료
  6. 재원정보
  7. 기타
번호 선택:
```

`consultation_purpose` 저장.

> ⚠️ 상담 목적은 상담결과 DB에 select 필드 있는지 확인 필요. 없으면 상담내용 본문 시작에 "[상담 목적: {목적}]"로 포함.

#### 3-4. 상담진행한 선생님

쿼리: 선생님 DB — `재직상태 == "재직"`

재원생 담당 선생님 자동 추천 절차:
1. 수강 등록 DB 쿼리 — `학생 == <selected_student>` AND `수강종료일 == null`
2. 매칭된 반의 `반` relation → 클래스관리 DB
3. 클래스의 `담당 선생님` relation → 선생님 page_id 추출

```
[?/?] 상담 진행한 선생님?
  1. {담당 선생님} — 담당 선생님 추천
  2. 이승환 (원장)
  3. 김동환
  ...
번호 선택:
```

담당 선생님 추출 실패 → 원장(이승환) 추천 fallback.

`selected_teacher` 저장.

### 4. 입력 방식 분기 (공통)

```
[?/?] 상담 결과를 어떻게 입력할까요?

  1. 메모 텍스트 직접 입력 (요점 정리한 내용)
  2. 녹취록 텍스트 붙여넣기 (이미 STT 변환된 전체 대화)
  3. 녹음 파일 업로드 (음성 첨부 + 변환 텍스트 별도)

번호 선택:
```

#### 4-A. 메모 텍스트 (선택 1)

```
[?/?] 메모를 입력해주세요. (한 메시지로 여러 줄 가능)

> 
```

빈 입력 → "메모가 비어있어요. 다시 입력해주세요." 재입력.

`raw_text` = 입력 메모.

```
[?/?] AI로 학원 내부 기록용 톤으로 다듬을까요? (네/아니오)
```

- 긍정 → 5-A 다듬기 진행
- 부정 → AI 처리 안 함. `final_text = raw_text`, 5단계 검수 skip, 6단계로 이동

#### 4-B. 녹취록 텍스트 (선택 2)

```
[?/?] 녹취록 텍스트를 붙여넣어주세요. (긴 텍스트 가능, 한 메시지로)

> 
```

빈 입력 → 재입력.

`raw_text` = 녹취록 → **AI 요약 자동 진행** (5-B).

#### 4-C. 녹음 파일 (선택 3)

```
[?/?] 녹음 파일 경로를 입력해주세요. (PC 절대 경로, .m4a/.mp3/.wav 등)

> 
```

PowerShell로 파일 존재 검증 (`Test-Path`). 없으면 "파일이 존재하지 않습니다. 경로 확인" 재입력.

```
[?/?] 변환된 텍스트가 있나요? 있으면 붙여넣기, 없으면 "없음"

> 
```

`recording_file_path` 저장.

분기:
- 변환 텍스트 입력 → `raw_text` = 변환 텍스트, 5-B 요약 진행
- "없음" → `raw_text = "녹음만 첨부됨 — 노션에서 음성 확인"`, AI 처리 스킵, 6단계로

### 5. AI 처리 + 검수

#### 5-A. 메모 다듬기 (옵션 1 + 사용자 "네")

Claude(이 명령어 실행 모델, Opus 4.7)가 그 자리에서 처리. 별도 API 호출 없음.

다듬기 가이드:
- 사실 변경 금지
- 학원 운영 관점에서 핵심 위주
- 5~10줄, 자연스러운 문장
- 군더더기 제거
- **학원 내부 기록용** (학부모 전달용 아님)

`ai_text` = Claude 다듬은 결과.

#### 5-B. 녹취록·녹음 요약 (옵션 2 또는 옵션 3 + 변환 텍스트 있음)

Claude가 직접 요약:
- 상담 목적·결정·후속 액션 위주
- 5~10줄
- 학부모 발언 인용 가능 (중요 부분)
- **학원 내부 기록용**

`ai_text` = Claude 요약 결과.

#### 5-C. 검수 단계 (공통, 모든 AI 처리 후)

```
━━━━━━━━━━━━━━━━━━━━━━
🤖 AI {다듬은/요약한} 결과
━━━━━━━━━━━━━━━━━━━━━━

{ai_text}

━━━━━━━━━━━━━━━━━━━━━━

이대로 저장할까요?
  1. 네, 이대로 저장
  2. 다시 다듬기 (Claude가 한 번 더 정리)
  3. 직접 수정 (사용자가 수정본 입력)
  4. AI 처리 취소 (원본만 저장)
번호 선택:
```

처리:
- 1 → `final_text = ai_text` → 6단계로
- 2 → 같은 `raw_text` 다시 Claude 처리 (다른 결과) → 5-C 재진입
  - 5회 연속 다시 다듬기 시도 시 "5회 시도. 원본 또는 직접 수정 권장" 안내
- 3 → 사용자 수정본 입력:
  ```
  [?/?] 수정본을 입력해주세요.

  > 
  ```
  → `final_text = 사용자 수정본`. 빈 입력 → 재입력
- 4 → `final_text = raw_text` (원본 그대로)

### 6. 신규상담 후속 분기 (등록 여부)

> ⚠️ `consultation_type == "신규상담"` AND `booking_row != null` (예약된 신규상담)일 때만 진행.
> 그 외(재원생상담·예약 없는 상담)는 이 단계 스킵.

```
[?/?] 등록 결정됐나요?
  1. 등록 → 등록일 입력
  2. 미등록 → 미등록 사유 + (선택) 재접촉 예정일
  3. 미정 (결정 보류) → 재접촉 예정일 (필수)
번호 선택:
```

#### 6-1. 등록 (선택 1)

```
[?/?] 등록일은 언제로 할까요? (자연어 OK: "오늘", "내일", "2026-05-15")

> 
```

Claude 자연어 해석 → ISO 8601 date.

`enrollment_status = true`, `enrollment_date` 저장.

#### 6-2. 미등록 (선택 2)

```
[?/?] 미등록 사유?
  1. 가격
  2. 거리
  3. 일정
  4. 기대불일치
  5. 경쟁학원
  6. 기타
번호 선택:

[?/?] 재접촉 예정일? (자연어 OK: "1개월 후", "여름방학 후", "없음")

> 
```

- "없음" → `recontact_date = null`
- 그 외 → Claude 자연어 해석 → date

`enrollment_status = false`, `non_enrollment_reason`, `recontact_date` 저장.

#### 6-3. 미정 (선택 3)

```
[?/?] 재접촉 예정일? (자연어 OK: "1주 후", "다음주 금요일", "고민해보고 연락하기로 한 날")

> 
```

빈 입력·"없음" 불가 (미정은 재접촉 필수).

`enrollment_status = false`, `non_enrollment_reason = null`, `recontact_date` 저장 (필수).

### 7. 미리보기 (분기별)

#### 7-A. 예약된 신규상담

```
━━━━━━━━━━━━━━━━━━━━━━
📋 신규상담 결과 기록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:        {학생이름}
- 학교/학년:   {학교} {학년}
- 등록현황:    {기존 상태}

[상담 정보 — 예약 row에서 가져옴]
- 일시:        {YYYY-MM-DD HH:MM} ({요일})
- 상담유형:    {유형}
- 상담 목적:   {목적}
- 상담 선생님: {선생님}

[상담 결과]
{final_text}

[등록 결정]
{등록 케이스} ✅ 등록 — 등록일: {YYYY-MM-DD}
{미등록 케이스} ❌ 미등록 — 사유: {사유}, 재접촉: {YYYY-MM-DD 또는 "없음"}
{미정 케이스} ⏳ 미정 — 재접촉: {YYYY-MM-DD}

[자동 처리]
- 상담결과 DB row 생성 (상담내용·상담결과AI 둘 다 동일)
- 상담예약 DB row 상담진행: 예약 → 완료 patch
- 상담예약 ↔ 상담결과 dual relation 자동 연결
{녹음 파일 있는 경우}
- 음성 파일 노션 첨부 안내 (수동)
- 디스코드 parent_webhook으로 선생님 검수 발송

[제외]
- 학부모 발송 일체 X (알림톡·카톡·SMS·이메일 모두)

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

#### 7-B. 예약된 재원생상담

```
━━━━━━━━━━━━━━━━━━━━━━
📋 재원생상담 결과 기록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:        {학생이름}
- 학교/학년:   {학교} {학년}
- 등록현황:    {등록/휴원}

[상담 정보 — 예약 row에서 가져옴]
- 일시:        {YYYY-MM-DD HH:MM} ({요일})
- 상담유형:    {유형}
- 상담 목적:   {목적}
- 상담 선생님: {선생님}

[상담 결과]
{final_text}

[자동 처리]
- 상담결과 DB row 생성 (둘 다 동일 저장)
- 상담예약 DB row 상담진행: 예약 → 완료
- 상담예약 ↔ 상담결과 dual relation 연결
{녹음 파일 있는 경우}
- 음성 파일 노션 첨부 안내
- 디스코드 발송

[제외]
- 학부모 발송 일체 X

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

#### 7-C. 예약 없는 재원생상담

```
━━━━━━━━━━━━━━━━━━━━━━
📋 재원생상담 결과 기록 미리보기 (예약 없음)
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:        {학생이름}
- 학교/학년:   {학교} {학년}
- 등록현황:    {등록/휴원}

[상담 정보 — 직접 입력]
- 일시:        {YYYY-MM-DD HH:MM} ({요일})
- 상담유형:    {유형}
- 상담 목적:   {목적}
- 상담 선생님: {선생님}

[상담 결과]
{final_text}

[자동 처리]
- 상담결과 DB row 생성 (예약 relation 빈 채로)
{녹음 파일 있는 경우}
- 음성 파일 노션 첨부 안내
- 디스코드 발송

[제외]
- 상담예약 DB 변경 없음 (예약 row 없으므로)
- 학부모 발송 일체 X

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

부정 → 즉시 종료, 노션 변경 0.

### 8. 노션 처리

#### 8-1. 상담결과 DB row 생성 (`API-post-page`)

```json
{
  "parent": {"type": "database_id", "database_id": "<상담결과 DB id>"},
  "properties": {
    "상담라벨": {"title": [{"text": {"content": "{학생이름} - {상담유형} ({YYYY-MM-DD HH:MM})"}}]},
    "학생이름": {"relation": [{"id": "<selected_student>"}]},
    "신규/재원생": {"select": {"name": "<consultation_type>"}},
    "상담일자": {"date": {"start": "<ISO 8601 datetime>"}},
    "상담유형": {"select": {"name": "<consultation_method>"}},
    "상담진행한 선생님": {"relation": [{"id": "<selected_teacher>"}]},
    "상담내용": {"rich_text": [{"text": {"content": "<final_text>"}}]},
    "상담결과AI": {"rich_text": [{"text": {"content": "<final_text>"}}]},
    "상담예약 연결": {"relation": [{"id": "<booking_row>"}]} // booking_row null이면 빈 배열
  }
}
```

신규상담 케이스 추가 properties:
- `등록 여부`: checkbox (true/false)
- `미등록 사유`: select (미등록 케이스만)
- `등록일` 또는 `재접촉 예정일`: date

→ `result_page_id` 추출.

#### 8-2. 음성 파일 첨부 안내 (옵션 3만)

> ⚠️ 노션 API files 필드는 외부 URL만 받음. 로컬 파일 자동 업로드 불가.

```
🎙️ 음성 파일 첨부 안내

상담결과 row 생성됨: https://notion.so/<result_page_id>

📌 음성 파일은 노션에서 직접 첨부해주세요:
1. 위 링크 열기
2. "상담 녹음파일" 필드 클릭
3. 파일 업로드: {recording_file_path}
```

#### 8-3. 상담예약 row patch (예약 있을 때만, `booking_row != null`)

```json
{
  "page_id": "<booking_row>",
  "properties": {
    "상담진행": {"select": {"name": "완료"}}
  }
}
```

이미 "완료"면 patch skip.

#### 8-4. 트랜잭션 일관성

- Step 1 실패 → 종료, 후속 진행 안 함
- Step 3 실패 → 상담결과 row 그대로 유지 (롤백 안 함, 결과 row는 의미 있음). 결과 보고에 patch 실패 명시

### 9. 디스코드 검수 발송

`config.discord.parent_webhook` (선생님 내부 검수 채널) 사용.

> ⚠️ **이 webhook은 선생님 내부 채널**. 학부모 발송 절대 시도 X.

#### 9-A. 예약된 신규상담

```
📝 상담 결과 기록 (선생님 공유)

[상담 유형] 🆕 신규상담 (예약 → 완료)

[학생]
- 이름:        {학생이름}
- 학교/학년:   {학교} {학년}
- 등록현황:    {기존 상태}

[상담 정보]
- 일시:        {YYYY년 M월 D일 (요일) HH:MM}
- 상담유형:    {유형}
- 상담 목적:   {목적}
- 상담 선생님: {선생님}

[상담 결과]
{final_text}

[등록 결과]
{등록} ✅ 등록 결정
- 등록일:      {YYYY-MM-DD}
- 후속:        /수강등록 호출 필요

{미등록} ❌ 미등록
- 미등록 사유: {사유}
- 재접촉 예정: {YYYY-MM-DD 또는 "없음"}

{미정} ⏳ 미정 (결정 보류)
- 재접촉 예정: {YYYY-MM-DD}

[자동 처리 결과]
✅ 상담결과 DB row 생성 (상담내용·상담결과AI 동일)
✅ 상담예약 DB 상담진행: 예약 → 완료
✅ 상담예약 ↔ 상담결과 dual relation 연결
{녹음 파일 있는 경우}
🎙️ 음성 파일은 노션에서 직접 첨부

[다음 작업]
- (등록인 경우) /수강등록 호출
- (미정·미등록인 경우) 재접촉 예정일에 follow-up
```

#### 9-B. 예약된 재원생상담

```
📝 상담 결과 기록 (선생님 공유)

[상담 유형] 🎓 재원생상담 (예약 → 완료)

[학생]
- 이름:        {학생이름}
- 학교/학년:   {학교} {학년}
- 등록현황:    {등록/휴원}

[상담 정보]
- 일시:        {YYYY년 M월 D일 (요일) HH:MM}
- 상담유형:    {유형}
- 상담 목적:   {목적}
- 상담 선생님: {선생님}

[상담 결과]
{final_text}

[자동 처리 결과]
✅ 상담결과 DB row 생성
✅ 상담예약 DB 상담진행: 예약 → 완료
✅ relation 연결
{녹음 파일 있는 경우}
🎙️ 음성 파일은 노션에서 직접 첨부
```

#### 9-C. 예약 없는 재원생상담

```
📝 상담 결과 기록 (선생님 공유) — 예약 없는 상담

[상담 유형] 🎓 재원생상담 (예약 없음 — 직접 기록)

[학생]
- 이름:        {학생이름}
- 학교/학년:   {학교} {학년}
- 등록현황:    {등록/휴원}

[상담 정보]
- 일시:        {YYYY년 M월 D일 (요일) HH:MM}
- 상담유형:    {유형}
- 상담 목적:   {목적}
- 상담 선생님: {선생님}

[상담 결과]
{final_text}

[자동 처리 결과]
✅ 상담결과 DB row 생성 (예약 row 없음, relation 빈 채로)
{녹음 파일 있는 경우}
🎙️ 음성 파일은 노션에서 직접 첨부

[참고]
- 사전 예약 없이 진행된 상담 → 상담예약 DB row 생성 안 함
- 이력 조회 시 상담결과 DB 기준
```

#### 9-D. 발송 (PowerShell)

```powershell
$webhook = $config.discord.parent_webhook
$username = $config.discord.username
$content = $body
$payload = @{ content = $content; username = $username }
$json = $payload | ConvertTo-Json -Compress -Depth 5
$bytes = [System.Text.Encoding]::UTF8.GetBytes($json)
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.SecurityProtocolType]::Tls12

try {
    Invoke-RestMethod -Uri $webhook -Method Post -Body $bytes -ContentType 'application/json; charset=utf-8' -TimeoutSec 30 -ErrorAction Stop
} catch {
    # 노션 변경 유지. 실패만 보고
}
```

#### 9-E. 발송 실패 처리

- 노션 변경 유지
- 결과 보고에 디스코드 실패만 명시

### 10. 결과 보고 (분기별)

#### 10-A. 예약된 신규상담

```
✅ 신규상담 결과 기록 완료

[학생]
- 이름:    {학생이름}

[상담 결과]
- 노션:        https://notion.so/<result_page_id>
- 상담예약 row: https://notion.so/<booking_row> (상담진행: 예약 → 완료)

[등록 결정]
{등록} ✅ 등록 — 등록일: {YYYY-MM-DD}
{미등록} ❌ 미등록 — 사유: {사유}, 재접촉: {YYYY-MM-DD 또는 "없음"}
{미정} ⏳ 미정 — 재접촉: {YYYY-MM-DD}

[디스코드 발송]
✅ 선생님 채널 발송 완료
{실패}
❌ 디스코드 발송 실패: <에러>

{녹음 파일 있는 경우}
🎙️ 음성 파일 첨부:
- 위 노션 링크에서 "상담 녹음파일" 필드에 직접 첨부
- 파일 경로: {recording_file_path}

[다음 작업 추천]
- (등록) /수강등록 호출
- (미정·미등록) 재접촉 예정일에 follow-up
```

#### 10-B. 예약된 재원생상담

```
✅ 재원생상담 결과 기록 완료

[학생]
- 이름:    {학생이름}

[상담]
- 노션:    https://notion.so/<result_page_id>
- 상담예약: https://notion.so/<booking_row> (완료 patch)

[디스코드 발송]
✅ 선생님 채널 발송 완료

{녹음 파일 있는 경우}
🎙️ 음성 파일은 노션에서 직접 첨부 ({recording_file_path})
```

#### 10-C. 예약 없는 재원생상담

```
✅ 재원생상담 결과 기록 완료 (예약 없음)

[학생]
- 이름:    {학생이름}

[상담]
- 노션:    https://notion.so/<result_page_id>
- (상담예약 row 없음, relation 빈 채로)

[디스코드 발송]
✅ 선생님 채널 발송 완료

{녹음 파일 있는 경우}
🎙️ 음성 파일은 노션에서 직접 첨부 ({recording_file_path})
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 학생 검색 0건 | "/상담예약 먼저 또는 등록현황 변경" 안내 후 종료 |
| 예약 없는 신규 학생 (학생 DB에 없음) | "/상담예약 먼저 호출" 안내 후 종료 |
| 예약 row 0건 | 사용자 확인 후 예약 없는 상담으로 진입 |
| 예약 row 다수 | 최근 5건 + "새 기록" 옵션 |
| 입력 방식 1·2·3 외 입력 | 재입력 |
| 메모·녹취록 빈 입력 | "비어있어요" 재입력 |
| 녹음 파일 경로 없음 | `Test-Path` 검증, 없으면 재입력 |
| 녹음 + 변환 텍스트 "없음" | AI 스킵, 음성만 첨부 안내 |
| 검수 1~4 외 입력 | 재입력 |
| 검수 "2 다시 다듬기" 5회 시도 | "5회 시도. 원본 또는 직접 수정 권장" 안내 |
| 검수 "3 직접 수정" 빈 입력 | 재입력 |
| 등록 여부 1·2·3 외 입력 | 재입력 |
| 미등록 사유 1~6 외 입력 | 재입력 |
| 자연어 일시·날짜 모호 | Claude 재확인 ("오후 X시로?", "여름방학 후는 8월말로?") |
| 상담일자 너무 오래된 과거 (1개월+) | 경고 + 확인 |
| 상담일자 너무 먼 미래 (6개월+) | 경고 + 확인 |
| 상담결과 row 생성 실패 | 종료, 후속 진행 안 함 |
| 상담예약 patch 실패 | 상담결과 유지, patch 실패만 안내 |
| 디스코드 발송 실패 | 노션 유지, 발송 실패만 안내 |
| 미리보기 부정 | 즉시 종료, 노션 변경 0 |
| 어느 단계든 "취소"·"q" | 즉시 종료 |

## 주의사항

- **확인 없는 자동 쓰기 금지**: 미리보기 후 명시적 긍정 응답 없이 노션 POST 금지
- **AI 검수 단계 강제**: AI 결과를 사용자 확인 없이 노션 patch 절대 금지 (/수업결과의 흐름 문제 회피)
- **두 필드 동일 저장**: 상담내용·상담결과AI 둘 다 검수 최종본으로 동일 저장
- **음성 파일 수동 첨부**: 노션 API 제약. 결과 보고·미리보기·디스코드에 첨부 경로 안내
- **재원생 검색 — 등록현황 필터**: "등록"·"휴원"·"예비"만. 퇴원·졸업 제외
- **예약 row 신규/재원생 변경 금지**: 결과 row의 신규/재원생은 예약 row와 일치 또는 예약 없는 상담은 "재원생상담"
- **학원캘린더 등록 X**
- **session_save_webhook X**: parent_webhook (선생님 내부 채널)만
- **학부모 발송 일체 금지**: 알림톡·카톡·SMS·이메일 모두 어떤 채널로도 X
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (메모리 `feedback_yes_no_korean`)
- **터미널 default**: 엔터 사용 안 함. 번호 선택 또는 키워드만 (메모리 `feedback_terminal_default_input`)
- **자연어 일시·날짜**: Claude가 해석, 모호 시 재확인

## 예시 1: 예약된 신규상담 — 등록 결정 (메모 + AI 다듬기)

**입력**
- 학생: "박철수" → 1건 매칭 (목동초 5학년, 예비)
- 상담예약 row 1건 → 선택 (어제 14:00, 방문상담, 신규상담, 진행: 예약)
- 입력 방식: 1 (메모)
- 메모: "코딩 처음이라 입문반 추천. 학부모도 만족하셨고 다음주부터 시작하기로 함."
- AI 다듬기: 네
- 검수: 1 (이대로 저장)
- 등록 결정: 1 (등록)
- 등록일: "다음주 월요일" → "2026-05-20"

**노션 결과**
- 상담결과 DB row 생성:
  - 상담내용·상담결과AI 동일 (AI 다듬은 결과)
  - 등록 여부: true, 등록일: 2026-05-20
  - 상담예약 dual relation 연결
- 상담예약 DB row 상담진행: 예약 → 완료 patch
- 디스코드 발송: 📝 상담 결과 기록 (신규상담 ✅ 등록)

## 예시 2: 예약된 재원생상담 (녹취록 + AI 요약)

**입력**
- 학생: "추득만" → 1건 매칭 (목동초 5학년, 등록)
- 상담예약 row 1건 → 선택 (오늘 16:00, 전화상담, 재원생상담, 진행: 예약)
- 입력 방식: 2 (녹취록)
- 녹취록: (긴 raw 텍스트, 학부모-원장 대화 화자 구분 있음)
- AI 요약 자동 진행
- 검수: 3 (직접 수정) → 수정본 입력 ("진학 문의 — 중1 진학 후 코딩 지속 의사 강함. 정보올림피아드 도전 권유.")

**노션 결과**
- 상담결과 DB row 생성:
  - 상담내용·상담결과AI 둘 다 "진학 문의 — ..." (사용자 수정본)
- 상담예약 DB 상담진행: 완료 patch
- 디스코드 발송

## 예시 3: 예약 없는 재원생상담 (갑자기 학부모 방문 + 메모 없이 AI 처리)

**입력**
- 학생: "김민준" → 1건 매칭 (목동초 4학년, 등록)
- 상담예약 row 0건 → "예약 없는 상담으로 진행" 확인 → 네
- 메타데이터: 일시 "지금", 방문상담, 모름, 담당 선생님 추천
- 입력 방식: 1 (메모)
- 메모: "지나가다 들렀다고 함. 동생도 곧 입학 예정. 별 이슈 없음."
- AI 다듬기: 아니오 (간단해서 그대로)
- 미리보기 확인 → 진행

**노션 결과**
- 상담결과 DB row 생성 (예약 relation 빈 채로)
- 상담내용·상담결과AI 둘 다 메모 raw 그대로
- 디스코드 발송: 📝 상담 결과 기록 — 예약 없는 상담

## 예시 4: 녹음 파일 + 변환 텍스트 없음

**입력**
- 학생: "이수민" / 예약 없는 상담
- 메타데이터: 1시간 전, 방문상담, 학업, 담당 선생님
- 입력 방식: 3 (녹음 파일)
- 파일 경로: "C:/Users/이승환/recordings/2026-05-13-이수민.m4a"
- 변환 텍스트: "없음"

**노션 결과**
- 상담결과 DB row 생성
- 상담내용·상담결과AI 둘 다 "녹음만 첨부됨 — 노션에서 음성 확인"
- 음성 파일 첨부 안내 (결과 보고에 노션 링크 + 파일 경로)
- 디스코드 발송 (🎙️ 음성 파일 안내 포함)
