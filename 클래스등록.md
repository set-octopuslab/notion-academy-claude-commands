---
description: 반(클래스) 신규 생성. 클래스관리 DB row 1건 생성 (대화식, 미리보기·확인 후 생성)
---

# 클래스등록

원장이 `/클래스등록` 을 입력하면, **클래스타입(정규반/특강반) → 클래스이름 → 선생님** 순서로 1개씩 질문하고, **미리보기 → 확인(네/아니오)** 후 노션 클래스관리 DB에 row를 1건 생성한다.

학생·시간표·수강료 등은 이 명령어에서 다루지 않음 (별도 명령어 `/수강등록`, `/정규시간표등록`).

## 범위

**포함**
- 클래스이름 (title) 신규 입력 — 자유 입력 + 추천 형식 안내
- 클래스타입 (select) 선택 — 정규반 / 특강반
- 선생님 1명 지정 (single_property relation, 재직 중인 선생님만)
- 같은 이름 클래스 중복 차단 + 행동 가이드 안내

**제외 (별도 명령어)**
- 학생 등록 — `/수강등록`
- 정규/보강 시간표 row 추가 — `/정규시간표등록`, `/보강시간표등록`
- 수강료 결제내역, 학원 캘린더 일정 등 — 자동으로 dual_property 채워짐
- 선생님 신규 등록 — 노션에서 직접 추가

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-post-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.classes` ID 확인
   - `notion.data_sources.teachers` ID 확인 (없으면 즉시 중단 + `/노션-초기설정` 안내)

## 절차

### 1. 클래스타입 선택

```
[1/4] 어떤 클래스인가요?
  1. 정규반 (매주 반복되는 일반 수업)
  2. 특강반 (기간한정·주제특화·특수반)
번호 선택:
```

분기:
- "1" → `class_type = "정규반"`
- "2" → `class_type = "특강반"`
- 그 외 → "1 또는 2를 입력해주세요" 재요청
- "취소"·"q"·부정 답변 → 즉시 종료

### 2. 클래스이름 입력

#### 2-1. 정규반 선택 시 안내문

```
[2/4] 클래스 이름을 입력해주세요.

추천 형식: {과목} {대상} {반식별}
  예) 파이썬 초등 A
  예) C언어 중등 B
  예) 자바 고등 1

자유 입력 가능합니다. 추천 형식대로 입력하시면 정렬·검색이 편합니다.

> 
```

#### 2-2. 특강반 선택 시 안내문

```
[2/4] 클래스 이름을 입력해주세요.

추천 형식: {주제/기간} {특강반/특강}
  예) 정보올림피아드 특강반
  예) 주말 부트캠프
  예) 12월 동계특강
  예) 코딩테스트 대비반

자유 입력 가능합니다.

> 
```

#### 2-3. 빈 문자열·공백만 입력 시

"이름을 입력해주세요" 재요청.

### 3. 같은 이름 클래스 중복 검사

쿼리: 클래스관리 DB
- `클래스이름 == <입력한 이름>` (정확히 일치)

#### 3-1. 0건 → 다음 단계로 (4. 선생님 선택)

#### 3-2. 1건+ → 차단 + 행동 가이드 안내 후 종료

기존 클래스 정보를 함께 보여줌 (이름·타입·선생님·노션 링크). 사용자가 처한 상황(우연한 이름 겹침/운영 중 클래스 정리/종강된 클래스 정리)에 따라 적절한 해결책이 다르므로 3가지 방법 모두 안내.

```
━━━━━━━━━━━━━━━━━━━━━━
❌ '{클래스이름}' 이름의 클래스가 이미 있습니다.
━━━━━━━━━━━━━━━━━━━━━━

📍 기존 클래스 정보
   - 이름:        {클래스이름}
   - 타입:        {정규반/특강반}
   - 담당 선생님: {선생님이름}
   - 노션 링크:   https://notion.so/<page-id>

💡 같은 이름의 클래스는 중복으로 만들 수 없습니다.
   아래 3가지 중 하나를 선택하시고 다시 시도해주세요.

  1. 다른 이름으로 신규 등록 (가장 간단)
     → 명령어 다시 호출하고 다른 이름 입력
     → 예: 'A' 대신 'B' 또는 학년·기수 추가
       ('파이썬 초등 A' → '파이썬 초등 B' 또는 '파이썬 초등 5학년')

  2. 기존 클래스 이름을 변경 (기존 클래스가 진행 중이라면)
     → 위 노션 링크 클릭 → 기존 클래스의 '클래스이름' 필드 수정
     → 그 후 명령어 /클래스등록 다시 호출

  3. 기존 클래스를 삭제 (종강·취소된 클래스라면)
     → 위 노션 링크 클릭 → 기존 클래스 페이지 우상단 메뉴 → 'Delete'
     → 그 후 명령어 /클래스등록 다시 호출

━━━━━━━━━━━━━━━━━━━━━━
종료 (노션 변경 없음).
```

### 4. 선생님 선택

#### 4-1. 재직 중 선생님 목록 조회

쿼리: 선생님 DB
- `재직상태 == "재직"`

#### 4-2. 분기

| 결과 | 처리 |
|---|---|
| 0건 | 종료 (4-3 메시지) |
| 1건 | 자동 선택 + 확인 (4-4) |
| 2건+ | 목록 번호 매겨 선택 (4-5) |

#### 4-3. 재직 선생님 0명

```
❌ 재직 중인 선생님이 없습니다.
   클래스에 담당 선생님을 지정하려면 먼저 선생님이 등록되어야 합니다.

💡 노션에서 선생님 DB로 이동해 신규 선생님을 등록하시거나,
   기존 선생님의 재직상태를 '재직'으로 변경해주세요.
   그 후 /클래스등록 다시 호출.

종료 (노션 변경 없음).
```

#### 4-4. 1건 (자동 선택)

```
[3/4] 담당 선생님:
  ✅ {선생님이름} (수업과목: {과목1, 과목2, ...})

이 선생님이 맞나요? (네/아니오)
```

긍정 → 5단계. 부정 → 종료.

#### 4-5. 2건+ (목록 선택)

```
[3/4] 어느 선생님이 담당하나요?
  1. 김선생 (수업과목: 파이썬, 코딩)
  2. 이선생 (수업과목: C언어, 자바)
  3. 박선생 (수업과목: 스크래치, 엔트리)
번호 선택:
```

수업과목 multi_select가 비어 있으면 "수업과목: (미입력)" 표시.

#### 4-6. 동명이인 처리

같은 이름 선생님 2명 이상이면 전화번호 뒷자리·입사일을 함께 표시:
```
  1. 김선생 (010-****-1234, 입사: 2024-03)
  2. 김선생 (010-****-5678, 입사: 2025-09)
```

전화번호 마스킹: 끝 4자리만 노출, 나머지는 `*`로.

### 5. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 클래스 신규 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[클래스]
- 이름:        {클래스이름}
- 타입:        {정규반/특강반}
- 담당 선생님: {선생님이름}

[처리]
- 동작:        클래스관리 DB에 row 1건 신규 생성
- 후속 작업:   학생 등록은 /수강등록, 시간표는 /정규시간표등록

━━━━━━━━━━━━━━━━━━━━━━
[4/4] 진행할까요? (네/아니오)
```

부정 또는 "취소" → 즉시 종료, 노션 변경 0.

### 6. 노션 row 생성

#### 6-1. 클래스 row 생성 (`API-post-page`)

```json
{
  "parent": {"type": "database_id", "database_id": "<classes database_id>"},
  "properties": {
    "클래스이름": {"title": [{"text": {"content": "<클래스이름>"}}]},
    "클래스타입": {"select": {"name": "<정규반/특강반>"}},
    "선생님": {"relation": [{"id": "<선생님 page_id>"}]}
  }
}
```

응답에서 `id` (클래스 page_id) 추출 → 결과 보고 URL에 사용.

### 7. 결과 보고

```
✅ 클래스 등록 완료

[클래스]
- 이름:        {클래스이름}
- 타입:        {정규반/특강반}
- 담당 선생님: {선생님이름}
- 노션 URL:    https://notion.so/<page-id>

[다음 작업 추천]
1. /수강등록           → 이 반에 학생 등록 + 수강료 설정
2. /정규시간표등록     → 학생 요일·시간 추가 (정규반)
3. /보강시간표등록     → 보강수업 일정 추가 (보강 발생 시)
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 클래스타입 1/2 외 입력 | 재입력 요청 |
| 클래스이름 빈 문자열 또는 공백만 | "이름을 입력해주세요" 재요청 |
| 클래스이름 매우 김 (50자+) | 그대로 진행 (제약 없음) |
| 같은 이름 클래스 이미 있음 | 차단 + 3가지 행동 가이드 안내 후 종료 |
| 재직 중 선생님 0명 | 안내 후 종료 |
| 동명이인 선생님 | 전화번호 마스킹 + 입사일 표시로 구분 |
| 미리보기에서 부정 답변 | 즉시 종료, 노션 변경 0 |
| 어느 단계든 "취소"·"q" 입력 | 즉시 종료, 노션 변경 0 |
| 답변에 "네/아니오" 외 자유 답변 | 의미로 해석 (메모리 `feedback_yes_no_korean`) |

## 주의사항

- **확인 없는 자동 쓰기 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 노션에 절대 POST 금지
- **클래스타입 select 옵션**: 노션 클래스관리 DB의 `클래스타입` select에 "정규반"·"특강반" 옵션이 사전 등록되어 있어야 함 (config v4 기준 사전 추가됨)
- **선생님 single_property**: 클래스관리 DB의 `선생님` relation은 single_property → 1명만 link 가능. 복수 선생님 운영 시 노션 DB 스키마 변경 필요
- **dual_property 자동 sync**: `시간표`·`보강수업 시간표`·`수강 등록`·`수강료 결제내역`·`학원 캘린더 일정` 등은 후속 명령어가 link하면 자동 채워짐. 이 명령어에서 신경 쓸 필요 없음
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (예: "응", "ㅇㅇ", "yes", "ok" 모두 긍정)

## 예시

**입력**
- 클래스타입: 1 (정규반)
- 클래스이름: 파이썬 초등 A
- 선생님: 김선생 (재직 중 1명만 있어 자동 선택)

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📋 클래스 신규 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[클래스]
- 이름:        파이썬 초등 A
- 타입:        정규반
- 담당 선생님: 김선생

[처리]
- 동작:        클래스관리 DB에 row 1건 신규 생성
- 후속 작업:   학생 등록은 /수강등록, 시간표는 /정규시간표등록

━━━━━━━━━━━━━━━━━━━━━━
[4/4] 진행할까요? (네/아니오)
```

**노션 결과**
- 클래스관리 DB row 1건 추가
- 클래스이름="파이썬 초등 A", 클래스타입="정규반", 선생님=김선생 link
