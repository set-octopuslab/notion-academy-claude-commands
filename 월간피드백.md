---
description: 학생-반 단위로 월간피드백 노션 페이지 본문(callout 4개)을 자동 생성. 작성월 입력 → 학생·반 선택 → 일일피드백 자동 수집 → AI 종합 의견 → 사용자 검수 → 페이지 본문 적용 (Phase 1, 학부모 발송은 Phase 2 별도)
---

# 월간피드백 (Phase 1: 페이지 본문 자동 완성)

원장 또는 선생님이 한 학생의 한 달치 일일피드백을 종합해서 **노션 월간피드백 페이지의 본문 (callout 4개)을 자동 생성**한다. AI 종합 의견 + 사용자 검수 단계 포함. 학부모 발송은 Phase 2에서 별도 진행.

## Phase 분리

- **Phase 1 (이 명령어)**: 페이지 본문 자동 완성까지. 노션 페이지는 비공개 유지. 사용자가 노션에서 결과 확인
- **Phase 2 (나중)**: 학원 도메인 + HTML 호스팅 + UUID URL + 문자/카카오 알림톡 자동 발송

## 범위

이 명령어는 **학생 1명 + 반 1개 = 1 페이지** 생성. 학생이 여러 반 수강 중이면 명령어를 반별로 다시 실행.

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-post-page,mcp__notionApi__API-patch-page,mcp__notionApi__API-patch-block-children,mcp__notionApi__API-delete-a-block,mcp__notionApi__API-retrieve-a-page-property
   ```
2. config 읽기: `G:/내 드라이브/ClaudeSetting/notion-academy-config.json`
   - `notion.api_token` 확인 (file_upload API 호출에 필수, 없으면 사진 자동 첨부 불가)
   - `notion.data_sources.weekly_monthly_feedback` 확인 (없으면 즉시 중단 + `/노션-초기설정` 안내)
   - `notion.data_sources.lesson_results` 확인 (일일피드백 DB)
   - `notion.data_sources.lesson_plans` 확인 (수업계획 DB — fallback 검색용)
   - `notion.data_sources.students` 확인
   - `notion.data_sources.classes` 확인
3. PowerShell + `curl.exe` 가용 확인 (Windows 10 기본 포함, file_upload multipart 전송용)

## 입력 항목 (대화식, 1개씩)

> Step 순서: 반 → 학생 → 월
> 반을 먼저 받으면 학생 후보가 자동 좁혀지고, 학생이 여러 반인 경우도 자연스럽게 해결됨

### 1. 반 선택 (자동 목록 + 사용자 선택)

명령어 시작 즉시 클래스관리 DB 전체 목록 표시 (사용자가 이름 외울 필요 없음):

```
filter_properties:  필요 필드만 (클래스이름·선생님·수강인원·수강중인 학생 rollup)
page_size: 50
```

표시 형식:
```
운영 중인 반 목록:
1. 코딩 초등반 (담당: 문성준, 수강 N명)
2. 코딩 중등반 (담당: 김동환, 수강 N명)
3. 마인크래프트 특강 (담당: 이승환, 수강 N명)
...

월간피드백 작성할 반을 선택해주세요. (번호 또는 이름)
```

사용자 입력 분기:
- 번호 (예: `1`) → 해당 반 선택
- 이름 (예: `코딩 초등반`) → 정확히 매칭 → 선택
- 부분 이름 (예: `초등`) → 후보 좁혀 표시 → 재선택
- 매칭 0건 → 재입력 요청

### 2. 학생 이름 입력 (필수, 자유 입력)

학생 목록 자동 표시 안 함 (응답 크기·시간 절약).

```
월간피드백 작성할 학생 이름을 입력해주세요.
(모르겠으면 "목록"이라고 입력)
```

사용자 입력 분기:
- 일반 이름 입력 (예: "김민석") → 검증은 [절차 1] 일일피드백 검색 단계에서 자동
- "목록" 또는 비슷한 키워드 → 그제야 클래스의 `수강중인 학생` rollup으로 학생 목록 표시 → 사용자 재선택

### 3. 작성할 연도+월 (필수)

자유 입력. Claude가 파싱:
- "2026년 4월" / "2026-04" / "4월" (현재년도 가정) / "지난달" / "이번달"
- 모호하면 ISO 형식(YYYY-MM)으로 재확인
- 미래 월 또는 너무 오래된 월(2년 이상 전) 입력 시 경고

## 절차

### 1. 입력 처리 후 자동 검색 (2단계 — 수업계획 경유 fallback)

> **중요 발견 (2026-05-19)**: 일일피드백 row의 `학생 직접` relation이 비어있는 경우가 많음 (학생 정보는 `수업계획 선택` relation 통해 rollup으로 채워짐). 그래서 일일피드백 DB를 `학생 직접`으로 직접 검색하면 0건 나옴. **반드시 수업계획 DB를 먼저 거쳐야 함**.

#### 1-1. 학생 이름 → 학생 page_id 조회
학생 DB query (filter_properties로 응답 작게):
```
filter: 학생이름(title) contains "<입력값>" AND 등록현황 == "등록"
filter_properties: ["title", "Q%7Dxm", "T%5Bre", "Knlg", "%7Bz%3F%7B"]
```
- 0건: 재입력 요청
- 1건: page_id 추출
- 2건 이상: 학교·학년으로 좁혀 사용자 선택

#### 1-2. 수업계획 DB에서 그 월 + 그 학생 row 검색
```
filter:
  학생 (relation) contains <학생 page_id>
filter_properties: ["title"]
page_size: 30
```

결과를 클라이언트에서 추가 필터링:
- 수업명(title)에 대상월(예: "4월")이 포함되거나
- 또는 수업계획 row의 클래스를 비교해서 매칭

수업계획 row들의 page_id 추출 → 다음 단계에서 일일피드백 검색에 사용.

#### 1-3. 일일피드백 DB에서 수업계획 경유로 검색
```
filter (or 조합):
  수업계획 선택 contains <수업계획 page_id 1> OR
  수업계획 선택 contains <수업계획 page_id 2> OR ...
filter_properties: ["title", "Dz%60l", "IBJZ", "eBni", "fxJf", "y%5CK%5E", "cSu%60", "%7CWT%3E", "%7Db~_", "u%7D%60%40", "yR%40S", "XCHf", "%5C%3FS%5B", "pMHe"]
sorts: 수업일자 ascending
```

결과 필터링:
- 각 row의 **클래스 rollup**이 사용자가 선택한 반 page_id와 일치하는 것만 (학생이 여러 반 수강 시 다른 반 row 제외)
- 결과 N건이 그 학생의 그 반 그 월 일일피드백 row들

결과 분기:
- **0건**: "그 학생의 그 월 그 반 수업 데이터가 없습니다" 안내 → 재입력 또는 종료
- **N건**: 다음 단계로 (월간피드백 row 생성)

### 2. 기존 월간피드백 row 검사

주간 or 월간피드백 DB 쿼리:
```
필터:
  학생 = <학생 page_id> AND
  클래스 = <반 page_id> AND
  주간/월간 = "월간" AND
  피드백 월 = "<대상월>월"
```

- **0건**: 신규 생성 흐름으로 진입
- **1건 이상**: 사용자에게 "이미 OOO월 OO반 월간피드백이 있습니다. 어떻게 할까요? (덮어쓰기 / 새로 생성 / 취소)"
  - 덮어쓰기: 기존 row 사용 + 페이지 본문 전체 갱신
  - 새로 생성: 새 row 추가
  - 취소: 중단

### 3. 월간피드백 row 생성 (신규)

`API-post-page`:

parent: `{"type": "data_source_id", "data_source_id": "<weekly_monthly_feedback>"}`

properties:
- 피드백라벨 (title): `"<대상월>월 <반 이름> <학생 이름> 월간 피드백"` (예: "4월 코딩 초등반 김민석 월간 피드백")
- 주간/월간 (select): `"월간"`
- 피드백 월 (select): `"<대상월>월"` (예: "4월")
- 학생 (relation): 학생 page_id
- 클래스 (relation): 반 page_id
- 주간, 월간피드백 작성날짜 (date): 오늘

### 4. 수업결과 relation 자동 연결

3번에서 만든 row(또는 덮어쓰기 대상 기존 row)의 `수업결과` relation에 1번에서 찾은 일일피드백 row N개 모두 연결.

`API-patch-page`:
```
properties:
  수업결과 (relation): [{id: row1}, {id: row2}, ...]
```

### 5. AI 종합 의견 생성 + 사용자 검수 (반복 가능)

#### 5-1. AI 종합 의견 초안 생성

입력 데이터:
- 일일피드백 N개의 수업내용 (rollup된 수업내용 종합)
- 과제 관련 선생님 의견(AI 또는 수동)
- 테스트 관련 선생님 의견(AI 또는 수동)
- 수업태도·과제완수도 분포

출력: 한 문단 한 달 종합 의견 (200~400자 권장)

스타일 가이드:
- "이번 달 학생은 ..."으로 시작
- 학습 내용·태도·과제 흐름·향후 방향 포함
- 학부모용이므로 정중하고 따뜻한 어조
- 부정적 표현 자제 (개선 필요 사항은 부드럽게)

#### 5-2. 터미널 출력 + 검수 (필수)

생성된 의견을 **터미널에 그대로 출력** + 테스트 결과 의견(있는 경우)도 함께 출력:

```
━━━━━━━━━━━━━━━
📋 검수 대상

[월간 선생님 의견]
{AI 생성 의견 본문}

[테스트 결과 의견 — 4/18 회차]
{테스트 의견 본문 — 일일피드백에서 가져온 것}

━━━━━━━━━━━━━━━

이대로 적용할까요?
- 네 → 다음 단계 진행
- 수정 / 의견만 수정 → 의견 새로 입력 → 다시 미리보기
- 테스트만 수정 → 테스트 의견 새로 입력 → 다시 미리보기
- 아니오 → 종료 (지금까지 생성한 row 롤백)
```

#### 5-3. 검수 분기

- **네/y/응** → 5-4 단계 (적용)
- **수정/의견 수정** → 사용자에게 "어떻게 수정하시겠어요?" → 새 의견 입력 → 5-2로 복귀 (반복 횟수 무제한)
- **테스트만 수정** → 테스트 의견 새 입력 → 5-2로 복귀
- **아니오/취소** → 4단계에서 생성한 월간피드백 row와 relation 모두 롤백 → 종료
- 모호한 답 → 재질문 (메모리 `feedback_yes_no_korean` 준수)

#### 5-4. 의견 적용

- 월간 의견은 `월간 선생님의견AI` 필드에 patch
- 테스트 의견은 페이지 본문의 Callout 3 내부 paragraph에 사용 (다음 단계 6에서)

### 6. 페이지 본문 자동 생성 — callout 4개

#### 6-1. 사진 노션 internal 업로드 (사용자 검수 후 실행)

> **중요**: 노션 API의 image block external 타입은 일일피드백 row의 S3 presigned URL을 reference만 함 → 1시간 후 만료. **반드시 file_upload API로 노션 internal에 사본을 만들어야 영구**.

사진 자동 업로드 PowerShell 흐름:

```powershell
# 1) S3 URL에서 이미지 다운로드 → 임시 폴더
$tempDir = "G:\내 드라이브\ClaudeSetting\temp\feedback-images"
New-Item -ItemType Directory -Force -Path $tempDir | Out-Null
Invoke-WebRequest -Uri $s3Url -OutFile $tempPath -UseBasicParsing

# 2) 노션 file_upload 객체 생성 (POST /v1/file_uploads)
$headers = @{
  "Authorization" = "Bearer $token"
  "Notion-Version" = "2022-06-28"
  "Content-Type" = "application/json"
}
$body = @{ filename = $filename; content_type = "image/jpeg" } | ConvertTo-Json -Compress
$resp = Invoke-RestMethod -Uri "https://api.notion.com/v1/file_uploads" -Method Post -Headers $headers -Body $body
$uploadId = $resp.id

# 3) 파일 데이터 전송 (POST /v1/file_uploads/{id}/send)
$asciiCopy = "$env:TEMP\notion_upload_$uploadId.jpg"
Copy-Item -LiteralPath $tempPath -Destination $asciiCopy -Force
$sendUrl = "https://api.notion.com/v1/file_uploads/$uploadId/send"
& curl.exe -s -X POST $sendUrl `
  -H "Authorization: Bearer $token" `
  -H "Notion-Version: 2022-06-28" `
  -F "file=@$asciiCopy;type=image/jpeg"
Remove-Item $asciiCopy
```

각 사진마다 1)~3) 반복. 반환된 `uploadId`들을 모아두기.

> 한글 경로는 `curl.exe`가 multipart에 처리 어려울 수 있어 `$env:TEMP`로 ASCII 복사 후 업로드.

#### 6-2. callout 4개 + 본문 추가 (`API-patch-block-children`)

사진이 들어가는 image block은 **file_upload 타입**으로:
```json
{
  "object": "block",
  "type": "image",
  "image": {
    "type": "file_upload",
    "file_upload": { "id": "<uploadId>" }
  }
}
```

callout icon이 있으므로 heading_3에 이모지 중복 X (예: callout icon `📚`만, heading은 "이번달 진행한 수업"). 메모리 `feedback_command_design_dialogue` 준수 — 시각 정리.

#### Callout 1 (icon: 📚) — 이번달 진행한 수업

내부 블록 (heading_3에 이모지 X, callout icon이 시각 표시):
- divider
- 각 일일피드백 row마다:
  - heading_3: `"<날짜>\n<학생> 학생 <반> 수업"` (예: "2026년 4월 9일\n김민석 학생 코딩 초등반 수업")
  - paragraph: `"정규/보강 : <값>\n출결 : <값>\n과제완수도 : <값>\n수업태도 : <값>"`
  - paragraph: `"▶수업내용\n<수업내용 rollup 값>"`
- heading_3: "수업사진" (이모지 X)
- divider
- 각 row의 `수업사진, 영상` files → file_upload 후 image block (각 row 최대 2장)

#### Callout 2 (icon: 📝) — 월간 과제 결과 피드백

내부 블록:
- divider
- 각 일일피드백 row마다 (과제 있는 경우만):
  - heading_3: `"<날짜>\n과제완수도 : <값>"`
  - paragraph: `"▶지난과제내용\n<지난과제내용>"` (비어있으면 생략)
  - paragraph: `"▶과제 선생님의견\n<과제 관련 선생님 의견AI>"`
- heading_3: "과제 사진"
- divider
- `과제사진` files → file_upload 후 image block (각 row 최대 2장)

#### Callout 3 (icon: 📊) — 월간 테스트 결과 피드백

내부 블록:
- divider
- 각 일일피드백 row마다 (의미있는 테스트 의견 있는 경우만):
  - heading_3: `"<날짜>"`
  - paragraph: `"▶테스트주제\n<테스트주제>"` (비어있으면 생략)
  - paragraph: `"▶테스트 선생님의견\n<테스트 관련 선생님 의견AI — 검수 후 최종 텍스트>"`
- heading_3: "테스트 결과 사진"
- divider
- `테스트사진` files → file_upload 후 image block (각 row 최대 2장)

> "No content" 또는 "별도 테스트는 진행하지 않았습니다." 같은 메타 메시지인 회차는 표시 생략.
> 테스트가 한 건도 없으면 callout 자체를 생략.

#### Callout 4 (icon: 💡) — 월간 선생님 의견

내부 블록:
- divider
- paragraph: AI 생성 의견 (5단계 검수 후 최종 텍스트)

### 7. 미리보기 출력

```
━━━━━━━━━━━━━━━
📦 월간피드백 미리보기

학생       : 김민석
반         : 코딩 초등반
월         : 2026년 4월
수업 수    : 4회 (정규 3회 + 보강 1회)
사진 총수  : 수업 5장 + 과제 4장 + 테스트 2장 = 11장

💡 AI 종합 의견:
{생성된 의견 본문}

━━━━━━━━━━━━━━━

이대로 적용할까요? (네 / 아니오 / 수정)
```

### 8. 검수 분기

- **네/y/yes/응** → 의견 그대로 적용. callout 4개 페이지에 추가. 완료 보고.
- **수정/edit** → "어떻게 수정하시겠어요?" → 사용자가 새 의견 입력 → 의견 교체 → 다시 미리보기
- **아니오/no/취소** → 4단계에서 생성한 row 및 relation 모두 삭제 (롤백) → 종료
- 모호하면 재질문 (메모리 `feedback_yes_no_korean` 준수)

### 9. 최종 결과 보고

```
✅ 월간피드백 페이지 완성

피드백라벨 : 4월 코딩 초등반 김민석 월간 피드백
페이지 URL : <노션 URL>
포함 회차  : 4회
사진       : 11장

다음 단계 (Phase 2):
이 페이지는 현재 비공개 상태입니다. 학부모 발송은 Phase 2 (도메인·호스팅·문자/카톡 자동 발송) 셋업 후 진행됩니다.

확인하실 점:
1. 페이지 URL 클릭해서 본문 내용·사진 정상 확인
2. 어색한 부분 있으면 노션 UI에서 직접 수정 가능
```

## 엣지케이스

- **일일피드백 row 0건** → "그 학생의 그 월 수업 데이터가 없습니다. 일일피드백이 누락되지 않았는지 확인해주세요" 안내 후 중단
- **학생 0명** (재원생 없음) → "재원생이 없습니다" 중단
- **학생 입력 모호 또는 동명이인** → 후보 표시 후 재선택
- **사진 첨부 실패** (S3 URL 만료 등) → 해당 사진만 skip하고 나머지 계속
- **AI 의견 생성 실패** → 의견 빈 상태로 진행 + 사용자에게 직접 입력 요청
- **사용자가 중간 취소** → 이미 생성된 row·relation 모두 롤백
- **테스트 한 건도 없음** → Callout 3 자체 생략 (사용자에게 알림)

## 답변 인식 (네/아니오)

메모리 `feedback_yes_no_korean`:
- 표시: `(네/아니오)` 또는 `(네/아니오/수정)`
- 자유 답변 받기: y / yes / 네 / 응 → 긍정 / n / no / 아니 / 취소 → 부정 / edit / 수정 → 수정 모드
- 모호하면 재질문

## 주의사항

- **확인 없이 페이지 본문 적용 금지**: 사용자의 명시적 (네) 응답 없이 절대 page block 추가 금지
- 메모리 `feedback_simple_commands` 준수 — 학부모 발송·통계·알림 등 Phase 2 기능은 절대 추가하지 말 것
- 사진은 일일피드백 row의 `수업사진, 영상` / `과제사진` / `테스트사진` files property에서 가져옴. 노션 internal file은 S3 presigned URL이라 image block 추가 시 external file 타입으로 그대로 사용
- **사진 개수 제한**: 각 일일피드백 row마다 각 섹션 최대 2장. 정렬은 files property 순서 (첫 2장)
- **수업결과 relation 연결은 부수 효과**: 이 작업으로 다른 rollup 필드들(과제사진 종합·테스트사진 종합 등)도 자동으로 채워짐. 사용자에게 안내

## 예시 흐름

```
사용자: /월간피드백

Claude: 운영 중인 반 목록:
        1. 코딩 초등반 (담당: 문성준, 수강 5명)
        2. 코딩 중등반 (담당: 김동환, 수강 3명)
        3. 마인크래프트 특강 (담당: 이승환, 수강 8명)
        
        월간피드백 작성할 반을 선택해주세요. (번호 또는 이름)
사용자: 1

Claude: 월간피드백 작성할 학생 이름을 입력해주세요.
        (모르겠으면 "목록")
사용자: 김민석

Claude: 어느 달 월간피드백 작성하시나요? (예: 2026년 4월)
사용자: 2026년 4월

[자동: 4월 일일피드백 검색 - 학생 김민석 + 반 코딩 초등반(문성준)]

Claude: 김민석 학생 4월 코딩 초등반(문성준) 수업 3회 검색됨.

[월간피드백 row 생성: "4월 코딩 초등반 김민석 월간 피드백"]
[수업결과 relation 연결: 3건]
[AI 종합 의견 생성 중...]

Claude: 미리보기 (위 형식)
        이대로 적용할까요? (네/아니오/수정)
사용자: 네

[페이지 본문 callout 4개 추가]

Claude: ✅ 월간피드백 페이지 완성 (위 형식)
        페이지 URL: https://www.notion.so/...
```

## Phase 2 진입 조건

다음 사항이 결정되면 Phase 2 추가 명령어 또는 기능 확장:
- 학원 도메인 셋업 (예: your-academy.com)
- 호스팅 방식 (Cloudflare Pages / GitHub Pages / Vercel 등)
- 노션 페이지 → HTML 변환 도구 (Python 라이브러리 결정)
- 문자/카카오 알림톡 API 연동
- 학부모 격리 보안 (UUID URL, noindex, 만료 등)
