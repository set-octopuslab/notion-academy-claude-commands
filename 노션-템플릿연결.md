---
description: Git에서 받은 노션 명령어를 사용자의 노션 학원관리 템플릿에 자동 연결 (16개+게시판 DB id를 검색·매칭해 config 자동 작성)
---

# 노션 템플릿 자동 연결

사용자가 `/노션-템플릿연결` 을 실행하면, 노션 워크스페이스의 학원관리 템플릿(상위 페이지)을 받아 그 안의 데이터베이스를 자동 검색·매칭해 `notion-academy-config.json` 을 작성한다. 다른 학원 사용자가 Git에서 명령어 폴더를 받은 직후 1회 실행 → 이후 모든 `/학생-신규등록` `/결제선생` 등이 동작 가능해진다.

수동 입력으로 셋업하려면 `/노션-초기설정` 을 사용. 두 명령어는 결과 파일이 같다.

## 사전 조건

- 노션 MCP 가 Claude Code에 연결되어 있어야 함 (claude_desktop_config 또는 settings.json의 `mcpServers.notionApi`)
- 학원관리 노션 템플릿이 사용자 워크스페이스에 복제·세팅되어 있어야 함 (옥토퍼스코딩 노션 템플릿 또는 동등 구조)
- 노션 MCP 통합이 그 템플릿 페이지에 권한 추가되어 있어야 함

## Config 저장 위치

- **Config 파일 기본 위치**: `~/.claude/notion-academy-config.json` (사용자 홈, 로컬 전용)
- 또는 `G:/내 드라이브/ClaudeSetting/notion-academy-config.json` 같은 공유 위치 (여러 PC에서 한 학원을 공유하는 환경, 예: G드라이브 마운트)
- 사용자가 여러 PC와 한 학원 데이터를 공유한다고 하면 공유 위치 권장
- 이 파일이 이후 모든 학원관리 커맨드의 진입점

## 절차

### 1. MCP 도구 로드 + 연결 확인

```
select:mcp__notionApi__API-get-self,mcp__notionApi__API-post-search,mcp__notionApi__API-retrieve-a-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-get-block-children
```

`mcp__notionApi__API-get-self` 호출. 실패 시 사용자에게 "노션 MCP 연결이 필요합니다. setup/setup-notion.md 또는 노션 통합 설정을 먼저 진행해주세요." 안내 후 중단.

### 2. 학원관리 템플릿 상위 페이지 URL 입력 받기

사용자에게:
```
학원관리 노션 템플릿의 부모(상위) 페이지 URL을 붙여넣어 주세요.
예: https://www.notion.so/내워크스페이스/학원관리-329ca39da8438156...

(이 페이지 아래에 학생·선생님·클래스관리 등 16개 DB가 들어있어야 합니다)
```

URL에서 page_id 추출:
- 마지막 32자 hex (또는 dash 포함 36자)를 정규식으로 추출
- 추출 실패 시 "URL에서 페이지 id를 찾지 못했습니다. URL 끝의 32자 hex를 직접 알려주세요." 재요청
- dash 형식(`8-4-4-4-12`)으로 정규화

### 3. 워크스페이스 DB 자동 검색·매칭

검색 전략: 사용자 워크스페이스의 모든 데이터베이스를 한 번에 가져와서, 이름 기반으로 16개 + 게시판 = 17개 키에 매칭한다.

#### 3-1. 전체 DB 목록 수집

`mcp__notionApi__API-post-search` 호출 (전체):
```json
{
  "filter": { "value": "data_source", "property": "object" },
  "page_size": 100
}
```

next_cursor 가 있으면 추가 호출해서 모두 수집. 결과 각 row에서 다음 추출:
- `id` (data_source id)
- `database` 필드의 `id`, `title` (database id, DB 제목)
- `parent` (워크스페이스 부모 파악용)

#### 3-2. 매칭 키 정의

17개 키 ↔ DB title 매칭 규칙 (한글 부분 매칭, 공백·괄호 무시):

| config 키 | DB 제목 매칭 (부분 일치, 대소문자 무시) |
|---|---|
| `students` | "학생" (단, "학부모"·"학생관리"·"학생블로그" 제외 — 정확히 "학생" 또는 "학생 DB") |
| `parents` | "학부모" |
| `monthly_tuition` | "월간 수강료" (단, "데이터" 포함 안 되는 것 — 헤더 DB) |
| `tuition_lines` | "월간 수강료 데이터" 또는 "특강 수강료 데이터" → **두 후보 모두 후보 표시** (옥토퍼스는 월간 라인 = "월간 수강료 데이터", 다른 학원은 명명이 다를 수 있음. 가장 잘 매칭되는 것 자동 선택, 둘 다 있으면 사용자에게 선택) |
| `regular_schedule` | "정규" + "시간표" (예: "정규수업 시간표") |
| `makeup_schedule` | "보강" + "시간표" |
| `classes` | "클래스관리" 또는 "반관리" |
| `lesson_results` | "수업결과" 또는 "일일피드백" 또는 "개인 수업결과" |
| `lesson_plans` | "수업계획" 또는 "수업요약" |
| `academy_calendar` | "학원" + "캘린더" 또는 "캘린더" |
| `refunds` | "환불" |
| `consultations` | "상담결과" (단순 "상담"이면 booking과 충돌하므로 "결과" 또는 "기록" 포함) |
| `consultations_booking` | "상담예약" |
| `class_enrollments` | "수강 등록" 또는 "수강등록" |
| `teachers` | "선생님" 또는 "강사" |
| `supplies` | "물품" 또는 "운영 물품구매" |
| `weekly_monthly_feedback` | "주간" OR "월간" + "피드백" (예: "주간 or 월간피드백") |
| `boards` | "게시판" |

#### 3-3. 매칭 실행

각 키마다:
1. 후보 DB 목록 필터링 (제목 부분 매칭)
2. 후보 1개 → 자동 선택 (data_source id 채택)
3. 후보 2개 이상 → 후보 목록을 미리 표시한 뒤 사용자에게 번호로 선택받기
4. 후보 0개 → `null` 로 두고 진행 (나중에 보고에 누락으로 표시)

#### 3-4. 매칭 결과 미리보기

```
🔍 자동 매칭 결과

✅ 매칭 성공 (17/17):
   학생 (students)               → 학생 DB (id: 329c...)
   학부모 (parents)              → 학부모 DB (id: 34cc...)
   월간 수강료 (monthly_tuition)  → 월간 수강료 (id: 329c...)
   ... (생략)

⚠️ 누락 (0개):
   (없음)

이 매핑으로 config을 작성할까요? 일부 수정이 필요하면 알려주세요.
(네 / 아니오 / [키]만 다시 / 누락 [키] 직접 입력)
```

사용자가 "네" → 4단계로
사용자가 수정 요청 → 해당 키만 다시 매칭 또는 직접 id 입력

#### 3-5. 게시판 database_id 추가 수집 (있을 때만)

`/노션-게시판` 은 일부 호출에 data_source_id 대신 database_id 필요. `boards` 매칭에 성공했다면, search 응답에서 그 DB의 `database` 필드 `id` 를 함께 저장 → `notion.boards_database_id`.

### 4. 사용자 직접 입력 항목

자동 매핑으로는 알 수 없는 값들. 한 번에 한 질문씩.

#### 4-1. 학원명

```
학원명을 알려주세요. (예: 옥토퍼스코딩, 행복코딩학원 등)
이 값은 메시지·게시판 안내에 노출될 수 있습니다.
```

#### 4-2. 노션 API token (선택)

```
노션 Internal Integration Secret (ntn_ 으로 시작)을 알려주세요.
- 사진 자동 첨부(/주간피드백·/월간피드백 file_upload)에 필요
- MCP만 쓰는 경우 비워둬도 OK ("없음" 입력)
- 발급 방법: notion.so/profile/integrations
```

빈 문자열 또는 "없음" → `""` 저장.

#### 4-3. 디스코드 학부모 웹훅

```
학부모 안내(수강료·일일피드백) 발송용 디스코드 채널 웹훅 URL을 알려주세요.
- 디스코드 채널 설정 → 연동 → 웹훅 → 새 웹훅 → URL 복사
- 없으면 "없음" (디스코드 발송 기능 비활성)
```

`https://discord.com/api/webhooks/...` 형식 검증.

#### 4-4. 디스코드 세션 저장 웹훅 (선택)

```
세션 저장 알림용 디스코드 채널 웹훅 URL을 알려주세요 (선택).
- /디코저장 명령어가 이 채널로 재개 메시지를 보냅니다
- 학부모 채널과 반드시 분리하세요 (학부모 메시지가 잘못 가지 않게)
- 안 쓰면 "없음"
```

#### 4-5. 매월 결제 기준일

```
매월 며칠을 결제 안내일로 표시할까요? (1~31, 기본 1)
- 결제선생 메시지에 "매월 X일에 결제 부탁드립니다" 같이 들어갑니다
```

기본 1, 1~31 범위 검증.

#### 4-6. 수강료 패턴 (선택, 기본 4종 유지)

```
수강료 패턴 4종이 기본값으로 설정됩니다:
- 초주1: 200,000원 (4회, 회당 50,000)
- 초주2: 350,000원 (8회, 회당 50,000, 버퍼 1)
- 중고주1: 250,000원 (4회, 회당 62,500)
- 중고주2: 400,000원 (8회, 회당 50,000)

이대로 사용할까요? (네 / 수정)
- 수정하려면 어떤 패턴 어떻게 바꿀지 알려주세요
- 수강료는 나중에 config 파일을 직접 수정해도 됩니다
```

#### 4-7. 월별 인사말 (선택)

```
결제선생 메시지 끝에 붙는 월별 인사말이 기본 12개 설정됩니다.
이대로 사용할까요? (네 / 나중에 직접 수정)
```

### 5. Config 파일 저장

최종 구조 (`notion-academy-config.example.json` 참고):

```json
{
  "_version": 5,
  "_workspace_root": "<3단계에서 추출한 page_id>",
  "academy": { "name": "<4-1 입력값>" },
  "discord": {
    "parent_webhook": "<4-3>",
    "session_save_webhook": "<4-4>",
    "username": "결제선생"
  },
  "notion": {
    "api_token": "<4-2>",
    "boards_database_id": "<3-5>",
    "data_sources": {
      "students": "...", "parents": "...", ...  (3단계 매칭 결과 17개)
    }
  },
  "tuition": {
    "patterns": { ... (4-6) },
    "_매월_결제기준일": <4-5>,
    "_천원_절삭": true
  },
  "month_closing": { ... (4-7 또는 기본 12개) }
}
```

저장 위치:
- 기본: `~/.claude/notion-academy-config.json` (Windows: `C:/Users/<본인>/.claude/notion-academy-config.json`, Mac/Linux: `$HOME/.claude/notion-academy-config.json`)
- 여러 PC 공유 환경 (예: G드라이브 마운트) → `G:/내 드라이브/ClaudeSetting/notion-academy-config.json` 같은 공유 경로 사용

Write 도구로 저장. 디렉토리 없으면 생성.

기존 파일이 있으면 **백업** (`notion-academy-config.json.bak.YYYYMMDD-HHMM`) 후 덮어쓰기.

### 6. 검증 단계 (read-only)

매칭된 DB 각각에 대해 `mcp__notionApi__API-retrieve-a-data-source` 호출해 필수 필드 존재 여부 점검:

| DB 키 | 필수로 보고 싶은 필드 (없으면 경고) |
|---|---|
| `students` | 학생이름(title), 등록현황(select), 초·중·고(select) |
| `parents` | 학부모(title), 관계(select), 주 연락처(checkbox) |
| `monthly_tuition` | 청구라벨(title), 학생(relation), 수강료 년/월(rich_text), 매월 결제해야하는날짜(number) |
| `tuition_lines` | 라인라벨(title), 월간 수강료(relation), 대상월(date), 단가(number), 회당 단가(number) |
| `regular_schedule` | 시간표 라벨(title), 수강생(relation), 요일(select), 시간(rich_text) |
| `makeup_schedule` | 시간표 라벨(title), 수강생(relation), 보강수업 날짜(date) |
| `classes` | 클래스명(title), 담당 선생님(relation) |
| `lesson_results` | 라벨(title), 학생(relation), 수업일자(date), 출결(select) |
| `lesson_plans` | 라벨(title), 클래스(relation), 수업일자(date) |
| `academy_calendar` | 일정명(title), 일정 기간(date), 일정 유형(select) |
| `consultations` | 상담라벨(title), 학생(relation), 상담일시(date) |
| `consultations_booking` | 예약라벨(title), 학생(relation), 예약일시(date) |
| `class_enrollments` | 등록라벨(title), 학생(relation), 클래스(relation) |
| `teachers` | 선생님이름(title), 재직(checkbox) |
| `supplies` | 물품명(title), 구매자(relation), 카테고리(select), 금액(number) |
| `weekly_monthly_feedback` | 라벨(title), 학생(relation), 반(relation), 작성월(date) |
| `boards` | 제목(title), 내용(rich_text), 글 작성자(relation), 글 유형(select), 작성일(date) |
| `refunds` | 환불라벨(title), 학생(relation) |

필드명이 정확히 일치하지 않더라도 유사 이름(예: "학생이름" 대신 "이름")이 있으면 통과. 정말 없는 경우만 경고 표시.

검증 결과:
```
🔎 DB 필드 검증

✅ 정상 (15개):
   학생, 학부모, 월간 수강료, ...

⚠️ 필드 누락 (2개 — 명령어 동작에 영향 있음):
   학생 DB — "초,중,고" 필드 없음
      → /수강등록 의 수강료 패턴 자동 판별이 동작하지 않을 수 있습니다.
   상담결과 DB — "상담일시" 필드 없음
      → 직접 노션 페이지 속성을 추가하거나 명령어를 수동 모드로 사용하세요.

이대로 진행하시겠어요? (네/아니오)
```

사용자가 "아니오" → 어떻게 진행할지 물음. "네" → 7단계.

### 7. 완료 보고

```
✅ 노션 학원관리 템플릿 연결 완료

📁 Config 파일: <저장된 실제 경로> (기본 `~/.claude/notion-academy-config.json`)
🏫 학원명: <이름>
📚 매칭된 DB: 17/17
🔔 디스코드 학부모 채널: 연결됨 ✓ / 미설정
🔔 디스코드 세션 저장 채널: 연결됨 ✓ / 미설정
🔑 노션 API token: 설정됨 ✓ / 비어있음 (file_upload 기능 비활성)

이제 다음 명령어를 사용할 수 있습니다:
- /학생-신규등록 — 신규 학생을 학생 DB에 등록
- /클래스등록 — 반 신규 생성
- /수강등록 — 학생-반 매핑 + 수강료 설정
- /시간표등록 — 매주 반복 수업 시간표 등록
- /출석·/결석 — 출결 기록
- /수업결과 — 일일피드백 입력
- /결제선생 — 수강료 안내 발송 (미리보기 → 확인 → 발송)
- /상담예약·/상담기록 — 상담 관리
- ... (전체 22개 명령어, README 참고)

먼저 /결제선생 --dryrun 으로 수강료 계산이 정상 동작하는지 확인해보세요.
```

## 주의사항

- **이 명령어는 노션에 쓰지 않음**. retrieve·search·query 만 사용 (read-only)
- 노션 API token 값은 응답·로그·메모리 어디에도 출력하지 말 것 (저장만)
- 디스코드 웹훅 URL도 응답·로그에 출력 금지 (저장만)
- 매칭 결과 미리보기에서 id 끝 4자리만 표시 권장 (`329c...3b59`)
- 기존 config가 있는데 일부 키만 새로 입력하고 싶다면 `/노션-초기설정` (수동 입력)을 권장 — 이 명령어는 전체 재작성
- 옥토퍼스코딩 외 학원에서는 DB 제목이 약간 다를 수 있음. 그 경우 매칭 단계에서 사용자가 직접 선택. 정 안 되면 `/노션-초기설정` 으로 수동 입력
- 16개 DB 중 누락분(예: 게시판이 없는 학원, 환불 DB 미사용)이 있어도 동작은 함 — 해당 DB를 사용하는 명령어만 동작 안 함

## 예시 흐름

```
사용자: /노션-템플릿연결

Claude: 노션 MCP 연결 확인 중... ✓ 연결됨
        학원관리 노션 템플릿의 부모(상위) 페이지 URL을 알려주세요.

사용자: https://www.notion.so/myacademy/학원관리-aabb1122cc33...

Claude: ✓ 페이지 id 추출: aabb1122-cc33-...
        워크스페이스의 데이터베이스를 검색하는 중... (28개 발견)
        매칭 결과:

        ✅ 매칭 성공 (16/17):
           학생 (students)               → 학생 DB (...aaaa)
           학부모 (parents)              → 학부모 DB (...bbbb)
           ...

        ⚠️ 누락 (1개):
           refunds (환불 DB)             → 후보 없음 (해당 DB를 만들지 않은 듯)

        환불 DB 없이 진행할까요? (네/아니오/직접 입력)

사용자: 네

Claude: 학원명을 알려주세요.

사용자: 행복코딩학원

Claude: 노션 Internal Integration Secret을 알려주세요. (없으면 "없음")

사용자: ntn_xxxxxxxx

Claude: 학부모 안내용 디스코드 웹훅 URL을 알려주세요. (없으면 "없음")

... (4-4 ~ 4-7) ...

Claude: 입력 완료. config 미리보기:
        - 학원명: 행복코딩학원
        - 매월 결제일: 1일
        - 매칭된 DB: 16/17 (환불 없음)
        - 디스코드 학부모 채널: 연결됨
        이대로 저장할까요? (네/아니오)

사용자: 네

Claude: ✅ 저장 완료. DB 필드 검증 중...
        ✅ 정상 (16개)
        ⚠️ 필드 누락 (0개)

        ✅ 노션 학원관리 템플릿 연결 완료
        ... (7단계 보고)
```
