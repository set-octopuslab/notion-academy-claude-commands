---
description: 수업 시간표 row의 시간(요일·시간) 또는 주N회(횟수) 변경 + 디스코드 선생님 검수 발송 (학원 전체/선생님/반/학생 분기, 횟수 변경은 학생 1명 한정)
---

# 시간표변경

원장이 `/시간표변경` 을 입력하면, **변경 범위 분기(학원 전체/선생님/반/학생) → 변경 항목 입력(요일/시간/요일+시간/주N회) → 변경 시작일 → 충돌 검사 → 미리보기 → 노션 patch → 디스코드 검수 발송** 순서로 처리한다.

**v2 — 주N회 변경 분기 추가** (학생 1명 분기에서만 활성화):
- 늘림 (주1회 → 주2회): 추가 요일·시간으로 새 row 생성 + 수강 등록 DB 월간 횟수 patch
- 줄임 (주2회 → 주1회): 기존 row 1개씩 확인 후 archive + 수강 등록 DB 월간 횟수 patch
- 청구는 다음 달부터 새 횟수 적용 (당월 청구 유지)

반 변경(다른 반 이동)은 처리 X — 옛 수강 등록 종료 + /수강등록 재호출(운영자 수동). 학부모 카톡 안내도 운영자 수동.

> ⚠️ **Claude 동작 가드 (반드시 준수)**
>
> 1. **AskUserQuestion 도구 사용 금지**. 모든 질문은 일반 텍스트 출력 + 자유 답변 받기.
> 2. **자의적 부연 설명 금지**. .md에 정의되지 않은 안내문 노출 금지.
> 3. **session_save_webhook 절대 사용 금지**. `discord.parent_webhook`만 사용.
> 4. **메시지에 학부모 전화번호 포함 금지** (개인정보 보호).
> 5. **학부모 카톡 발송 절대 시도 금지**. 이 명령어는 디스코드 선생님 검수만. 학부모 안내는 운영자가 노션 보고 수동 처리.
> 6. **반 변경 처리 절대 금지**. 시간(요일·시간) 또는 횟수(주N회)만 변경. 학생 반 이동은 운영자 수동.
> 7. **시간표라벨(title) 자동 갱신**. 시간 변경 시 title도 함께 patch (`{학생} - {반} ({요일}{시작시간})`).
> 8. **횟수 변경은 학생 1명 분기에서만**. 학원 전체·선생님·반 범위에서는 4번 항목(주N회 변경) 숨김.
> 9. **횟수 변경 시 수강 등록 patch 필수**. 시간표 row 변경과 동시에 수강 등록 DB의 `월간 수업 횟수` patch. 실패 시 시간표 변경 롤백.

## 범위

**변경 가능 필드 (시간 변경)**
- 수업요일 (select: 월~일)
- 수업 시작 시간 (rich_text, HH:MM)
- 수업 종료 시간 (rich_text, HH:MM)
- 시간표라벨 (title) — 자동 갱신

**변경 가능 동작 (횟수 변경 — 학생 1명 분기 한정)**
- 늘림: 수업 시간표 DB에 row 신규 생성 (N-M건)
- 줄임: 수업 시간표 DB row archive (M-N건)
- 동반 patch: 수강 등록 DB의 `월간 수업 횟수` ({M*4} → {N*4})

**변경 불가 (이 명령어에서 처리 X)**
- 수업선택 (반) — 반 변경 시 운영자 수동 (옛 수강 등록 종료 + /수강등록 재호출)
- 수강생 (학생) — row의 학생 그대로 유지
- 보강수업 시간표 — `/보강시간표등록` 별도

**제외 (별도 처리)**
- 학부모 카톡 안내 — 운영자 수동 (노션 보고 직접)

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-patch-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.regular_schedule` (수업 시간표)
   - `notion.data_sources.classes`
   - `notion.data_sources.students`
   - `notion.data_sources.teachers`
   - `notion.data_sources.makeup_schedule` (보강 충돌 검사용)
   - `discord.parent_webhook`
   - `discord.username`
3. 현재 날짜 기록

## 절차

### 1. 변경 범위 분기 (4가지)

```
[1/?] 어떤 시간표 변경인가요?
  1. 학원 전체 시간표 변경 (전체 시간대 이동 — 매우 드뭄)
  2. 특정 선생님 시간 변경 (그 선생님 담당 반들 일괄)
  3. 특정 반 시간 변경 (반 1개의 시간 — 그 반 모든 학생 영향)
  4. 학생 1명 시간 변경 (그 학생의 1개 시간표 row만)
번호 선택:
```

분기:
- "1" → `scope = "academy"` → 1-A
- "2" → `scope = "teacher"` → 1-B
- "3" → `scope = "class"` → 1-C
- "4" → `scope = "student"` → 1-D
- 그 외 → 재입력

#### 1-A. 학원 전체 (scope == "academy")

쿼리: 수업 시간표 DB 전체 row

```
⚠️ 학원 전체 시간표 변경 — 수업 시간표 DB의 모든 row({N건})가 영향받습니다.
   이 케이스는 매우 드뭅니다 (전체 시간대 이동 등).
   진행하시겠어요? (네/아니오)
```

긍정 → `target_rows` = 모든 row. 부정 → 종료.

#### 1-B. 특정 선생님 (scope == "teacher")

쿼리: 선생님 DB
- `재직상태 == "재직"`

```
[?/?] 어느 선생님의 시간표 변경인가요?
  1. 이승환 (재직, 수업과목: 파이썬·코딩)
  2. 김동환 (재직, 수업과목: 코딩)
  3. 문성준 (재직, 수업과목: 코딩)
번호 선택:
```

- 0건 → "재직 선생님 없음" 종료
- 선택 후 클래스관리 DB query (선생님 == 선택한 선생님인 반들)
- 그 반들의 수업 시간표 row 모두 → `target_rows`

#### 1-C. 특정 반 (scope == "class")

쿼리: 클래스관리 DB 전체

```
[?/?] 어느 반의 시간표 변경인가요?
  1. 파이썬 초등 A (담당: 김선생)
  2. C언어 중등 B (담당: 이선생)
  ...
번호 선택:
```

- 0건 → "/클래스등록 먼저" 종료
- 선택한 반의 수업 시간표 row 모두 → `target_rows`

#### 1-D. 학생 1명 (scope == "student")

```
[?/?] 어떤 학생의 시간표 변경인가요? (이름 일부)

> 
```

쿼리: 학생 DB — `학생이름 contains <입력>` AND `등록현황 == "등록"`

- 0건 → "/학생-신규등록 먼저" 종료
- 1건 → 자동 선택 + 확인
- 2건+ → 학교·학년 표시 후 선택

선택한 학생 page_id를 `selected_student` 변수로 저장.

> ⚠️ 학생 1명 분기에서는 시간표 row 선택을 **변경 항목 선택 뒤(2단계 끝)** 로 미룬다. 변경 항목이 1·2·3(요일·시간)이면 row 1개 선택, 4(주N회)면 학생 전체 시간표 자동 처리.

### 2. 변경 내용 입력

**범위 1·2·3 (학원 전체·선생님·반)**: 1·2·3 항목만 표시.

```
[2/?] 어떤 항목을 변경하시나요?
  1. 요일만 변경
  2. 시간만 변경
  3. 요일·시간 둘 다 변경
번호 선택:
```

**범위 4 (학생 1명)**: 4번 항목(주N회 변경) 추가 표시.

```
[2/?] 어떤 항목을 변경하시나요?
  1. 요일만 변경
  2. 시간만 변경
  3. 요일·시간 둘 다 변경
  4. 주N회 변경 (수업 횟수 늘림/줄임)
번호 선택:
```

분기에 따라 입력 단계:

#### 2-pre. 변경 대상 row 선택 (학생 1명 분기 + 변경 항목 1·2·3 한정)

> ⚠️ 이 단계는 **범위 4(학생 1명) + 변경 항목 1·2·3** 케이스에서만 작동.
> - 범위 1·2·3 (학원 전체·선생님·반): `target_rows` 이미 1-A/B/C에서 수집됨. 이 단계 스킵.
> - 변경 항목 4 (주N회): 학생 전체 시간표 자동 처리. 이 단계 스킵 → 2-4로 직행.

학생 1명 분기에서 변경 항목 1·2·3 선택 시:

쿼리: 수업 시간표 DB
- `수강생 == <selected_student>`

분기:
- 0건 → "이 학생 시간표 없음 (/시간표등록 먼저)" 종료
- 1건 → 자동 선택 + 확인 ("`{요일} {시작}~{종료}` 시간표 맞나요? 네/아니오")
- 2건+ → 목록 선택:

```
[?/?] 어느 시간표를 변경하나요?
  1. 월 16:00~17:30 (코딩 초등반(문성준))
  2. 수 16:00~17:30 (코딩 초등반(문성준))
번호 선택:
```

선택한 row 1건만 → `target_rows`.

#### 2-1. 요일만 (선택 1)

```
[?/?] 새 수업요일?
  1. 월  2. 화  3. 수  4. 목  5. 금  6. 토  7. 일
번호 선택 (1~7):
```

`new_weekday` 결정.

#### 2-2. 시간만 (선택 2)

```
[?/?] 새 수업 시작 시간? (HH:MM, 24시간제)
       (예: 16:00, 17:30)

> 

[?/?] 새 수업 종료 시간? (HH:MM, 24시간제)

> 
```

검증:
- 정규식: `^([01]?[0-9]|2[0-3]):[0-5][0-9]$`
- 단일 자릿수 자동 0 패딩 (`9:00` → `09:00`)
- 시작 ≥ 종료 → 재입력

`new_start_time`, `new_end_time` 결정.

#### 2-3. 둘 다 (선택 3)

위 2-1 + 2-2 모두 입력.

> ⚠️ 같은 변경 내용을 모든 `target_rows`에 일괄 적용. 개별 row 다른 변경은 X (그러려면 학생 1명씩 호출).

#### 2-4. 주N회 변경 (선택 4) — 학생 1명 분기 한정

> ⚠️ 이 분기는 범위 4(학생 1명)에서만 표시. 범위 1·2·3에서는 4번 항목 자체를 노출하지 않음.

##### 2-4-1. 학생의 현재 시간표 + 수강 등록 조회

> ⚠️ 변경 항목 4 선택 시 2-pre 스킵하고 여기로 직행. 학생 전체 시간표 자동 처리 (row 선택 없음).

쿼리: 수업 시간표 DB
- `수강생 == <selected_student>`
- 정렬: 수업요일·시작 시간 순

`current_rows` 수집. M = current_rows 건수 = 현재 주N회.

쿼리: 수강 등록 DB
- `학생 == <selected_student>`
- (수강종료일이 없는 진행 중 등록만)

수강 등록 row가 시간표 row의 반과 매칭되는 row를 `enrollment_row`로 선택. `current_monthly_count` = enrollment_row의 `월간 수업 횟수`.

매칭되는 수강 등록 0건:
```
❌ {학생이름} 학생의 수강 등록 정보를 찾을 수 없습니다.
   /수강등록으로 학생-반 매핑부터 처리해주세요.

종료 (노션 변경 없음).
```

##### 2-4-2. 현재 상태 표시 + 새 주N회 입력

```
[?/?] 주N회 변경

[현재 상태]
- 학생:        {학생이름}
- 반:          {반이름}
- 현재 주N회:  주{M}회 (월 {M*4}회)
- 시간표:
  1. {요일1} {시작1}~{종료1}
  2. {요일2} {시작2}~{종료2}
  ...

[?/?] 새 주N회는? (1~7 사이 정수)

> 
```

검증:
- 정수 1~7 외 → 재입력
- N = M → "현재와 같음. 요일/시간 변경은 1·2·3 분기 사용 권장" 종료
- N > M → 늘림 분기 (2-4-A)
- N < M → 줄임 분기 (2-4-B)

##### 2-4-A. 늘림 분기 (N > M)

추가 row 수 = N - M. K = 1, 2, ..., (N-M) 반복:

```
[?/?] 주{M}회 → 주{N}회. 추가할 시간표 {N-M}건을 입력해주세요.

[{K}번째 추가 시간표] 수업요일?
  1. 월  2. 화  3. 수  4. 목  5. 금  6. 토  7. 일
번호 선택:

[{K}번째 추가 시간표] 시작 시간? (HH:MM, 24시간제)
> 

[{K}번째 추가 시간표] 종료 시간? (HH:MM, 24시간제)
> 
```

검증:
- 정규식: `^([01]?[0-9]|2[0-3]):[0-5][0-9]$`
- 단일 자릿수 자동 0 패딩 (`9:00` → `09:00`)
- 시작 ≥ 종료 → 재입력
- K번째가 이전 K-1번째 또는 current_rows와 같은 요일·시간 → 재입력

`new_rows` 수집 ((N-M)개).

##### 2-4-B. 줄임 분기 (N < M)

archive 대상 수 = M - N. current_rows를 순서대로 순회하며 1개씩 확인:

```
[?/?] 주{M}회 → 주{N}회. {M-N}건을 archive 해야 합니다. 1개씩 확인합니다.

[1/{M}] {요일1} {시작1}~{종료1} ({반이름}) archive할까요? (네/아니오)
> 

[2/{M}] {요일2} {시작2}~{종료2} ({반이름}) archive할까요? (네/아니오)
> 

...
```

규칙:
- 각 row 순서대로 "네/아니오" 확인
- "네" → `archive_rows`에 추가
- archive 대상 수가 (M-N)에 도달하면 즉시 종료 (남은 row는 묻지 않음)
- 모든 row 순회 후에도 (M-N) 미만이면:
  ```
  ⚠️ archive 대상이 {선택된}건만 선택되었습니다 (필요: {M-N}건).
     다시 선택할까요?
     1. 다시 선택 (처음부터)
     2. 종료
  번호 선택:
  ```
- N = 0이 되는 경우(주0회): 진행은 가능하지만 결과 보고에 "주0회는 휴원 처리(/학생-휴원처리) 권장" 안내

`archive_rows` 수집 ((M-N)개).

### 3. 변경 시작일 입력

```
[3/?] 변경 시작일을 입력해주세요.
       YYYY-MM-DD 또는 키워드 ("오늘", "내일", "다음주")
       (학부모 안내에 "○월 ○일부터 변경됩니다" 형식으로 표시됨)

> 
```

처리:
- "오늘" → today
- "내일" → today + 1
- "다음주" → 다음주 월요일
- "YYYY-MM-DD" → 검증 후 사용

검증:
- 형식 오류 → "날짜 형식 오류. YYYY-MM-DD 또는 키워드 입력" 재입력
- 과거 날짜 → "⚠️ 과거 날짜인데 맞나요? (네/아니오)" 확인
- 너무 먼 미래 (오늘 + 6개월 이상) → 경고 + 확인

`change_start_date` 결정.

### 4. 시간 충돌 검사

#### 4-0. 분기별 처리 범위

- **변경 항목 1·2·3 (요일·시간 변경)**: 각 `target_row`에 대해 (학생, 새 요일·시간) 기준 충돌 검사
- **변경 항목 4-A (주N회 늘림)**: 각 `new_rows` 항목에 대해 (학생, 새 요일·시간) 기준 충돌 검사. current_rows + new_rows[이전 K-1개]도 비교
- **변경 항목 4-B (주N회 줄임)**: archive만 하므로 충돌 검사 불필요 (4-3 스킵)

#### 4-1. 같은 학생-다른 시간표 충돌 (수업 시간표)

각 row의 학생에 대해:
```
쿼리: 수업 시간표 DB
- `수강생 == <학생 page_id>` AND `수업요일 == <새 요일>`
- (target_row 자신 또는 new_rows 자신 제외)
```

겹침 판정: `start_new < end_existing AND start_existing < end_new`

#### 4-2. 보강수업 시간표 충돌

```
쿼리: 보강수업 시간표 DB
- `수강생 == <학생 page_id>` AND `수업요일 == <새 요일>`
- `보강수업 날짜 >= change_start_date`
```

(변경 시작일 이후의 보강 일정만 검사)

#### 4-3. 충돌 시 차단

```
━━━━━━━━━━━━━━━━━━━━━━
❌ 시간 충돌 발견.
━━━━━━━━━━━━━━━━━━━━━━

[충돌 정보]
- 학생:        {학생이름}
- 새 시간:     {새 요일} {새 시작}~{새 종료}
- 기존 시간:   {기존 요일} {기존 시작}~{기존 종료} ({기존 반})
- 종류:        {매주 반복 / 보강}
- 노션 링크:   https://notion.so/<기존 page-id>

💡 한 학생이 같은 시간대에 두 수업을 동시에 받을 수 없습니다.
   다음 중 하나로 처리해주세요:

  1. 다른 시간으로 변경 시도
     → 명령어 다시 호출하고 다른 시간 입력

  2. 기존 시간표 먼저 변경
     → 위 노션 링크에서 기존 시간 수정 후 다시 호출

  3. 기존 시간표 삭제 후 재등록
     → 위 노션 링크에서 기존 row 삭제 후 다시 호출

━━━━━━━━━━━━━━━━━━━━━━
종료 (노션 변경 없음).
```

### 5. 미리보기 (분기별 양식)

#### 5-1. 변경 항목 1·2·3 (요일·시간 변경) 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 시간표 변경 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[변경 정보]
- 범위:        {학원 전체 / {선생님이름} 담당 / {반이름} / {학생이름}}
- 변경 시작:   {YYYY-MM-DD} ({요일})
- 변경 항목:   {요일 / 시간 / 요일+시간}

[옛 → 새]
- 요일:    {옛 요일} → {새 요일}        (변경 시만)
- 시작:    {옛 시작} → {새 시작}        (변경 시만)
- 종료:    {옛 종료} → {새 종료}        (변경 시만)

[영향 row] 총 N건
- {반1} {학생1} {옛 요일/시간} → {새 요일/시간}
- {반1} {학생2} {옛 요일/시간} → {새 요일/시간}
- {반2} {학생3} {옛 요일/시간} → {새 요일/시간}
  ... (10건 초과 시: 처음 10개 + "외 X건")

[자동 처리]
- 수업 시간표 DB row {N}건 patch (요일·시간 변경 + 시간표라벨 갱신)
- 디스코드 parent_webhook으로 선생님 검수 메시지 발송

[제외]
- 학부모 카톡 안내: 운영자가 노션 보고 직접 카톡 발송 (자동화 X)

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

#### 5-2. 변경 항목 4-A (주N회 늘림) 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 시간표 변경 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[변경 정보]
- 범위:        {학생이름} (학생 1명)
- 변경 시작:   {YYYY-MM-DD} ({요일})
- 변경 항목:   주N회 변경 (늘림)
- 옛 → 새:     주{M}회 → 주{N}회 (월 {M*4}회 → 월 {N*4}회)

[추가될 시간표]
- {요일1} {시작1}~{종료1} ({반이름})
- {요일2} {시작2}~{종료2} ({반이름})
  ... ((N-M)개)

[변경 후 전체 시간표]
- {기존 요일1} {기존 시작1}~{기존 종료1}
- {기존 요일2} {기존 시작2}~{기존 종료2}
- {추가 요일1} {추가 시작1}~{추가 종료1}  ← NEW
- {추가 요일2} {추가 시작2}~{추가 종료2}  ← NEW

[청구 영향]
- 이번 달 청구: 기존 월 {M*4}회 유지 (변경 없음)
- 다음 달부터: 월 {N*4}회 (새 횟수)

[자동 처리]
- 수업 시간표 DB row 신규 {N-M}건 생성
- 수강 등록 DB 월간 수업 횟수 patch ({M*4} → {N*4})
- 디스코드 parent_webhook으로 선생님 검수 메시지 발송

[제외]
- 학부모 카톡 안내: 운영자가 노션 보고 직접 카톡 발송 (자동화 X)

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

#### 5-3. 변경 항목 4-B (주N회 줄임) 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 시간표 변경 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[변경 정보]
- 범위:        {학생이름} (학생 1명)
- 변경 시작:   {YYYY-MM-DD} ({요일})
- 변경 항목:   주N회 변경 (줄임)
- 옛 → 새:     주{M}회 → 주{N}회 (월 {M*4}회 → 월 {N*4}회)

[archive 대상 시간표]
- {요일1} {시작1}~{종료1} ({반이름})
  ... ((M-N)개)

[남는 시간표]
- {요일} {시작}~{종료}
  ... (N개)

[청구 영향]
- 이번 달 청구: 기존 월 {M*4}회 유지 (변경 없음)
- 다음 달부터: 월 {N*4}회 (새 횟수)

[자동 처리]
- 수업 시간표 DB row archive {M-N}건
- 수강 등록 DB 월간 수업 횟수 patch ({M*4} → {N*4})
- 디스코드 parent_webhook으로 선생님 검수 메시지 발송

[제외]
- 학부모 카톡 안내: 운영자가 노션 보고 직접 카톡 발송 (자동화 X)

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

부정 또는 "취소" → 즉시 종료, 노션 변경 0.

### 6. 노션 처리

#### 6-1. 변경 항목 1·2·3 (요일·시간 변경) — 각 target_row patch (`API-patch-page`)

```json
{
  "page_id": "<수업 시간표 row page_id>",
  "properties": {
    "수업요일": {"select": {"name": "<새 요일>"}},
    "수업 시작 시간": {"rich_text": [{"text": {"content": "<HH:MM>"}}]},
    "수업 종료 시간": {"rich_text": [{"text": {"content": "<HH:MM>"}}]},
    "시간표라벨": {"title": [{"text": {"content": "{학생이름} - {반이름} ({새 요일}{새 시작시간})"}}]}
  }
}
```

규칙:
- 변경 안 한 항목은 properties에서 omit
- 시간표라벨은 시간 변경 시 항상 함께 갱신

#### 6-2. 일부 실패 처리 (1·2·3 분기)

각 row patch 중 실패하면:
- 그 row 실패 로그 수집
- 나머지 계속 처리
- 결과 보고에서 실패 row 명시

#### 6-3. 변경 항목 4-A (주N회 늘림) — row 신규 생성 + 수강 등록 patch

순서:

**Step 1**: `new_rows` 각각 post-page (수업 시간표 DB)
```json
{
  "parent": {"type": "database_id", "database_id": "<수업 시간표 DB id>"},
  "properties": {
    "시간표라벨": {"title": [{"text": {"content": "{학생이름} - {반이름} ({요일}{시작시간})"}}]},
    "수업요일": {"select": {"name": "<요일>"}},
    "수업 시작 시간": {"rich_text": [{"text": {"content": "<HH:MM>"}}]},
    "수업 종료 시간": {"rich_text": [{"text": {"content": "<HH:MM>"}}]},
    "수업선택": {"relation": [{"id": "<반 page_id>"}]},
    "수강생": {"relation": [{"id": "<학생 page_id>"}]}
  }
}
```

**Step 2**: 수강 등록 row patch (`API-patch-page`)
```json
{
  "page_id": "<수강 등록 row page_id>",
  "properties": {
    "월간 수업 횟수": {"number": <N*4>}
  }
}
```

트랜잭션 일관성 (롤백):
- K번째 신규 row 생성 실패 → 1~K-1번째 신규 row 모두 archive (`archived: true`) 롤백, Step 2 안 진행
- Step 2 실패 → Step 1에서 생성한 모든 row archive 롤백, 결과 보고에 실패 명시

#### 6-4. 변경 항목 4-B (주N회 줄임) — row archive + 수강 등록 patch

순서:

**Step 1**: `archive_rows` 각각 patch (`archived: true`)
```json
{
  "page_id": "<수업 시간표 row page_id>",
  "archived": true
}
```

**Step 2**: 수강 등록 row patch
```json
{
  "page_id": "<수강 등록 row page_id>",
  "properties": {
    "월간 수업 횟수": {"number": <N*4>}
  }
}
```

트랜잭션 일관성 (롤백):
- K번째 archive 실패 → 1~K-1번째 archive된 row 복구 (`archived: false`) 롤백, Step 2 안 진행
- Step 2 실패 → Step 1에서 archive한 모든 row 복구, 결과 보고에 실패 명시

### 7. 디스코드 검수 발송

`config.discord.parent_webhook` 사용.

#### 7-1. 메시지 본문 (분기별)

##### 7-1-A. 변경 항목 1·2·3 (요일·시간 변경) 메시지

```
⚙️ 시간표 변경 알림 (선생님 검수용)

[변경 정보]
- 범위:        {학원 전체 / {선생님이름} 담당 / {반이름} / {학생이름}}
- 변경 시작:   {YYYY년 M월 D일} ({요일})
- 변경 항목:   {요일 / 시간 / 요일+시간}

[옛 → 새]
- 요일:    {옛 요일} → {새 요일}
- 시작:    {옛 시작} → {새 시작}
- 종료:    {옛 종료} → {새 종료}

[영향 학생] 총 N명
- 추득만 (목동초 5학년, 코딩 초등반)
- 김철수 (목동초 6학년, 코딩 초등반)
  ... (10건 초과 시 "외 X명")

[자동 처리 결과]
✅ 수업 시간표 DB row {N}건 patch 완료 (시간표라벨 자동 갱신 포함)
{실패 row 있으면}
⚠️ 실패 {M}건

[다음 작업]
- 학부모 카톡 안내는 운영자가 직접 (노션 보고 카톡 발송)
```

##### 7-1-B. 변경 항목 4-A (주N회 늘림) 메시지

```
⚙️ 시간표 변경 알림 (선생님 검수용)

[변경 정보]
- 범위:        {학생이름} (학생 1명, {학교} {학년})
- 변경 시작:   {YYYY년 M월 D일} ({요일})
- 변경 항목:   주N회 변경 (늘림)
- 옛 → 새:     주{M}회 → 주{N}회 (월 {M*4}회 → 월 {N*4}회)

[추가된 시간표] (신규 row 생성)
- {요일} {시작}~{종료} ({반이름})
  ... ((N-M)개)

[변경 후 전체 시간표]
- {요일} {시작}~{종료}
  ... (N개)

[청구 영향]
- 이번 달 청구: 기존 월 {M*4}회 유지 (변경 없음)
- 다음 달부터: 월 {N*4}회 (새 횟수)

[자동 처리 결과]
✅ 수업 시간표 DB row 신규 {N-M}건 생성
✅ 수강 등록 DB 월간 수업 횟수 patch ({M*4}→{N*4})

[다음 작업]
- 학부모 카톡 안내는 운영자가 직접 (노션 보고 카톡 발송)
```

##### 7-1-C. 변경 항목 4-B (주N회 줄임) 메시지

```
⚙️ 시간표 변경 알림 (선생님 검수용)

[변경 정보]
- 범위:        {학생이름} (학생 1명, {학교} {학년})
- 변경 시작:   {YYYY년 M월 D일} ({요일})
- 변경 항목:   주N회 변경 (줄임)
- 옛 → 새:     주{M}회 → 주{N}회 (월 {M*4}회 → 월 {N*4}회)

[archive된 시간표]
- {요일} {시작}~{종료} ({반이름})
  ... ((M-N)개)

[남은 시간표]
- {요일} {시작}~{종료}
  ... (N개)

[청구 영향]
- 이번 달 청구: 기존 월 {M*4}회 유지 (변경 없음)
- 다음 달부터: 월 {N*4}회 (새 횟수)

[자동 처리 결과]
✅ 수업 시간표 DB row archive {M-N}건
✅ 수강 등록 DB 월간 수업 횟수 patch ({M*4}→{N*4})

[다음 작업]
- 학부모 카톡 안내는 운영자가 직접 (노션 보고 카톡 발송)
```

#### 7-2. 발송 (PowerShell)

```powershell
$webhook = $config.discord.parent_webhook
$username = $config.discord.username
$content = $body
$payload = @{ content = $content; username = $username }
$json = $payload | ConvertTo-Json -Compress -Depth 5
$bytes = [System.Text.Encoding]::UTF8.GetBytes($json)
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.SecurityProtocolType]::Tls12

try {
    Invoke-RestMethod -Uri $webhook -Method Post -Body $bytes -ContentType 'application/json; charset=utf-8' -TimeoutSec 30 -ErrorAction Stop
} catch {
    # 노션 변경은 유지. 실패만 보고
}
```

#### 7-3. 발송 실패 처리

webhook 오류 시:
- 노션 변경은 유지 (롤백 안 함)
- 결과 보고에 발송 실패 명시

### 8. 결과 보고 (분기별)

#### 8-1. 변경 항목 1·2·3 (요일·시간 변경) 결과

```
✅ 시간표 변경 완료

[변경]
- 범위:    {범위 설명}
- 항목:    {요일 / 시간 / 둘 다}
- 시작일:  {YYYY년 M월 D일}

[노션 처리]
- 수업 시간표 row {N}건 patch (시간표라벨 자동 갱신)
{실패 시}
- 실패 {M}건: 노션에서 직접 확인 필요

[디스코드 발송]
{성공}
✅ 선생님 검수 채널 발송 완료
{실패}
❌ 디스코드 발송 실패: <에러>

[다음 작업 추천]
- 학부모 안내: 운영자가 노션 시간표 row 확인 후 카톡으로 직접 안내
  → 영향 학부모: 메시지 본문에 명시된 N명
```

#### 8-2. 변경 항목 4-A (주N회 늘림) 결과

```
✅ 시간표 변경 완료 (주{M}회 → 주{N}회 늘림)

[변경]
- 학생:    {학생이름}
- 반:      {반이름}
- 시작일:  {YYYY년 M월 D일}

[노션 처리]
- 수업 시간표 row 신규 {N-M}건:
  → https://notion.so/<page-id-1>
  → https://notion.so/<page-id-2>
- 수강 등록 월간 수업 횟수: {M*4} → {N*4} patch 완료

[디스코드 발송]
✅ 선생님 검수 채널 발송 완료

[다음 작업 추천]
- 학부모 안내: 운영자가 노션 보고 직접 카톡 발송
- 다음 달 청구부터 새 횟수({N*4}회) 자동 반영
```

#### 8-3. 변경 항목 4-B (주N회 줄임) 결과

```
✅ 시간표 변경 완료 (주{M}회 → 주{N}회 줄임)

[변경]
- 학생:    {학생이름}
- 반:      {반이름}
- 시작일:  {YYYY년 M월 D일}

[노션 처리]
- 수업 시간표 row archive {M-N}건
- 수강 등록 월간 수업 횟수: {M*4} → {N*4} patch 완료

[디스코드 발송]
✅ 선생님 검수 채널 발송 완료

{N == 0인 경우}
⚠️ 주0회 결과: 사실상 수업이 없는 상태입니다.
   /학생-휴원처리 또는 /학생-퇴원처리 권장

[다음 작업 추천]
- 학부모 안내: 운영자가 노션 보고 직접 카톡 발송
- 다음 달 청구부터 새 횟수({N*4}회) 자동 반영
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 변경 범위 1·2·3·4 외 입력 | 재입력 |
| 학원 전체 — 시간표 row 0건 | "/시간표등록 먼저" 종료 |
| 학원 전체 부정 답변 | 즉시 종료 |
| 선생님 — 재직 선생님 0건 | "재직 선생님 없음" 종료 |
| 선생님 — 담당 반·시간표 0건 | "변경 대상 없음" 종료 |
| 특정 반 — 시간표 row 0건 | "이 반에 시간표 없음" 종료 |
| 학생 1명 — 학생 0건 | "/학생-신규등록 먼저" 종료 |
| 학생 1명 + 변경 항목 1·2·3 — 시간표 row 0건 | 2-pre 단계에서 "이 학생 시간표 없음" 종료 |
| 학생 1명 + 변경 항목 1·2·3 — 시간표 row 1건 | 2-pre 단계에서 자동 선택 + 확인 |
| 학생 1명 + 변경 항목 1·2·3 — 시간표 row N개 | 2-pre 단계에서 목록 선택 |
| 학생 1명 + 변경 항목 4 (주N회) | 2-pre 스킵, 학생 전체 시간표 자동 처리 |
| 변경 항목 1·2·3 외 입력 | 재입력 |
| 요일 1~7 외 입력 | 재입력 |
| 시간 형식 오류 | 재입력 |
| 시작 ≥ 종료 | 재입력 |
| 변경 시작일 형식 오류 | 재입력 |
| 변경 시작일 과거 | 경고 + 확인 |
| 변경 시작일 너무 먼 미래 (6개월+) | 경고 + 확인 |
| 시간 충돌 (수업 시간표 or 보강) | 차단 + 3가지 가이드 |
| 미리보기 부정 | 즉시 종료, 노션 변경 0 |
| 일부 row patch 실패 | 그 row 실패 보고, 나머지 계속 |
| 디스코드 발송 실패 | 노션 유지, 발송 실패만 안내 |
| 어느 단계든 "취소"·"q" | 즉시 종료 |
| 변경 항목 4 선택했는데 범위가 1·2·3 | 4번 항목 자체를 노출하지 않음 (학생 1명 분기에서만 표시) |
| 4-A·4-B 분기 — 수강 등록 row 0건 | "/수강등록 먼저" 종료 |
| 4 분기 — 새 주N회 == 현재 주M회 | "현재와 같음. 1·2·3 분기 사용 권장" 종료 |
| 4-A 분기 — K번째 추가 시간표 충돌 (수업 or 보강) | 차단 + 3가지 가이드 (노션 변경 전) |
| 4-A 분기 — K번째 row 생성 실패 | 1~K-1 신규 row archive 롤백, 수강 등록 patch 안 함 |
| 4-A 분기 — 수강 등록 patch 실패 | 신규 row 모두 archive 롤백 |
| 4-B 분기 — 순회 후 archive 대상 부족 | "다시 선택 / 종료" 분기 |
| 4-B 분기 — K번째 archive 실패 | 1~K-1 archive된 row 복구 롤백 |
| 4-B 분기 — 수강 등록 patch 실패 | archive된 row 모두 복구 롤백 |
| 4-B 분기 — N == 0 (주0회) | 정상 진행, 결과 보고에 "/학생-휴원처리 권장" 안내 |

## 주의사항

- **확인 없는 자동 처리 금지**: 미리보기 후 명시적 긍정 응답 없이 노션 patch 금지
- **반 변경 처리 X**: 다른 반으로 이동은 운영자 수동. 옛 수강 등록 종료(수강종료일 입력) + /수강등록 재호출
- **학부모 발송 X**: 디스코드 검수만. 학부모 안내는 운영자가 노션 보고 카톡으로 직접 (자동화 안 함)
- **시간표라벨 자동 갱신**: 시간 변경 시 title도 일괄 patch. row 정렬·검색 일관성
- **일괄 적용**: 학원 전체·선생님·반 케이스는 모든 row에 같은 변경. 개별 다른 변경은 학생 1명씩 호출
- **시간 충돌은 다른 시간표와도 차단**: 같은 학생이 같은 시간 두 수업 받을 수 없음
- **session_save_webhook 절대 사용 금지**: parent_webhook 만 사용
- **개인정보 보호**: 메시지에 학부모 전화번호 포함 금지
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (메모리 `feedback_yes_no_korean`)
- **터미널 default**: 엔터 사용 안 함. 번호 선택 또는 키워드만 (메모리 `feedback_terminal_default_input`)
- **횟수 변경은 학생 1명 분기 한정**: 학원 전체·선생님·반 범위에서는 4번 항목 노출 X. 반 단위 일괄 변경 필요 시 학생별 반복 호출
- **수강 등록 patch 누락 금지**: 횟수 변경 시 시간표 row와 수강 등록 DB는 반드시 동시 patch. 한쪽만 변경되면 청구 누락 위험
- **청구는 다음 달부터**: 당월 청구는 기존 횟수 유지, 다음 달 청구부터 새 횟수 적용. `/결제선생`이 매월 현재 수강 등록 보고 청구하므로 즉시 patch해도 자연스럽게 반영

## 예시 1: 학생 1명 시간 변경

**입력**
- 변경 범위: 4 (학생 1명)
- 학생: 추득만
- 시간표 row: 월 16:00~17:30 (코딩 초등반)
- 변경 항목: 3 (요일+시간)
- 새 요일: 수
- 새 시간: 17:00~18:30
- 변경 시작일: "다음주" → 2026-05-19

**노션 결과**
- 추득만의 시간표 row 1건 patch:
  - 수업요일: 월 → 수
  - 시작 시간: 16:00 → 17:00
  - 종료 시간: 17:30 → 18:30
  - 시간표라벨: "추득만 - 코딩 초등반 (월16:00)" → "추득만 - 코딩 초등반 (수17:00)"
- 디스코드 검수 발송: 영향 학생 1명 (추득만)

## 예시 2: 특정 반 시간 변경

**입력**
- 변경 범위: 3 (특정 반)
- 반: 파이썬 초등 A
- 변경 항목: 2 (시간만)
- 새 시간: 16:30~18:00 (기존 16:00~17:30)
- 변경 시작일: 2026-06-01

**노션 결과**
- 파이썬 초등 A 반의 모든 시간표 row patch (예: 3명 row → 3건)
- 디스코드 검수: 영향 학생 3명 명단 + 옛/새 시간

## 예시 3: 충돌 차단

**입력**
- 추득만의 시간표 row: 월 16:00 → 화 16:00로 변경
- 충돌: 추득만이 이미 화 16:00~17:00 보강 일정 있음

**기대**: 차단 메시지 + 3가지 행동 가이드. 노션 변경 0.

## 예시 4: 주N회 늘림 (주1회 → 주2회)

**입력**
- 변경 범위: 4 (학생 1명)
- 학생: 추득만
- 변경 항목: 4 (주N회 변경)
- 현재 주1회 (월 16:00~17:30)
- 새 주2회 → 추가 1건 입력: 수 16:00~17:30
- 변경 시작일: "다음주" → 2026-05-19

**노션 결과**
- 수업 시간표 DB row 신규 1건: 수 16:00~17:30 (코딩 초등반)
- 수강 등록 DB 월간 수업 횟수: 4 → 8 patch
- 디스코드 검수 발송 (옛/새 시간표 + 청구 영향 명시)

## 예시 5: 주N회 줄임 (주2회 → 주1회)

**입력**
- 변경 범위: 4 (학생 1명)
- 학생: 추득만
- 변경 항목: 4 (주N회 변경)
- 현재 주2회 (월 16:00~17:30, 수 16:00~17:30)
- 새 주1회 → archive 1건 선택:
  - [1/2] 월 16:00~17:30 archive할까요? → 아니오
  - [2/2] 수 16:00~17:30 archive할까요? → 네
- 변경 시작일: 2026-06-01

**노션 결과**
- 수업 시간표 DB row archive 1건: 수 16:00~17:30
- 수강 등록 DB 월간 수업 횟수: 8 → 4 patch
- 디스코드 검수 발송 (archive 대상 + 남는 시간표 + 청구 영향 명시)

## 예시 6: 주N회 변경 — 같은 횟수 (차단)

**입력**
- 변경 범위: 4 (학생 1명)
- 학생: 추득만
- 변경 항목: 4 (주N회 변경)
- 현재 주2회 → 새 주2회 입력

**기대**: "현재와 같음. 요일/시간 변경은 1·2·3 분기 사용 권장" 종료. 노션 변경 0.
