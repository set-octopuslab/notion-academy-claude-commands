---
description: 학부모에게 수강료 안내 메시지를 디스코드로 발송 (수동 트리거, 어디서든 실행 가능)
---

# 결제선생 (v8 — 휴강 사유 자동 노출)

원장이 `/결제선생` 을 입력하면, 지정한 대상월(기본=다음달)의 학생별 수강료를 계산해 **미리보기를 출력**하고, **원장의 한 번 확인(y/n)**을 받은 뒤 **노션 월간 수강료 헤더 + 수강료 결제내역 라인**을 생성하고 **디스코드로 학부모 메시지를 발송**한다.

집/학원/서버 어디서든 실행 가능 (G드라이브 마운트만 되어 있으면 됨).

## v7 → v8 변경

- 휴강일 사유를 메시지에 자동 합성: 학생의 수업 요일에 걸린 휴강이 있으면 "5월 5일 어린이날 휴강" 같이 표기
- 캘린더 `일정명`이 비어있으면 "휴강"으로만 표기 (학원·선생 사정 휴강은 사유 비공개)
- 학생 개인 결석은 사유 표기 없이 일자만 (현행 유지) — 학부모가 이미 알고 있고 민감 정보 노출 방지

## v6 → v7 변경

- 기본 동작 변경: 인자 없을 때 = **미리보기 → y/n 확인 → 발송** (v6는 dryrun이 기본이었음)
- 사일런트 dryrun이 필요한 경우만 `--dryrun` 명시
- `--send` 옵션 폐지 (확인 단계가 항상 들어가므로 불필요)
- `--force` 옵션 신설: 미리보기 후 확인 단계 생략하고 즉시 발송 (자동화/배치용, 대화형 환경에서는 비권장)

## 실행 모드 (`$ARGUMENTS` 파싱)

- (인자 없음) → 다음달 기준 **미리보기 → 확인(y/n) → 발송**
- `--dryrun` → 미리보기만 출력하고 즉시 종료 (확인·발송 단계 없음)
- `--force` → 미리보기 출력 후 확인 단계 생략하고 즉시 발송
- `5월` / `2026-05` → 대상월 명시 (없으면 다음달)
- 조합 가능: `/결제선생 6월 --dryrun`

> 위험한 의미를 가진 옵션은 명시적으로 받음. 대상월 추론 실패 시 즉시 중단하고 사용자에게 확인.

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-retrieve-a-data-source,mcp__notionApi__API-query-data-source,mcp__notionApi__API-post-page,mcp__notionApi__API-patch-page,mcp__notionApi__API-retrieve-a-page-property
   ```
2. config 읽기: `G:/내 드라이브/ClaudeSetting/notion-academy-config.json`
   - `notion.data_sources` 12개 ID 확인 (없으면 즉시 중단 + `/노션-초기설정` 안내)
   - `discord.parent_webhook` 확인 (실제 발송 모드일 때만 필수)
   - `tuition.patterns` 4종 확인
3. G드라이브 마운트 확인 (`G:/` 접근 가능 여부)

## 수강료 패턴 자동 판별

학생 DB의 `초,중,고` + 정규수업 시간표 DB에서 해당 학생이 `수강생` 으로 등록된 row 개수 (특강 제외)로 판별:

| 초,중,고 | 시간표 수 | 패턴 | 월수강료 | 월기준 | 회당단가 | 할인버퍼 |
|---|---|---|---|---|---|---|
| 초 | 1 | 초주1 | 200,000 | 4회 | 50,000 | 0 |
| 초 | 2+ | 초주2 | 350,000 | 8회 | 50,000 | 1 |
| 중·고·대 | 1 | 중고주1 | 250,000 | 4회 | 62,500 | 0 |
| 중·고·대 | 2+ | 중고주2 | 400,000 | 8회 | 50,000 | 0 |

**제외 조건:**
- 정규수업 시간표 0개
- 학생의 `수강중인 클래스` 이름 또는 시간표의 `수업선택` 클래스 이름에 "특강" 포함 (특강 시간표만 제외, 정규는 유지)
- `등록현황 ≠ 등록`

패턴 값은 `config.tuition.patterns` 에서 읽음.

## 호칭 결정

학생 DB `학부모` relation → 학부모 DB row들 조회.

**우선순위**:
1. `주 연락처 = true`인 학부모 → `관계` 값 → `어머니`→`어머님`, `아버지`→`아버님`, `조모`→`할머님`, `조부`→`할아버님`
2. `주 연락처 = true`가 없으면 `관계 = 어머니`인 학부모 → `어머님`
3. 그것도 없으면 `관계 = 아버지` → `아버님`
4. 모두 없거나 학부모 relation 비어있으면 `학부모님` fallback

## 차감 공식

```
월 버퍼        = max(0, 월 예정 − 월 기준) + 할인 버퍼
차감 횟수      = max(0, 결석 수 − 월 버퍼)
차감액         = 차감 횟수 × 회당 단가
차감 후 납부액  = 월 수강료 − 차감액
최종 안내액    = floor(차감 후 납부액 / 10000) × 10000   ※ 천원 미만 절삭
```

- 결석 대상: 정확히 `출결 = 결석`만. 지각·조퇴 제외
- 보강수업은 월 예정 횟수에 더하지 않음
- 결석 집계 대상 월 = 대상월의 직전 달 (예: 5월 수강료 발송 시 4월 결석 집계)

## 절차

### 1. 대상월 결정
- 인자에 `5월`/`2026-05`/`05` 같은 형식 있으면 그것
- 없으면 = (오늘 + 1달의 1일이 속한 달). 즉 4월에 실행하면 5월
- YYYY-MM 형식으로 정규화

### 2. 학원 캘린더에서 휴강일 자동 수집
- 쿼리 조건: `유형 = 수업없음` AND `일자 in [대상월 1일 ~ 대상월 말일]`
- 반환: 수업없음 일자 + 각 일정의 `관련 클래스` + `일정명` (사유)
  - `관련 클래스` 비어있음 → 모든 학생 일괄 차감 (전체 휴강)
  - `관련 클래스` 있음 → 연결된 클래스를 수강하는 학생만 차감
  - `일정명` 있음 → 메시지 합성 시 사유로 노출 (예: "어린이날", "부처님오신날")
  - `일정명` 비어있음 → "휴강"으로만 표기 (학원·선생 사정 비공개)
- 휴강일 없으면 빈 리스트로 진행

### 3. 학생 목록 수집
- 학생 DB `등록현황 == 등록` 필터
- 각 학생: page_id, 이름, `초,중,고`, `학부모` relation ids

### 4. 학생별 계산 루프

**4-1. 호칭 결정** (위 호칭 결정 섹션)

**4-2. 정규 시간표 조회**
- 정규수업 시간표 DB 쿼리: `수강생 contains <학생 page_id>`
- 각 시간표 → `수업선택` → 클래스관리 `클래스이름` 조회
- 클래스이름에 "특강" 포함 시 제외
- 남은 정규 시간표 수 N
- N == 0 → 학생 스킵

**4-3. 패턴 결정** (위 표)

**4-4. 월 예정 횟수**
- 각 시간표의 `수업요일` → 대상월 해당 요일 날짜들 계산
- 학원 캘린더의 휴강일과 매칭하여 차감
- 합산

**4-5. 결석 집계**
- 개인 수업결과 DB: `학생 직접` relation = <학생 page_id> AND `출결 = 결석` AND `수업일자 in [직전달 1일~말일]`
- 결석 row 수 + 결석일 리스트

**4-6. 차감 계산** (위 공식)

### 5. 메시지 포맷팅

**반 표기명** (패턴별 고정):
- 초주1 → "초등반 주 1회 수업"
- 초주2 → "초등반 주 2회 수업"
- 중고주1 → "중고등반 주 1회 수업"
- 중고주2 → "중고등반 주 2회 수업"

**금액 표기**: 만원 단위로 떨어지면 `N만원`, 아니면 `N,NNN원`

**날짜 나열**: 대상월 해당 요일 날짜들을 `N일, N일, ...` 형식. 휴강일 제외.

**월별 마무리 문구**: `config.month_closing[대상월]` 사용

**휴강 안내 합성 (v8 신규)**:
- 학생의 수업 요일에 걸린 휴강만 추려서 표기
- 일정명 있음 → `"5월 5일 어린이날"` 형태
- 일정명 없음 → `"5월 5일"` (사유 생략)
- 복수 휴강 → 콤마 결합 + 마지막에 `" 휴강으로"` 추가
  - 예 1건: `"5월 24일 부처님오신날 휴강으로"`
  - 예 1건(사유 없음): `"5월 24일 휴강으로"`
  - 예 2건: `"5월 5일 어린이날, 5월 24일 부처님오신날 휴강으로"`
  - 예 2건(혼합): `"5월 5일 어린이날, 5월 24일 휴강으로"`
- 학생의 요일에 걸린 휴강 0건이면 휴강 안내 문구 자체 생략

**메시지 템플릿 (차감 없음, 휴강 없음):**
```
━━━━━━━━━━━━━━━
안녕하세요 {학생이름} {호칭}^^
{대상월}월 수강료 안내드리고자 합니다.

이번달에는 {수업일_나열} 총 {총횟수}회 수업 예정이며 {반표기} 수강료는 {원금액}입니다.

{월_마무리_문구}

감사합니다.
(수강료 안내가 잘못되었을 경우 문자 또는 전화 부탁드리겠습니다.)
```

**메시지 템플릿 (차감 없음, 휴강 있음):**
```
━━━━━━━━━━━━━━━
안녕하세요 {학생이름} {호칭}^^
{대상월}월 수강료 안내드리고자 합니다.

이번달에는 {휴강안내} {수업일_나열} 총 {총횟수}회 수업 예정이며 {반표기} 수강료는 {원금액}입니다.

{월_마무리_문구}

감사합니다.
(수강료 안내가 잘못되었을 경우 문자 또는 전화 부탁드리겠습니다.)
```

**메시지 템플릿 (차감 있음, 휴강 없음):**
```
━━━━━━━━━━━━━━━
안녕하세요 {학생이름} {호칭}^^
{대상월}월 수강료 안내드리고자 합니다.

이번달에는 {수업일_나열} 총 {총횟수}회 수업 예정입니다.
{반표기} 수강료는 {원금액}이며 {결석월}월 {결석일_나열} 결강한 {차감횟수}회 수업 수강료 {차감액} 차감하여 {대상월}월 수강료는 {최종안내액} 안내 드립니다.
(천원단위는 절삭하여 안내 드립니다.)

{월_마무리_문구}

감사합니다.
(수강료 안내가 잘못되었을 경우 문자 또는 전화 부탁드리겠습니다.)
```

**메시지 템플릿 (차감 있음, 휴강 있음):**
```
━━━━━━━━━━━━━━━
안녕하세요 {학생이름} {호칭}^^
{대상월}월 수강료 안내드리고자 합니다.

이번달에는 {휴강안내} {수업일_나열} 총 {총횟수}회 수업 예정입니다.
{반표기} 수강료는 {원금액}이며 {결석월}월 {결석일_나열} 결강한 {차감횟수}회 수업 수강료 {차감액} 차감하여 {대상월}월 수강료는 {최종안내액} 안내 드립니다.
(천원단위는 절삭하여 안내 드립니다.)

{월_마무리_문구}

감사합니다.
(수강료 안내가 잘못되었을 경우 문자 또는 전화 부탁드리겠습니다.)
```

### 6. 학년 그룹화 + 미리보기

학생들을 `초,중,고` 값으로 3그룹 분류:
- `초` → **초등반** (🧒)
- `중` / `고` → **중고등반** (🎒)
- `대` → **대학생반** (🎓)

각 그룹마다 헤더 메시지 먼저, 그 다음 학생 메시지들:
```
═══════════════════
🧒 초등반 (N명)
═══════════════════
```
빈 그룹은 헤더 생략.

**`--dryrun` 모드** → 콘솔에 미리보기 출력 후 종료. 노션·디스코드 변화 없음.

**기본 모드 (인자 없음)** → 미리보기 출력 → 사용자에게 "발송 진행할까요? (y/n)" 확인 → y면 7~9단계 실행

**`--force` 모드** → 미리보기 출력 후 확인 단계 생략하고 즉시 7~9단계 실행 (자동화 용도)

### 7. 노션 DB row 생성 (발송 단계에서만)

**7-1. 월간 수강료 헤더 row 생성 (또는 조회)**

중복 체크:
- 쿼리: `학생 = <page_id>` AND `수강료 년/월 = <YYYY-MM>`
- 있으면 **덮어쓰기/건너뛰기/수정** 사용자에게 선택받음

신규 생성:
```json
{
  "parent": {"type": "data_source_id", "data_source_id": "<monthly_tuition>"},
  "properties": {
    "청구라벨": {"title": [{"type": "text", "text": {"content": "<학생이름> <YYYY-MM>"}}]},
    "학생": {"relation": [{"id": "<학생 page_id>"}]},
    "수강료 년/월": {"rich_text": [{"type": "text", "text": {"content": "<YYYY-MM>"}}]},
    "매월 결제해야하는날짜": {"number": <config.tuition._매월_결제기준일>},
    "이번달 결제일": {"date": {"start": "<대상월 1일>"}}
  }
}
```

반환된 `page_id` → 라인 헤더 참조로 사용.

**7-2. 수강료 결제내역 라인 row 생성**

```json
{
  "parent": {"type": "data_source_id", "data_source_id": "<tuition_lines>"},
  "properties": {
    "라인라벨": {"title": [{"type": "text", "text": {"content": "<학생이름> <대상월> <반표기>"}}]},
    "월간 수강료": {"relation": [{"id": "<7-1 헤더 page_id>"}]},
    "대상월": {"date": {"start": "<대상월 1일>"}},
    "단가": {"number": <패턴.월수강료>},
    "회당 단가": {"number": <패턴.회당단가>},
    "수업 예정 횟수": {"number": <월 예정 횟수>},
    "결석 횟수": {"number": <결석 수>},
    "할인 버퍼": {"number": <패턴.할인버퍼 + max(0, 월예정 - 월기준)>},
    "할인 사유": {"select": {"name": "결석차감" or "없음"}},
    "비고": {"rich_text": [{"type": "text", "text": {"content": "<패턴명> / <결석월>월 결석일: <일자들>"}}]}
  }
}
```

라인의 `차감 횟수`·`결석 할인금액`·`최종 금액`은 formula로 자동 계산.

**7-3. 반(클래스관리) relation 연결 (선택)**

- 학생의 정규 시간표 중 첫 번째 → `수업선택` → 클래스관리 row ID
- 라인의 `반` relation에 연결 (없으면 생략)

**7-4. 천원 절삭은 어디서?**

- 라인 formula는 원 단위 정확 계산 (절삭 없음)
- 메시지의 "최종 안내액"은 스크립트가 별도로 `floor(최종금액/10000)*10000` 계산해 디스코드 메시지에만 반영
- 노션 저장값 ≠ 안내값 (의도된 것)

### 8. 디스코드 발송 (발송 단계에서만)

**반드시 `config.discord.parent_webhook` 사용** (session_save_webhook 절대 사용 금지).

한글 escaping 안전을 위해 JSON 파일 경유:
```bash
echo '{"content":"...","username":"결제선생"}' > /tmp/msg.json
curl -s -H "Content-Type: application/json" \
     -H "User-Agent: Mozilla/5.0 (academy-payment-bot)" \
     -X POST --data-binary @/tmp/msg.json "<parent_webhook>"
```

Python으로 발송 시 `User-Agent` 헤더 필수 (Discord가 기본 urllib UA 403 반환).
메시지 사이 0.8초 대기. 실패 시 해당 학생만 로그.

### 9. 완료 보고

- 처리/실패/스킵 건수, 노션 헤더 row + 라인 row 생성 수, 디스코드 발송 수 요약

## 주의사항

- **재실행 안전성**: 같은 대상월 헤더 row 중복 체크 필수
- **특강반 학생**: 스코프 밖. `/특강-수강료` 별도 커맨드(미구현) 또는 원장 수동 처리
- **한 학생이 여러 반**: 학생 DB `수강중인 클래스`가 single relation이라 구조상 1개만. 정규 시간표 여러 개면 첫 번째를 대표 반으로 라인에 연결
- **학부모 전화번호**: 디스코드 메시지에 포함 금지 (채널에 개인정보 쌓이지 않게)
- **Config·webhook URL**: 응답에 노출 금지
- **확인 없이 발송 금지**: `--force` 옵션이 명시되지 않은 경우, 미리보기 출력 후 사용자의 명시적 y 응답 없이 노션·디스코드에 절대 쓰기/발송 금지

## 예시

초등 주1회 (토요일반, 5월 5회 예정, 5/5·5/24·5/25 휴강이지만 토요일과 무관, 결석 없음):
```
━━━━━━━━━━━━━━━
안녕하세요 김레오 어머님^^
5월 수강료 안내드리고자 합니다.

이번달에는 2일, 9일, 16일, 23일, 30일 총 5회 수업 예정이며 초등반 주 1회 수업 수강료는 20만원입니다.

가정의 달 5월, 늘 가정의 행복이 가득하시길 바랍니다. 이번달도 즐겁게 배우며 성장해 나갈 수 있도록 함께하겠습니다.

감사합니다.
(수강료 안내가 잘못되었을 경우 문자 또는 전화 부탁드리겠습니다.)
```

중고 주1회 (월요일반, 5월 5/5·5/25 휴강으로 2회 차감, 4월 11·18일 결석 2회):
```
━━━━━━━━━━━━━━━
안녕하세요 홍길동 어머님^^
5월 수강료 안내드리고자 합니다.

이번달에는 11일, 18일 총 2회 수업 예정입니다.
중고등반 주 1회 수업 수강료는 25만원이며 4월 11일, 18일 결강한 2회 수업 수강료 125,000원 차감하여 5월 수강료는 12만원 안내 드립니다.
(천원단위는 절삭하여 안내 드립니다.)

가정의 달 5월, 늘 가정의 행복이 가득하시길 바랍니다. 이번달도 즐겁게 배우며 성장해 나갈 수 있도록 함께하겠습니다.

감사합니다.
(수강료 안내가 잘못되었을 경우 문자 또는 전화 부탁드리겠습니다.)
```

초등 주1회 (일요일반, 5월 5/24 부처님오신날 휴강, 결석 없음) — **v8 휴강 안내 예시**:
```
━━━━━━━━━━━━━━━
안녕하세요 강명찬 어머님^^
5월 수강료 안내드리고자 합니다.

이번달에는 5월 24일 부처님오신날 휴강으로 3일, 10일, 17일, 31일 총 4회 수업 예정이며 초등반 주 1회 수업 수강료는 20만원입니다.

가정의 달 5월, 늘 가정의 행복이 가득하시길 바랍니다. 이번달도 즐겁게 배우며 성장해 나갈 수 있도록 함께하겠습니다.

감사합니다.
(수강료 안내가 잘못되었을 경우 문자 또는 전화 부탁드리겠습니다.)
```
