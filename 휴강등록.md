---
description: 학원 캘린더에 휴강 row 추가 + 영향 학생 일일피드백 row 자동 생성(출결=휴강) + 디스코드 선생님 검수 발송
---

# 휴강등록

원장이 `/휴강등록` 을 입력하면, **휴강 범위(학원 전체/일부 반) → 기간(단일/기간) → 일정명·비고 → 중복 검사 → 영향 학생 추출 → 미리보기 → 노션 처리 → 디스코드 검수 발송** 순서로 처리한다.

**디스코드 발송 = 선생님 내부 검수용** (학원 단체 채널, 학부모 X). 학부모 카톡 발송은 별도 `/휴강카톡` (셋업 후).

> ⚠️ **Claude 동작 가드 (반드시 준수)**
>
> 1. **AskUserQuestion 도구 사용 금지**. 모든 질문은 일반 텍스트 출력 + 자유 답변 받기.
> 2. **자의적 부연 설명 금지**. .md에 정의되지 않은 안내문 노출 금지.
> 3. **session_save_webhook 절대 사용 금지**. `discord.parent_webhook`만 사용.
> 4. **메시지에 학부모 전화번호 포함 금지** (개인정보 보호).
> 5. **학부모 카톡 발송 절대 시도 금지**. 이 명령어는 디스코드 선생님 검수만. 학부모 발송은 `/휴강카톡` 별도.
> 6. **🚨 일정명 필드 절대 누락 금지**. 학원 캘린더 post-page 시 `properties.일정명` (title) 필드를 반드시 포함. 사용자가 입력한 휴강 사유 텍스트가 그대로 들어가야 함. 빈 title로 post하면 노션이 날짜를 자동 표시해서 휴강 사유가 보이지 않게 됨.
> 7. **관련 클래스 항상 명시적 link**. 학원 전체 휴강이든 일부 반이든 항상 `관련 클래스` 필드에 반들을 link. omit 금지.
> 8. **비고 빈 칸 금지**. 휴강 사유 텍스트가 반드시 들어가야 함. 사용자가 "동일" 입력 시 일정명 텍스트 자동 복사.

## 범위

**자동 처리**
- 학원 캘린더 DB에 휴강 row 1건 추가 (유형="수업없음")
- 영향 학생들의 일일피드백 row 자동 생성/patch (출결="휴강")
- 디스코드 parent_webhook 메시지 발송 (선생님 검수)

**제외 (별도 명령어/사후 처리)**
- 학부모 카톡 안내 — `/휴강카톡`
- 보강 일정 — `/보강시간표등록`
- 휴강 취소·수정 — 노션에서 직접

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-post-page,mcp__notionApi__API-patch-page
   ```
2. config 읽기: `G:/내 드라이브/ClaudeSetting/notion-academy-config.json`
   - `notion.data_sources.academy_calendar`
   - `notion.data_sources.classes`
   - `notion.data_sources.regular_schedule` (수업 시간표)
   - `notion.data_sources.makeup_schedule` (보강수업 시간표)
   - `notion.data_sources.lesson_results` (일일피드백)
   - `notion.data_sources.lesson_plans` (수업계획·수업요약)
   - `notion.data_sources.students`
   - `discord.parent_webhook`
   - `discord.username`

## 절차

### 1. 휴강 범위 분기 (3가지)

```
[1/?] 어떤 휴강인가요?
  1. 학원 전체 휴강 (공휴일·연휴 — 모든 반 자동 link)
  2. 특정 선생님 휴가 (선생님 1명 선택 — 그 선생님 담당 반 자동 link)
  3. 특정 반만 휴강 (반 직접 선택)
번호 선택:
```

분기:
- "1" → `scope = "academy"` → 1-A로 (모든 반 자동 수집)
- "2" → `scope = "teacher"` → 1-B로 (선생님 선택)
- "3" → `scope = "classes"` → 1-C로 (반 직접 선택)
- 그 외 → 재입력

#### 1-A. 학원 전체 (scope == "academy")

쿼리: 클래스관리 DB 전체 → 모든 반 page_id 수집

```
[?/?] 학원 전체 휴강 — 클래스관리 DB의 모든 반(N개)에 link됩니다.
  자동 추출된 반: 파이썬 초등 A, C언어 중등 B, 정보올림피아드 특강반, ...
진행하시겠어요? (네/아니오)
```

부정 → 종료. 긍정 → `target_classes` = 모든 반.

#### 1-B. 선생님 휴가 (scope == "teacher")

쿼리: 선생님 DB
- `재직상태 == "재직"`

```
[?/?] 어느 선생님이 휴가인가요?
  1. 이승환 (재직, 수업과목: 파이썬·코딩)
  2. 김동환 (재직, 수업과목: 코딩)
  3. 문성준 (재직, 수업과목: 코딩)
번호 선택:
```

- 0건 → "재직 선생님 없음" 종료
- 1건+ → 사용자가 1명 선택 → `target_teacher`

그 후 클래스관리 DB query:
- `선생님 == target_teacher`

→ `target_classes` = 그 선생님 담당 반들

```
✅ {선생님이름} 선생님 담당 반 N개:
  - 파이썬 초등 A
  - C언어 중등 B
  ...
진행하시겠어요? (네/아니오)
```

- 담당 반 0건 → "이 선생님의 담당 반이 없습니다" 종료
- 담당 반 1건+ → `target_classes`

#### 1-C. 특정 반 (scope == "classes")

쿼리: 클래스관리 DB (전체)

```
[?/?] 어느 반(들)이 휴강하나요? (복수 선택 가능, 쉼표로 구분)
  1. 파이썬 초등 A (담당: 김선생)
  2. C언어 중등 B (담당: 이선생)
  3. 정보올림피아드 특강반 (담당: 박선생)
  ...
번호 선택 (예: 1 또는 1,3):
```

- 0건 → "/클래스등록 먼저" 안내 종료
- 1건+ → 사용자 입력 파싱 → `target_classes`

### 2. 휴강 기간 분기

```
[2/?] 휴강 기간?
  1. 단일 날짜 (1일 휴강)
  2. 여러 날 (연휴·방학)
번호 선택:
```

#### 2-1. 단일 (선택지 1)

```
[?/?] 휴강 날짜를 입력해주세요. (YYYY-MM-DD)

> 
```

`target_date_start = target_date_end = <입력>`.

#### 2-2. 기간 (선택지 2)

```
[?/?] 시작 날짜? (YYYY-MM-DD)
> 

[?/?] 종료 날짜? (YYYY-MM-DD)
> 
```

검증:
- 형식 오류 → 재입력
- 종료 < 시작 → "종료가 시작보다 빠릅니다" 재입력
- 너무 먼 미래/과거 (오늘 ±1년 이상) → 경고 + 확인

### 3. 일정명·비고 입력

#### 3-1. 일정명

```
[?/?] 휴강 사유(일정명)를 입력해주세요.
       기간 휴강은 모든 날짜의 사유를 한 줄에 모두 포함해주세요.

       예 단일:  "어린이날", "선생님 휴가"
       예 기간:  "부처님오신날, 임시공휴일" (5/24~5/25 동시)
       예 연휴:  "추석 연휴" (단일 명칭으로 묶기)
       예 방학:  "여름방학"

> 
```

빈 칸 → 재요청.

#### 3-2. 비고 (번호 선택형)

```
[?/?] 비고는 어떻게 입력할까요?
       (일정명: {일정명})

  1. 일정명과 동일 (자동 복사)
  2. 직접 입력 (추가 사유·설명)

번호 선택:
```

분기:
- "1" → `비고 = 일정명` (자동 복사)
- "2" → 직접 입력 단계 (3-2-1)
- 그 외 → "1 또는 2 입력해주세요" 재요청

#### 3-2-1. 직접 입력 (선택지 2)

```
[?/?] 비고를 입력해주세요. (기간 전체에 대한 사유·설명)
       예: "정부 지정 공휴일", "선생님 가족 여행", "학원 외부 행사 참석"
       예 기간 상세: "5/24 부처님오신날, 5/25 임시공휴일"

> 
```

빈 칸 → 재요청 (휴강 사유 필수).

> ⚠️ 비고는 절대 빈 칸으로 저장 금지. 일정명 자동 복사(선택지 1) 또는 직접 입력(선택지 2) 중 반드시 채워짐.

### 4. 같은 날짜 중복 차단

쿼리: 학원 캘린더 DB
- `유형 == "수업없음"` AND 일자가 `target_date_start ~ target_date_end` 와 겹치는 row 검색

겹침 판정: `기존 start <= target_end AND target_start <= 기존 end`

#### 4-1. 0건 → 다음 단계 (5)

#### 4-2. 1건+ → 차단

```
━━━━━━━━━━━━━━━━━━━━━━
❌ 해당 기간에 이미 휴강 row가 있습니다.
━━━━━━━━━━━━━━━━━━━━━━

📍 기존 휴강 정보
   - 일정명:  {기존 일정명}
   - 일자:    {기존 시작}{ ~ 기존 종료 if 기간}
   - 유형:    수업없음
   - 비고:    {기존 비고 또는 "(없음)"}
   - 노션:    https://notion.so/<기존 page-id>

💡 다음 중 하나로 처리해주세요:

  1. 다른 날짜로 등록
     → 명령어 다시 호출하고 다른 날짜 입력

  2. 기존 row 수정 (일정명·기간·관련 클래스 등 변경 필요 시)
     → 위 노션 링크 클릭 → 기존 row 직접 수정
     → 그 후 /휴강등록 다시 호출 (다른 휴강 추가 필요 시)

  3. 기존 row 삭제 (잘못 등록 시)
     → 위 노션 링크 클릭 → 페이지 우상단 메뉴 → 'Delete'
     → 그 후 /휴강등록 다시 호출

━━━━━━━━━━━━━━━━━━━━━━
종료 (노션 변경 없음).
```

### 5. 영향 학생 추출

#### 5-1. 학원 전체 (scope == "academy")

쿼리 1: 수업 시간표 DB (매주 반복)
- 휴강 기간의 각 날짜 요일 ∈ `수업요일`
- 단일 휴강이면 하나의 요일, 기간이면 기간 안의 모든 요일

쿼리 2: 보강수업 시간표 DB
- `보강수업 날짜` ∈ `target_date_start ~ target_date_end`

각 row의 (수강생, 수업선택, 그 row의 수업 날짜) 추출.
- 정규수업이면 "그 row의 수업 날짜" = 휴강 기간 중 그 요일에 해당하는 날짜
- 보강수업이면 "그 row의 수업 날짜" = `보강수업 날짜` 그대로

#### 5-2. 일부 반 (scope == "classes")

쿼리 1: 수업 시간표 DB
- 위 정규 쿼리 + `수업선택 in target_classes`

쿼리 2: 보강수업 시간표 DB
- 위 보강 쿼리 + `수업선택 in target_classes`

#### 5-3. 결과 분류

`affected_students = [(학생 page_id, 수업 날짜, 반 page_id, 수업타입="정규수업"/"보강수업")]`

N = `affected_students` 길이.

#### 5-4. N == 0 시 처리

```
⚠️ 해당 기간에 수업이 있는 학생이 0명입니다 (평소 비수업일).

학원 캘린더에 휴강 row만 기록할까요?
  1. 네, 캘린더 기록만 (일일피드백·검수 발송 없음)
  2. 아니오, 취소
번호 선택:
```

- "1" → 7-1만 수행 (학원 캘린더 row만 생성) → 8 건너뛰고 9 결과 보고
- "2" → 즉시 종료

N > 0 → 6단계로.

### 6. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 휴강 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[휴강 정보]
- 일정명:   {일정명}
- 일자:     {YYYY-MM-DD}{ ~ YYYY-MM-DD if 기간}
- 범위:     {학원 전체 휴강 / 특정 선생님 휴가 ({선생님이름}) / 특정 반 휴강 (N개 반)}
- 비고:     {비고}

[휴강 반 목록] (모든 케이스 표시)
  - 파이썬 초등 A
  - 정보올림피아드 특강반
  ...

[학원 캘린더 row]
- 유형:        수업없음
- 관련 클래스: 위 반 N개 모두 link (학원 전체 / 선생님 휴가 / 특정 반 동일하게)

[영향 학생] 총 N명
▶ 정규수업 N1명:
  - 추득만 (목동초 5학년, 코딩 초등반 — 2026-05-04 월)
  - 김철수 (목동초 6학년, 코딩 초등반 — 2026-05-04 월)
  ...

▶ 보강수업 N2명:
  - 이영희 (목동초 5학년, 정보올림피아드 — 2026-05-05 화)
  ...

[자동 처리]
- 학원 캘린더 DB에 row 1건 추가
- 일일피드백 DB에 row {N}건 생성/patch (출결="휴강")
- 디스코드 parent_webhook으로 선생님 검수 메시지 발송

[제외]
- 학부모 카톡 안내는 /휴강카톡 (셋업 후 별도)
- 보강 일정은 /보강시간표등록 (필요 시 별도)

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

부정 또는 "취소" → 즉시 종료, 노션 변경 0.

### 7. 노션 처리 (트랜잭션 일관성)

#### 7-1. 학원 캘린더 row 생성 (`API-post-page`)

```json
{
  "parent": {"type": "database_id", "database_id": "<academy_calendar>"},
  "properties": {
    "일정명": {"title": [{"text": {"content": "<일정명>"}}]},
    "일자": {"date": {"start": "<target_date_start>", "end": <단일이면 omit / 기간이면 "target_date_end">}},
    "유형": {"select": {"name": "수업없음"}},
    "관련 클래스": {"relation": [{"id": "<반1 page_id>"}, {"id": "<반2 page_id>"}, ...]},
    "비고": {"rich_text": [{"text": {"content": "<비고>"}}]}
  }
}
```

규칙:
- **일정명 필드 절대 누락 금지** ⚠️. 사용자 입력 텍스트 그대로 title에 포함
- 단일 휴강 → `date.end` omit (start만)
- 기간 휴강 → `date.start` + `date.end` 둘 다
- **`관련 클래스` 항상 명시적 link** — scope 무관하게 `target_classes` 모든 반 link
  - scope == "academy" → 클래스관리 DB의 모든 반
  - scope == "teacher" → 선생님 담당 반들
  - scope == "classes" → 사용자가 선택한 반들
- **비고 필수** — 빈 칸 금지. "동일" 입력 시 일정명 텍스트 자동 복사

생성 실패 시 → 전체 중단, 노션 변경 0.

응답에서 `id` 추출 → `calendar_page_id` (디스코드 메시지 링크용).

#### 7-2. 일일피드백 row N건 처리

각 `(학생 page_id, 수업 날짜, 반 page_id, 수업타입)` 튜플마다:

**Step A. 기존 일일피드백 row 검색**

```
쿼리: 일일피드백 DB
- `학생 직접 == <학생 page_id>` AND `수업일자 == <수업 날짜>`
```

- 0건 → Step B (신규 생성)
- 1건+ → Step C (기존 row patch)

**Step B. 신규 생성**

먼저 수업계획서 row 확보:

```
쿼리: 수업계획·수업요약 DB
- `학생 == <학생 page_id>` AND `수업 날짜 == <수업 날짜>` AND `수업타입 == <수업타입>`
```

- 0건 → 자동 생성 (수업명·날짜·요일·타입·학생·클래스 필드, 수업내용 빈 칸 — /출석 동일 로직)
- 1건+ → 첫 row 사용

그 다음 일일피드백 row post-page:

```json
{
  "parent": {"type": "database_id", "database_id": "<lesson_results>"},
  "properties": {
    "피드백라벨": {"title": [{"text": {"content": "{YYYY-MM-DD} {학생} {반} 수업결과 (휴강)"}}]},
    "수업일자": {"date": {"start": "<수업 날짜>"}},
    "학생 직접": {"relation": [{"id": "<학생 page_id>"}]},
    "수업계획 선택": {"relation": [{"id": "<수업계획 page_id>"}]},
    "시간표": {"relation": [{"id": "<정규시간표 page_id>"}]},
    "출결": {"select": {"name": "휴강"}}
  }
}
```

(보강수업이면 `시간표` relation은 omit — 정규 시간표 link 없음)

**Step C. 기존 row patch**

```json
{
  "page_id": "<기존 일일피드백 page_id>",
  "properties": {
    "출결": {"select": {"name": "휴강"}}
  }
}
```

출결만 변경. 다른 필드는 그대로 유지 (피드백 내용 등 보존).

#### 7-3. 일부 실패 처리

각 row 처리 중 실패하면:
- 그 row 실패 로그 수집
- 다른 row 계속 처리
- 마지막에 결과 보고에서 실패 row 명시

### 8. 디스코드 검수 발송

`config.discord.parent_webhook` 사용.

#### 8-1. 메시지 본문 (상세형)

```
🚨 휴강 등록 알림 (선생님 검수용)

[휴강 정보]
- 일정명:   {일정명}
- 일자:     {YYYY년 M월 D일}{ ~ YYYY년 M월 D일 if 기간}
- 범위:     {학원 전체 휴강 / 특정 선생님 휴가 ({선생님이름}) / 특정 반 휴강}
- 휴강 반:  {반1}, {반2}, ...
- 비고:     {비고}

[영향 학생] 총 N명

▶ 정규수업 N1명:
  - 추득만 (목동초 5학년, 코딩 초등반 — 2026-05-04 월)
  - 김철수 (목동초 6학년, 코딩 초등반 — 2026-05-04 월)
  ...

▶ 보강수업 N2명:
  - 이영희 (목동초 5학년, 정보올림피아드 — 2026-05-05 화)
  ...

[자동 처리 결과]
✅ 학원 캘린더 row 1건 추가
✅ 일일피드백 row {N}건 처리 (출결="휴강")
   - 신규 생성 {N_new}건
   - 기존 patch {N_patch}건
{실패 row가 있다면}
⚠️ 실패 {N_fail}건: {학생 목록}

[노션 링크]
- 학원 캘린더: https://notion.so/{calendar_page_id}

[다음 작업]
- 학부모 카톡 안내: /휴강카톡 (셋업 완료 후)
- 보강 일정 필요 시: /보강시간표등록
```

#### 8-2. 발송 (PowerShell)

```powershell
$webhook = $config.discord.parent_webhook
$username = $config.discord.username
$content = $body  # 위 메시지 본문

$payload = @{ content = $content; username = $username }
$json = $payload | ConvertTo-Json -Compress -Depth 5
$bytes = [System.Text.Encoding]::UTF8.GetBytes($json)
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.SecurityProtocolType]::Tls12

try {
    Invoke-RestMethod -Uri $webhook -Method Post -Body $bytes -ContentType 'application/json; charset=utf-8' -TimeoutSec 30 -ErrorAction Stop
    # 발송 성공
} catch {
    # 발송 실패. 노션은 유지. 사용자에게 알림
}
```

#### 8-3. 발송 실패 처리

webhook 4xx·5xx 또는 timeout:
- 노션 변경은 유지 (롤백 안 함)
- 결과 보고에서 "디스코드 발송 실패" 표시
- 사용자에게 webhook 점검 권장

### 9. 결과 보고

```
✅ 휴강 등록 완료

[휴강]
- 일정명:   {일정명}
- 일자:     {YYYY년 M월 D일}{ ~ if 기간}
- 범위:     {학원 전체 / 일부 반}

[노션 처리]
- 학원 캘린더 row: https://notion.so/<calendar_page_id>
- 일일피드백 row 처리: 신규 {N_new}건 + patch {N_patch}건 = 총 {N - N_fail}건
{실패 시}
- 실패 {N_fail}건: 노션에서 직접 확인 필요

[디스코드 발송]
{성공}
✅ 선생님 검수 채널 발송 완료
{실패}
❌ 디스코드 발송 실패: <에러 원문>
   노션은 정상 등록됨. webhook 점검 후 수동 안내 권장

[다음 작업 추천]
1. 학부모 카톡 안내: /휴강카톡 (셋업 후)
2. 보강 일정 필요 시: /보강시간표등록 (학생별 케이스별)
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 휴강 범위 1/2/3 외 입력 | 재입력 |
| 학원 전체 — 클래스관리 DB에 반 0건 | "/클래스등록 먼저" 종료 |
| 선생님 휴가 — 재직 선생님 0건 | "재직 선생님 없음" 종료 |
| 선생님 휴가 — 그 선생님 담당 반 0건 | "담당 반 없음" 종료 |
| 특정 반 — 사용자가 선택한 반 번호 형식 오류 | 재입력 |
| 기간 입력 형식 오류 | 재입력 |
| 종료 < 시작 | 재입력 |
| ±1년 이상 차이 | 경고 + 확인 |
| 일정명 빈 칸 | 재요청 |
| 같은 기간 휴강 row 이미 있음 | 차단 + 3가지 가이드 |
| 영향 학생 0명 | 안내 + 진행 확인 (캘린더만 등록 가능) |
| 일일피드백 row 이미 존재 | 출결을 "휴강"으로 patch (다른 필드 보존) |
| 수업계획 row 없음 | 자동 생성 (/출석 동일 로직) |
| 학원 캘린더 row 생성 실패 | 전체 중단, 노션 변경 0 |
| 일일피드백 일부 실패 | 그 row 실패 로그, 나머지 계속 |
| 디스코드 발송 실패 | 노션 유지, 발송 실패 안내 |
| 미리보기 부정 | 즉시 종료, 노션 변경 0 |
| 어느 단계든 "취소"·"q" | 즉시 종료 |

## 주의사항

- **확인 없는 자동 처리 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 노션에 절대 POST 금지
- **디스코드 = 선생님 검수용**: 학부모 발송 절대 시도 X. 학부모 실제 발송은 /휴강카톡 (셋업 후 별도)
- **session_save_webhook 절대 사용 금지**: `parent_webhook` 만 사용
- **개인정보 보호**: 메시지에 학부모 전화번호 포함 금지
- **출결 변경만**: 일일피드백 기존 row patch 시 출결 필드만 변경. 다른 필드(피드백 내용 등) 보존
- **관련 클래스 항상 명시적 link**: 학원 전체·선생님 휴가·특정 반 모두 동일하게 `target_classes` 명시 link (학원 전체도 모든 반 자동 link, omit 금지)
- **일정명 절대 누락 금지**: post-page의 `properties.일정명` 반드시 포함. 빈 title이면 노션이 날짜 자동 표시 → 휴강 사유 보이지 않게 됨
- **비고 빈 칸 금지**: 휴강 사유 필수. "동일" 입력 시 일정명 자동 복사
- **단일 vs 기간 처리**: 단일 → `date.end` omit. 기간 → start + end 둘 다
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (메모리 `feedback_yes_no_korean`)
- **터미널 default**: 엔터 사용 안 함. 번호 선택만 (메모리 `feedback_terminal_default_input`)

## 예시 1: 학원 전체 단일 휴강 (어린이날)

**입력**
- 범위: 1 (학원 전체)
- 기간: 1 (단일) → 2026-05-05
- 일정명: 어린이날
- 비고: 없음

**영향 학생**: 정규 5명, 보강 0명 → 총 5명

**노션 결과**
- 학원 캘린더 row: 일정명="어린이날", 일자=2026-05-05, 유형=수업없음
- 일일피드백 row 5건: 출결="휴강" (5명 학생)
- 디스코드 검수 메시지 발송 완료

## 예시 2: 학원 전체 연휴 (추석)

**입력**
- 범위: 1 (학원 전체)
- 기간: 2 (기간) → 2026-09-15 ~ 2026-09-17
- 일정명: 추석 연휴
- 비고: 3일 연휴

**영향 학생**: 정규 12명 (3일 × 평균 4명), 보강 2명 → 총 14명

**노션 결과**
- 학원 캘린더 row 1건 (기간)
- 일일피드백 row 14건

## 예시 3: 일부 반 휴강

**입력**
- 범위: 2 (일부 반) → 파이썬 초등 A 선택
- 기간: 1 (단일) → 2026-06-15
- 일정명: 김선생 휴가
- 비고: 휴가 사유

**영향 학생**: 파이썬 초등 A 수강생만 (3명)

**노션 결과**
- 학원 캘린더 row: `관련 클래스` = 파이썬 초등 A link
- 일일피드백 row 3건
