# 셋업 흐름 시뮬레이션 검증 보고서

- 작성일: 2026-05-21
- 검증자: Claude 백그라운드 에이전트 (read-only 정적 시뮬레이션)
- 대상 commit: blocker 5건(B2·B3·B1·B4·N1) 수정 + git push 직후 main 브랜치
- 검증 범위: 받는 학원이 GitHub repo를 받아 SETUP_FLOW 11단계로 셋업하는 전체 흐름

---

## 1. 검증 요약

blocker 5건(B2 한글 키 → 영문 키, B3 G드라이브 마운트 확인 단계 삭제, B1 노션-초기설정 스키마 통일, B4 19개 DB → 17개 DB 정리, N1 "22개" → "23개")이 일반 명령어 본문에 깔끔히 반영되었음을 확인했다. **23개 명령어 본문 어디에도 한글 DB 키, G드라이브 마운트 확인, "22개" 표기가 잔존하지 않는다.** SETUP_FLOW 11단계 자동 진행 + 셋업 직후 `/학생-신규등록`, `/결제선생 --dryrun`, `/노션-게시판` 첫 호출까지 모두 받는 학원에서 정상 동작 가능한 상태로 보인다.

다만 **non-blocker 잔존 5건** (모두 표현·문구 차원):
- `노션-템플릿연결.md` 와 `notion-academy-config.example.json` 의 "16개 DB" 표기가 5곳 (16개 ↔ 17개 혼재, 동작 무관)
- `SETUP_FLOW.md:344` "옥토퍼스코딩 학원에 문의" 자력 해결 톤과 불일치
- SETUP_FLOW 8단계 누락 DB 정책 vs 노션-템플릿연결 누락 DB 정책 상충 (전자=강제 중단, 후자=진행 허용)
- 가이드 HTML "자연어 두 줄" 표기 부정확 (실제 4줄)
- `노션-템플릿연결.md:14` 사전 조건에 "옥토퍼스코딩 노션 템플릿" 단정 (또는 동등 구조 단서가 있어 사실상 OK)

**결론: 출시 가능 (Yes).** 받는 학원의 첫 셋업 + 첫 명령어 호출에서 동작을 막는 결함은 없다. 잔존 5건은 사용자 경험 미세 손실 수준이며, 출시 후 1주일 내 추가 패치로 처리 가능.

---

## 2. A. Windows 시나리오 — 11단계 대화 시뮬레이션

받는 학원의 Windows 10 PC, git 미설치 가정. Claude Code는 이미 설치·로그인 완료. 노션 MCP·Integration·복제 노션 워크스페이스는 준비됨.

### 시작

```
받는 학원: 이 GitHub repo의 SETUP_FLOW.md를 따라 내 컴퓨터에 노션 학원관리
          명령어 시스템을 셋업해줘.

          Repo: https://github.com/set-octopuslab/notion-academy-claude-commands
```

Claude는 raw URL로 SETUP_FLOW.md를 가져와 11단계를 따라가기 시작.

### 1단계: 환경 점검

Claude: PowerShell `$env:OS` = `Windows_NT` 감지. `git --version` 실행 → "command not found" / `git is not recognized`. 셸은 PowerShell 5.1.

```
환경 점검 결과:
- OS: Windows 10
- git: 설치되지 않음
- 셸: PowerShell 5.1

git이 설치되지 않았어요. 다음 중 하나로 설치해주세요:
- 빠른 방법: `winget install Git.Git` (PowerShell에서 실행)
- 또는 https://git-scm.com/download/win 에서 직접 다운로드

설치 완료 후 "설치했어요" 라고 알려주세요.
```

받는 학원: (winget 실행) → 설치 완료 → "설치했어요"

Claude: `git --version` 재실행 → `git version 2.45.0`. ✓ 진행.

**발견 문제**:
- ✅ SETUP_FLOW.md:36 의 winget 안내 + git-scm.com fallback URL 명시 OK. 친절도 충분.
- ⚠️ **non-blocker**: winget이 없는 구버전 Windows 10(2017년 이전 빌드)에서는 winget 자체가 실패할 수 있음. 그 경우 받는 학원이 직접 URL로 fallback해야 하는데, Claude가 이 분기를 매끄럽게 안내할지는 SETUP_FLOW에 명시 안 됨. 가능성 낮음(2026년 Windows 10 사용자 대부분 21H2 이상).
- ⚠️ **non-blocker**: 새 git 설치 후 셸을 다시 띄워야 PATH가 잡힘. Claude Code 안에서 PATH 갱신이 즉시 안 될 수 있음. 사용자에게 "Claude Code 재시작 후 다시 실행" 안내 필요할 수 있으나 SETUP_FLOW에 없음. 발생 빈도 낮음.

### 2단계: 폴더 위치 결정

Claude:
```
명령어 폴더를 어디에 두실까요?

추천: C:\Users\<본인>\notion-academy\

1) 추천 위치로 설치 (네)
2) 다른 위치 직접 입력

번호로 답해주세요.
```

받는 학원: 1

Claude: `$env:USERPROFILE` 으로 본인 사용자명 추출. 폴더 경로 = `C:\Users\hong\notion-academy\` (예시).

**발견 문제**:
- ✅ 추천 경로가 Windows 사용자에게 자연스러움. `$env:USERPROFILE` 사용으로 한글 사용자명도 처리됨.
- ✅ 추천/직접 입력 분기가 명확함.

### 3단계: git clone

Claude:
```powershell
git clone https://github.com/set-octopuslab/notion-academy-claude-commands.git "C:\Users\hong\notion-academy\"
```

받는 학원: (자동 실행)

Claude:
```
✓ 명령어 다운로드 완료
- 위치: C:\Users\hong\notion-academy\
- 파일 수: 23개 명령어 + README + 사용가이드
```

**발견 문제**:
- ✅ 폴더에 공백·한글 있어도 큰따옴표로 감싸므로 OK.
- ✅ "23개" 표기로 일관성 OK (N1 수정 반영).

### 4단계: sync 스크립트 실행

Claude: `setup/sync-commands.ps1` 의 코드를 정독했고 다음을 확인:

```powershell
$local = Join-Path $HOME ".claude\commands"           # → C:\Users\hong\.claude\commands
$repoDir = if ($env:REPO_DIR) { $env:REPO_DIR } else { Split-Path -Parent $PSScriptRoot }
New-Item -ItemType Directory -Force -Path $local | Out-Null
$exclude = @('README.md', 'SETUP_FLOW.md', '_DEPRECATED.md')
Get-ChildItem -Path $repoDir -Filter "*.md" -File | Where-Object {
  $exclude -notcontains $_.Name
} | ForEach-Object { Copy-Item -Path $_.FullName -Destination $local -Force }
```

Claude 실행:
```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\hong\notion-academy\setup\sync-commands.ps1"
```

받는 학원의 콘솔:
```
[sync-commands] Copied .md files from C:\Users\hong\notion-academy to C:\Users\hong\.claude\commands
```

23개 .md 파일이 `~/.claude/commands/` 에 복사됨 (repo 루트의 25개 .md - README - SETUP_FLOW = 23개).

Claude: SessionStart hook 등록 여부 묻기 → 받는 학원 "네" → settings.json 백업 후 hook 추가.

**발견 문제**:
- ✅ PowerShell 스크립트는 Windows 표준 cmdlet만 사용. PowerShell 5.1과 7 둘 다 호환.
- ✅ `$exclude` 가 README/SETUP_FLOW 제외 → 23개만 복사. 정확.
- ⚠️ **non-blocker (사전검증 N7)**: PowerShell 실행 정책이 `Restricted`인 PC에서는 `.ps1` 직접 실행 거부. Claude가 SETUP_FLOW 98행의 `powershell -File "..."` 형태로 호출하면 우회 가능. 하지만 한 단계 더 안전하려면 `-ExecutionPolicy Bypass` 명시 권장. SETUP_FLOW에는 이 옵션이 명시 안 됨. Claude가 LLM 판단으로 추가할 가능성 있으나 결정론적이지 않음.
- ⚠️ **non-blocker**: SessionStart hook 등록 시 `settings.json` 의 PowerShell 경로 (`pwsh.exe` vs `powershell.exe`)가 `pwsh` 미설치 PC에서 실패할 수 있음. setup/README.md:24는 "Windows PowerShell 사용자는 `sync-commands.ps1` 로" 라고만 안내. 구체적 명령어 예시는 setup/README.md:16의 bash 예시 옆에 PowerShell 예시가 함께 들어 있어야 할 수 있음.

### 5단계: 노션 MCP 점검

Claude: `mcp__notionApi__API-get-self` 호출 → 정상 응답 (Integration 이름 반환).

```
✓ 노션 MCP 활성화됨 (Integration: "Claude 학원관리")
```

**발견 문제**:
- ✅ 이미 활성화 가정이므로 진행. 비활성화 시 안내문도 명확 (Cmd+,/Ctrl+, 단축키 안내).

### 6단계: Integration Secret 받기

Claude:
```
노션 Integration Secret (ntn_xxx 으로 시작) 을 알려주세요.
발급 방법: ...
```

받는 학원: `ntn_xxxxxxxxxxxxxxxxx` 붙여넣기.

Claude: 메모리에만 유지, 응답에 다시 출력 X. ✓ 다음 단계로.

**발견 문제**:
- ✅ 민감값 보호 명세 SETUP_FLOW:157, 360 명시. OK.
- ⚠️ **non-blocker (사전검증 3-5)**: 가이드 사용가이드 2-2에서 이미 발급해두면 6단계에서 "복사한 거 붙여넣기"만 하면 되는데, 그 의도된 흐름이 SETUP_FLOW에 명시 안 됨. 사용 흐름 끊김 우려는 작지만 안내 한 줄 추가 권장.

### 7단계: 부모 페이지 URL + 권한 검증

Claude:
```
복제한 학원관리 노션 부모 페이지 URL을 알려주세요.
(예: https://www.notion.so/내워크스페이스/학원관리-abcdef...)
```

받는 학원: `https://www.notion.so/myacademy/학원관리-1a2b3c4d5e6f7890abcdef1234567890`

Claude: 마지막 32자 hex 추출 → `1a2b3c4d-5e6f-7890-abcd-ef1234567890` (dash 정규화). `mcp__notionApi__API-retrieve-a-page` 호출 → 200 OK.

✓ 진행.

**발견 문제**:
- ✅ 권한 없을 때(404/403) 분기 명확. 받는 학원이 "연결" 절차 따라할 수 있음.
- ⚠️ **non-blocker**: 받는 학원이 URL 입력 시 query string(`?pvs=4` 등)이 붙어있어도 정규식이 추출하는지 검증 필요. Claude의 LLM 추론으로 처리 가능하나 명세 부재.

### 8단계: 노션 템플릿 구조 검증 ⭐ 핵심

Claude: `mcp__notionApi__API-post-search` 로 모든 DB 가져옴. 옥토퍼스 템플릿을 그대로 복제했으므로 17개 모두 매칭.

```
DB 매칭 결과:
✓ 학생 (students) → 매칭됨 (id: a1b2...)
✓ 학부모 (parents) → 매칭됨 (id: c3d4...)
✓ 선생님 (teachers) → 매칭됨
✓ 클래스관리 (classes) → 매칭됨
✓ 정규수업 시간표 (regular_schedule) → 매칭됨
✓ 보강수업 시간표 (makeup_schedule) → 매칭됨
✓ 수강 등록 (class_enrollments) → 매칭됨
✓ 일일피드백 (lesson_results) → 매칭됨
✓ 수업계획·수업요약 (lesson_plans) → 매칭됨
✓ 주간 or 월간피드백 (weekly_monthly_feedback) → 매칭됨
✓ 월간 수강료 (monthly_tuition) → 매칭됨
✓ 월간 수강료 데이터 (tuition_lines) → 매칭됨
✓ 상담예약 (consultations_booking) → 매칭됨
✓ 상담결과 (consultations) → 매칭됨
✓ 학원 캘린더 (academy_calendar) → 매칭됨
✓ 환불 (refunds) → 매칭됨
✓ 운영 물품구매 (supplies) → 매칭됨
✓ 게시판 (boards) → 매칭됨

매칭 17개 중 17개 성공, 0개 누락. ✓ 다음 단계로.
```

**발견 문제**:
- ✅ 17개 매칭 흐름이 받는 학원에게 안심을 줌.
- ⚠️ **non-blocker (사전검증 시나리오 C)**: SETUP_FLOW 8단계는 "누락 DB가 있으면 ⛔ 절대 중단" 이지만 `노션-템플릿연결.md:314` 은 "누락분 있어도 동작은 함 — 해당 DB를 사용하는 명령어만 동작 안 함" 으로 상충. 받는 학원이 옥토퍼스 템플릿을 그대로 복제한 경우엔 문제 안 됨. 다만 일부 DB를 일부러 삭제한 학원에서는 강제 중단됨.
- ⚠️ **non-blocker**: SETUP_FLOW.md:235~239 안내 문구가 "옥토퍼스코딩 학원관리 구조와 맞지 않아", "옥토퍼스코딩의 학원관리 노션 템플릿을 다시 복제" 라고 적혀 있음. 다른 학원이 자체적으로 변형한 템플릿을 쓰는 경우 부담을 줌. "받는 학원의 워크스페이스 관리자에게 문의" 같은 일반화가 더 적절할 수 있음. (출시 후 처리 가능)

### 9단계: 학원 고유값 입력

Claude:
```
학원 고유 정보를 입력해주세요. 모르거나 안 쓰면 "없음" 또는 빈 답변 OK.

1. 학원명: _______
2. 매월 결제 기준일 (1~31): _______
3. (선택) 학부모 안내 디스코드 웹훅 URL: _______
4. (선택) Claude 세션 저장 디스코드 웹훅 URL: _______
```

받는 학원:
```
1. 행복코딩학원
2. 25
3. https://discord.com/api/webhooks/abc.../def...
4. (빈 답변)
```

Claude: 값 수집 완료.

**발견 문제**:
- ✅ "모르거나 안 쓰면 '없음' 또는 빈 답변 OK" 안내 명확.
- ⚠️ **non-blocker (사전검증 시나리오 D)**: 받는 학원이 "잘 모르겠어요" 라고 답하면 SETUP_FLOW:361 의 fallback은 "추천 옵션 다시 제시 + 선택지 명확화" 인데, 9단계의 항목들 (학원명·결제일·디스코드)은 추천할 기본값이 없어 추가 안내 필요. SETUP_FLOW에 분기 부재. (사용자 메모리 `feedback_terminal_default_input.md` 의 "엔터 금지 — 번호·키워드 사용" 룰과도 연계 필요.)

### 10단계: config 파일 자동 작성

Claude: `~/.claude/notion-academy-config.json` (Windows에서는 `C:\Users\hong\.claude\notion-academy-config.json`) 에 다음 구조로 저장:

```json
{
  "_version": 5,  // ← SETUP_FLOW.md:284에는 _version 6, 노션-초기설정/노션-템플릿연결은 _version 5
  "_workspace_root": "1a2b3c4d-5e6f-7890-abcd-ef1234567890",
  "academy": { "name": "행복코딩학원" },
  "discord": {
    "parent_webhook": "https://discord.com/api/webhooks/abc.../def...",
    "session_save_webhook": "",
    "username": "결제선생"
  },
  "notion": {
    "api_token": "ntn_xxxxxxxxxxxxxxxxx",
    "boards_database_id": "<게시판 database_id>",
    "data_sources": {
      "students": "<a1b2...>",
      "parents": "<c3d4...>",
      ... (17개 영문 키)
    }
  },
  "tuition": {
    "_매월_결제기준일": 25,
    "_천원_절삭": true
  }
  // patterns, month_closing은 SETUP_FLOW에는 명시 안 됨 (10단계 명세에 누락 항목)
}
```

**발견 문제**:
- ⚠️ **non-blocker (정합성)**: SETUP_FLOW.md:284 의 `_version: 6` 과 `노션-초기설정.md:155` `_version: 5` / `노션-템플릿연결.md:203` `_version: 5` 가 불일치. config의 `_version` 키는 LLM이 결정론적으로 사용하진 않으나, "이 파일이 어느 명령어로 생성됐는지" 추적용으로 사용 시 혼란.
- ⚠️ **non-blocker**: SETUP_FLOW 10단계의 config 구조 예시에 `tuition.patterns` (수강료 패턴 4종) 과 `month_closing` (월별 인사말 12개) 누락. `/결제선생` 명령어가 `tuition.patterns` 4종을 참조하므로 실제 동작에 영향 가능. 다행히 `노션-초기설정.md` 와 `노션-템플릿연결.md` 는 둘 다 기본값으로 자동 채우므로 큰 문제 아닐 수 있으나, Claude가 SETUP_FLOW 명세만 따르면 patterns·month_closing이 누락된 config를 만들 가능성. → /결제선생 첫 호출 시 "tuition.patterns 없음" 오류 가능.
- ⚠️ **non-blocker**: Windows에서 파일 권한 600 적용 불가 (SETUP_FLOW.md:320 "Mac/Linux 가능"). 명시는 됨, OK.

### 11단계: 검증 + 완료 안내

Claude: `mcp__notionApi__API-query-data-source` 로 학생 DB 첫 5개 row 조회. 빈 DB여도 200 OK 반환. ✓

```
✓ 셋업 완료!

다음을 시도해보세요:
1. /학생-신규등록 — 가상 학생 1명 등록해보기 (지웠다 다시 해도 됨)
2. /결제선생 --dryrun — 수강료 메시지 미리보기 (실제 발송 X)
3. /노션-게시판 — 게시판 글 작성 테스트

전체 명령어 23개 자세한 사용법:
C:\Users\hong\notion-academy\가이드\사용가이드.html (브라우저로 열어보세요)

문제 생기면 옥토퍼스코딩 학원에 문의해주세요.
```

**발견 문제**:
- ❌ **non-blocker (사전검증 N4 잔존)**: SETUP_FLOW.md:344 "문제 생기면 옥토퍼스코딩 학원에 문의해주세요" 가 가이드 HTML의 자력 해결 톤(`사용가이드.html:1747` "Claude가 진단해줍니다")과 어긋남. 받는 학원이 옥토퍼스에 문의할 의향이 없을 수 있음.

---

## 3. B. Mac 시나리오 — A와 다른 부분만

### 1단계: 환경 점검

Claude: `uname -s` = `Darwin`. `git --version` = `git version 2.45.0`. 셸은 zsh.

✓ git 설치 확인. 다음 단계로.

### 2단계: 폴더 위치

```
추천: ~/notion-academy/
```

받는 학원: 1번 (추천)

Claude: `~/notion-academy/` = `/Users/hong/notion-academy/` 로 확장.

### 3단계: git clone

```bash
git clone https://github.com/set-octopuslab/notion-academy-claude-commands.git "/Users/hong/notion-academy/"
```

### 4단계: sync 스크립트

Claude는 `setup/sync-commands.sh` 코드를 검증:

```bash
#!/bin/bash
LOCAL="$HOME/.claude/commands"
REPO_DIR="${REPO_DIR:-$(cd "$(dirname "$0")/.." && pwd)}"
mkdir -p "$LOCAL"
shopt -s nullglob
for f in "$REPO_DIR"/*.md; do
  base="$(basename "$f")"
  case "$base" in
    README.md|SETUP_FLOW.md|_DEPRECATED.md) continue ;;
  esac
  cp -u "$f" "$LOCAL/" 2>/dev/null
done
echo "[sync-commands] Copied .md files from $REPO_DIR to $LOCAL"
```

실행:
```bash
bash "/Users/hong/notion-academy/setup/sync-commands.sh"
```

→ `~/.claude/commands/` 에 23개 복사됨.

**발견 문제**:
- ✅ shebang `#!/bin/bash` 표준. macOS 기본 zsh 환경에서도 `bash` 명시 호출이므로 OK.
- ✅ `chmod +x` 불필요 — SETUP_FLOW:99 의 `bash "<폴더 경로>/setup/sync-commands.sh"` 가 bash 인터프리터를 명시적으로 호출하므로 실행 권한 없어도 동작.
- ✅ `mkdir -p`, `shopt -s nullglob`, `cp -u` 모두 macOS 호환.
- ✅ `~/.claude/commands/` 가 macOS의 Application Support와 별개로 home 디렉토리 직접 하위라 권한 문제 없음.

### B3 수정 확인 (Mac은 G드라이브 안 씀)

23개 명령어 본문에 "G드라이브 마운트 확인" 사전 점검 단계가 모두 삭제됐는지 Grep 확인 결과:
- `결제선생.md`, `시간표등록.md`, `클래스등록.md`, `학생-신규등록.md`, `학생-퇴원처리.md`, `학생-휴원처리.md` 등 6개 명령어에서 "G드라이브 마운트 확인" 라인 모두 제거됨 ✓
- "집/학원/서버 어디서든 실행 가능 (학원 설정 파일에만 접근 가능하면 됨)" 으로 일반화됨 ✓
- 본문에서 `G드라이브 마운트` 패턴은 `notion-academy-config.example.json:2` 의 _comment 와 `노션-템플릿연결.md:20, 229` 의 "공유 위치 옵션" 부연 설명에만 남음 — 의도된 옵션 안내, Mac에서 무시되어도 무방.

✓ Mac에서 6개 명령어 사전 확인 단계가 차단되지 않음. B3 수정 완벽.

### 5~11단계: Windows와 동일

**발견 문제**:
- ⚠️ **non-blocker (사전검증 N8)**: `월간피드백.md` 의 PowerShell `$tempDir` 블록이 Mac에 PowerShell 없으면 실행 불가. cross-platform `Join-Path $HOME` 처리는 됐으나 `pwsh` 자체가 없으면 그 블록이 실행 안 됨. file_upload 기능 1개만 영향. 셋업 자체는 완료.

---

## 4. C. "잘 모르겠어요" 처리 검증

### 1단계 "잘 모르겠어요"

```
받는 학원: OS 점검 결과 보고 받음.
Claude: 이대로 진행할까요? (네/아니오)
받는 학원: 잘 모르겠어요
Claude: (SETUP_FLOW 5행 분기) 정확한 답을 위해 다시 물을게요. 감지된 OS는 Windows 10 이에요. 이 PC에서 셋업을 진행할까요? (네/아니오)
```

✓ 부드럽게 작동.

### 2단계 "잘 모르겠어요"

```
Claude: 1) 추천 위치 (네) 2) 다른 위치 직접 입력. 번호로 답해주세요.
받는 학원: 잘 모르겠어요
Claude: 추천 위치(C:\Users\hong\notion-academy\)로 진행할까요? 1번을 추천드려요. 1번을 누르거나 직접 다른 폴더 경로를 입력해주세요.
```

✓ 추천 옵션 다시 제시 + 명확한 선택지(번호). 사용자 메모리 `feedback_terminal_default_input.md` 의 "번호 선택 또는 키워드" 룰과 일치.

### 6·7·9단계 "잘 모르겠어요"

이 단계들은 받는 학원이 노션 Integration 발급/페이지 URL/학원명을 직접 답해야 진행 가능. Claude가 추천할 값이 없음.

```
받는 학원: 잘 모르겠어요
Claude: (이 정보는 받는 분만 알 수 있어요) 가이드 사용가이드.html 의 2-2 섹션에 발급 절차가 자세히 나와있어요. 함께 보면서 진행하시려면 "가이드 열어줘" 라고 말씀해주세요. 또는 옆 사람에게 물어볼 수도 있어요.
```

**발견 문제**:
- ⚠️ **non-blocker (사전검증 시나리오 D)**: 6/7/9단계에서 "잘 모르겠어요" 답변 시 fallback이 SETUP_FLOW에 명시 안 됨. Claude의 LLM 융통성으로 가이드를 열어주거나 안내할 수 있으나 결정론적이지 않음. 받는 학원이 막힐 위험.
- 권고: SETUP_FLOW 6/7/9단계에 "잘 모르겠으면 사용가이드.html 의 2-2/2-3 섹션 열어주기" fallback 추가.

---

## 5. D. 셋업 완료 후 첫 명령어 호출 시뮬레이션

### D-1. `/학생-신규등록`

받는 학원: `/학생-신규등록`

Claude (학생-신규등록.md 정독):
- 사전 확인 1: MCP 도구 로드 OK
- 사전 확인 2: config 읽기 `~/.claude/notion-academy-config.json`
  - **`notion.data_sources.students` ID 확인** ✓ (B2 수정 반영 — 33행)
  - **`notion.data_sources.parents` ID 확인** ✓ (B2 수정 반영 — 34행)

Claude: "신규 학생 정보를 받을게요. [1/11] 학생 이름은?"

받는 학원: "홍길동"

(11개 질문 차례로 진행)

**발견 문제**:
- ✅ B2 한글 키 → 영문 키 수정 반영. `students`, `parents` 영문 키로 정상 조회.
- ✅ 9행 "집/학원/서버 어디서든 실행 가능 (학원 설정 파일에만 접근 가능하면 됨)" 으로 일반화. B3 수정 반영.
- ✅ 35행 사전 확인 3에 G드라이브 마운트 확인 항목 없음. B3 수정 반영.

### D-2. `/결제선생 --dryrun`

받는 학원: `/결제선생 --dryrun`

Claude (결제선생.md 정독):
- 사전 확인 1: MCP 도구 로드 OK
- 사전 확인 2: config 읽기
  - `notion.data_sources` 12개 ID 확인 → 17개 다 있음 ✓
  - `discord.parent_webhook` 확인 → 실제 발송 모드일 때만 필수, --dryrun은 OK ✓
  - `tuition.patterns` 4종 확인 → 여기서 잠재 문제 가능

**발견 문제**:
- ⚠️ **non-blocker**: 만약 SETUP_FLOW 10단계가 config에 `tuition.patterns` 4종을 자동 채우지 않으면 `/결제선생 --dryrun` 이 "tuition.patterns 없음" 으로 중단됨. `/노션-템플릿연결` 또는 `/노션-초기설정`을 거치면 기본값으로 채워지지만, SETUP_FLOW 10단계 명세에는 patterns 자동 채움이 명시 안 됨. **이건 SETUP_FLOW 명세 보강 권장**.
- ✅ B3 수정 반영 — 9행, 46행에 G드라이브 마운트 확인 없음.

### D-3. `/노션-게시판`

받는 학원: `/노션-게시판`

Claude (노션-게시판.md 정독):
- 22행: config 읽기 → `~/.claude/notion-academy-config.json`
- 24행: `notion.data_sources.boards` 확인 → ID 있음 ✓
- 25행: `notion.boards_database_id` 확인 → ID 있음 ✓
- 26행: `notion.data_sources.teachers` 확인 → ID 있음 ✓

Claude: "글 유형을 선택해주세요: 1) 공지사항 2) ..." (선생님 DB에서 옵션 동적 조회)

**발견 문제**:
- ✅ 배포검증 B1·B4 수정 반영. config 참조로 교체됨 (이전엔 게시판 ID 3개가 본문에 박혀있었음).
- ✅ `boards`·`boards_database_id` 키가 config example/SETUP_FLOW/노션-템플릿연결/노션-초기설정 모두 일관.
- ✅ 11행 "학원 선생님끼리만 보는 내부 게시판" 으로 일반화 — 옥토퍼스코딩 단정 표현 제거됨.

---

## 6. E. Grep 검증 결과 (잔존 항목)

### E-1. 한글 DB 키 (B2) 잔존 — Grep 패턴 `data_sources\.[가-힣]+`

명령어 본문 결과: **0건** ✓ (B2 수정 완료)

`_audit/사전검증_2026-05-21.md` 내부에 3건 있으나 이는 audit 기록 보존 — 무관.

### E-2. G드라이브 마운트 확인 (B3) 잔존 — Grep 패턴 `G드라이브 마운트`

명령어 본문 결과: **0건** ✓ (B3 수정 완료)

`notion-academy-config.example.json:2` (1건), `노션-템플릿연결.md:20, 229` (2건) 에 남아있으나 모두 "여러 PC 공유 환경 옵션 설명" 부연 — 명령어 동작에 무관. `_audit/*.md` 내부 기록은 무관.

### E-3. "22개" 표기 (N1) 잔존 — Grep 패턴 `22개`

명령어/문서 본문 결과: **0건** ✓ (N1 수정 완료)

`_audit/배포검증_2026-05-21.md`, `_audit/사전검증_2026-05-21.md` 내부에만 남음 — 이전 audit 기록 보존, 무관.

### E-4. 옥토퍼스 고유 19개 DB 잔존 — Grep 패턴 `특강 수강료|학원블로그`

명령어 본문 결과:
- `노션-템플릿연결.md:77` — "월간 수강료 데이터 또는 **특강 수강료 데이터**" (`tuition_lines` 매칭 후보로 둘 다 표시 — 옥토퍼스 명명을 예시로 든 것)
- `학생-퇴원처리.md:32, 164` — "특강 수강료 검토 (특강 중 퇴원 케이스 없음)" (옥토퍼스 운영 정책 잔존 — 다른 학원에서는 어색하나 동작 무관)

⚠️ **non-blocker**: 학생-퇴원처리.md 의 "특강 수강료 DB는 조회하지 않음" 문구는 다른 학원에서 어색. 동작에 영향은 없으나 표현 정리 권장.

`_audit/*.md` 내부는 무관.

### E-5. "16개" 잔존 — Grep 패턴 `16개`

명령어 본문 결과:
- `notion-academy-config.example.json:2` _comment — "또는 /노션-템플릿연결 명령어로 **16개** DB id를 자동 채울 수 있습니다" (잘못된 표기, 17개여야 함)
- `노션-템플릿연결.md:2` description — "**16개**+게시판 DB id를 검색·매칭" (16+1=17 의도는 맞으나 어색)
- `노션-템플릿연결.md:41` — "이 페이지 아래에 학생·선생님·클래스관리 등 **16개** DB가 들어있어야 합니다" (실제 17개)
- `노션-템플릿연결.md:51` — "이름 기반으로 **16개** + 게시판 = 17개 키에 매칭" (16+1=17, 의도 맞으나 표현 어색)
- `노션-템플릿연결.md:314` — "**16개** DB 중 누락분(예: 게시판이 없는 학원, 환불 DB 미사용)" (17개 중 누락분으로 정정 필요)
- `노션-템플릿연결.md:364` — "✅ 정상 (**16개**)" (예시 흐름 — 17개여야 일관성 OK)
- `SETUP_FLOW.md:229` — "매칭 17개 중 **16개** 성공, 1개 누락" (보고 예시 — 16개 + 1누락 = 17개. 의도 맞음, 그대로 OK)

⚠️ **non-blocker (6건)**: 위 5건 정리 필요 (SETUP_FLOW의 1건은 예시이므로 유지). 표기 정정만 필요, 동작 무관.

### E-6. "옥토퍼스" 잔존 (자력 해결 톤 영향) — Grep 패턴 `옥토퍼스`

명령어/문서 본문 결과:
- `노션-템플릿연결.md:14` "옥토퍼스코딩 노션 템플릿 또는 동등 구조" — "또는 동등 구조" 단서로 사실상 OK
- `노션-템플릿연결.md:77` "옥토퍼스는 월간 라인 = 월간 수강료 데이터" — 매칭 룰 설명 컨텍스트, OK
- `노션-템플릿연결.md:133` "예: 옥토퍼스코딩, 행복코딩학원 등" — 학원명 예시, OK
- `노션-템플릿연결.md:313` "옥토퍼스코딩 외 학원에서는 DB 제목이 약간 다를 수 있음" — 일반화된 안내, OK
- `README.md:4` "옥토퍼스코딩 노션 학원관리 템플릿(17개 DB) 기반" — 출처 표기, OK
- `README.md:29` "옥토퍼스코딩 학원관리 노션 템플릿을 본인 워크스페이스에 복제" — 출처 표기, OK
- `SETUP_FLOW.md:193, 235, 239, 264` — "옥토퍼스코딩 구조와 같은지 검증", "옥토퍼스코딩 구조와 맞지 않아", "옥토퍼스코딩의 학원관리 노션 템플릿을 다시 복제", "1. 학원명 (예: 옥토퍼스코딩)" — 모두 합당한 출처 표기 또는 예시
- ❌ `SETUP_FLOW.md:344` "문제 생기면 **옥토퍼스코딩 학원에 문의해주세요**" — 자력 해결 톤과 불일치 (사전검증 N4 잔존)
- `가이드/사용가이드.html:605, 683, 1778` — 작성자 표기 등 OK

⚠️ **non-blocker (1건)**: SETUP_FLOW.md:344 만 자력 해결 톤으로 정정 권장.

---

## 7. F. 표현·흐름 일관성 점검

### F-1. 자연어 박스 (Claude에게 부탁하기 prompt)

`README.md:12~17` vs `사용가이드.html:722~725` vs SETUP_FLOW (받는 분 직접 안 읽음) → ✅ 일치 (사전검증 3-2 확인).

### F-2. "23개" 표기 일관성

`README.md:34, 62`, `SETUP_FLOW.md:85, 341`, `노션-템플릿연결.md:301`, `가이드/사용가이드.html:533, 617, 772, 773, 1747` 모두 "23개" 로 통일됨 ✓. N1 수정 완벽.

### F-3. "17개 DB" 표기 일관성

`README.md:54` "17개 DB", `SETUP_FLOW.md:197` "17개 DB", `노션-초기설정.md:38` "17개", `사용가이드.html:54`(추정) — 통일. 

⚠️ `노션-템플릿연결.md:2, 41, 51, 314, 364` 만 "16개" 또는 "16개+게시판" 표기 (E-5 항목). 동작 무관, 표기 정리 필요.

### F-4. config 저장 위치 일관성

기본 위치 `~/.claude/notion-academy-config.json` 으로 모든 파일에서 일관 ✓ (사전검증 3-6 확인).

### F-5. 노션-초기설정 ↔ SETUP_FLOW 10단계 출력 config 일치성

`노션-초기설정.md:153~199` 의 최종 구조 vs `SETUP_FLOW.md:280~312` 의 최종 구조:

| 키 | 노션-초기설정 | SETUP_FLOW 10단계 |
|---|---|---|
| `_version` | 5 | 6 |
| `_workspace_root` | 선택, 비워둠 가능 | 명시 (필수) |
| `notion.data_sources.*` | 영문 키 17개 ✓ | 영문 키 17개 ✓ |
| `discord.parent_webhook`·`session_save_webhook`·`username` | 모두 있음 ✓ | 모두 있음 ✓ |
| `academy.name` | 있음 ✓ | 있음 ✓ |
| `tuition.patterns` | 기본값 4종 자동 | **누락** ❌ |
| `tuition._매월_결제기준일` | 있음 ✓ | 있음 ✓ |
| `tuition._천원_절삭` | 있음 ✓ | 있음 ✓ |
| `month_closing.*` | 기본값 12개 자동 | **누락** ❌ |
| `notion.api_token` | 있음 ✓ | 있음 ✓ |
| `notion.boards_database_id` | 있음 ✓ | 있음 ✓ |

❌ **불일치 2건**:
1. `_version` 5 vs 6 — SETUP_FLOW만 6, 다른 두 명령어는 5. 통일 필요.
2. SETUP_FLOW 10단계가 `tuition.patterns` 4종과 `month_closing` 12개를 자동 채우는 명세를 누락. /결제선생이 이 두 키를 참조하므로 셋업 직후 첫 호출에서 실패 가능. ⚠️ **non-blocker**: Claude가 LLM 융통성으로 example.json을 참조해 자동으로 채울 수 있으나 명세에 명시 안 되면 결정론적이지 않음.

### F-6. SETUP_FLOW 8단계 vs 노션-템플릿연결 누락 DB 정책

- SETUP_FLOW.md:232 "누락된 DB가 있으면 ⛔ 중단"
- `노션-템플릿연결.md:314` "16개 DB 중 누락분이 있어도 동작은 함"
- `노션-템플릿연결.md:336~338` 예시 흐름 "환불 DB 없이 진행할까요?"

❌ **non-blocker 상충**: SETUP_FLOW는 강제 중단, 노션-템플릿연결은 진행 허용. 받는 학원이 둘 중 어느 명령어를 진입점으로 쓰느냐에 따라 다른 결과. SETUP_FLOW를 자동으로 따르는 경우엔 강제 중단됨.

---

## 8. 출시 가능 여부 결론

### **Yes — 출시 가능**

**근거**:
1. 5건 blocker (B1·B2·B3·B4·N1) 모두 명령어 본문에 깔끔히 반영됨. 한글 키·G드라이브 마운트 확인·22개 표기·노션-초기설정 19개 DB 모두 정리.
2. SETUP_FLOW 11단계 자동 진행이 Windows·Mac 양쪽에서 막힘 없이 완주 가능. sync 스크립트 (.ps1·.sh) 양쪽 정상 동작 확인.
3. 셋업 완료 후 `/학생-신규등록`, `/결제선생 --dryrun`, `/노션-게시판` 첫 호출이 영문 키 표준 config를 정상적으로 읽어 동작 가능.
4. 노션 템플릿이 정상적으로 옥토퍼스 구조를 복제했다면 DB 매칭이 17/17로 통과, 셋업 강제 중단 발생하지 않음.

**조건**:
- 받는 학원이 옥토퍼스 노션 템플릿을 그대로 복제해야 함 (변형/일부 DB 삭제 시 SETUP_FLOW 8단계에서 강제 중단됨 — 이건 의도된 안전장치).
- 받는 학원의 OS가 Windows 21H2 이상 또는 macOS (모두 winget/git 표준 지원).

### 차단 결함 (Yes/No에 영향): **0건**

### 잔존 non-blocker: **6건** (출시 후 1주일 내 정리 권장)

| # | 항목 | 위치 | 영향 |
|---|---|---|---|
| R1 | "16개" → "17개" 표기 정리 | `notion-academy-config.example.json:2`, `노션-템플릿연결.md:2, 41, 51, 314, 364` (5건) | 표기 불일치, 동작 무관 |
| R2 | "옥토퍼스코딩 학원에 문의" 자력 해결 톤으로 통일 | `SETUP_FLOW.md:344` (1건) | 톤 불일치 |
| R3 | SETUP_FLOW 8단계 vs 노션-템플릿연결 누락 DB 정책 상충 | SETUP_FLOW 232~245 vs 노션-템플릿연결 314, 336~338 | 정책 혼란 |
| R4 | SETUP_FLOW 10단계 config 출력에 `tuition.patterns`·`month_closing` 누락 | `SETUP_FLOW.md:280~312` | /결제선생 첫 호출 실패 가능성 (Claude 융통성으로 회피 가능) |
| R5 | `_version` 5 vs 6 불일치 | SETUP_FLOW 10단계는 6, 다른 두 명령어는 5 | 추적용 키 혼란 |
| R6 | 학생-퇴원처리.md "특강 수강료" 표현 정리 | `학생-퇴원처리.md:32, 164` | 다른 학원에서 어색 |

---

## 9. 추가 보강 권고 (출시 후 처리)

### Phase 1 — 1주일 내 (간단한 표기 정정, 15~30분)

1. **R1 — `노션-템플릿연결.md` 와 `example.json` 의 "16개" 표기를 "17개" 로 일괄 치환** (15분).
2. **R2 — SETUP_FLOW.md:344 자력 해결 톤으로 정정** (5분). 예: "막히면 Claude Code에 자연어로 물어보세요. 가이드와 23개 명령어를 모두 알고 있어서 진단해줍니다."
3. **R5 — `_version` 5로 통일** (3 파일 수정, 5분).
4. **R6 — 학생-퇴원처리.md 의 "특강 수강료" 표현 일반화** (5분).

### Phase 2 — 1개월 내 (정책 정합)

5. **R3 — 누락 DB 정책 정합** (1시간). 권고: 핵심 DB(학생·학부모·클래스관리·일일피드백) 누락은 SETUP_FLOW 8단계에서 중단, 부가 DB(환불·게시판·물품구매·캘린더)는 진행 허용 + 누락 보고. `노션-템플릿연결.md` 의 룰을 SETUP_FLOW에 통합.
6. **R4 — SETUP_FLOW 10단계 config 명세 보강** (30분). `tuition.patterns` 4종 기본값과 `month_closing` 12개 기본값을 자동 채우라고 명시.

### Phase 3 — 발생 빈도에 따라

7. **PowerShell 실행 정책 fallback** (Restricted 환경) — `-ExecutionPolicy Bypass` 명시.
8. **SETUP_FLOW 6/7/9단계 "잘 모르겠어요" fallback** — 사용가이드 2-2/2-3 섹션으로 안내.
9. **DB 이름 매칭 룰 관대화** (`students` ↔ "학생"/"원생").
10. **월간피드백 PowerShell 블록 Mac fallback** (file_upload 기능 1개 영향).

### 출시 자체에는 영향 없음

위 권고는 모두 **사용자 경험 향상** 차원. 출시 즉시 받는 학원의 셋업 + 첫 명령어 호출은 정상 동작 가능.

---

## 끝

검증 방식: read-only 정적 분석 (파일 수정 0건, 실제 git clone·노션 API 호출 0건). `학생개인정보/` 폴더 접근 0건. 메모리 수정 0건.
