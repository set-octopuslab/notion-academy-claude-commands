---
description: 노션 워크스페이스의 게시판 DB에 글을 대화식으로 작성·업로드
---

# 노션 게시판 글 작성

사용자가 `/노션-게시판` 을 실행하면, 아래 절차에 따라 대화식으로 게시판 글을 작성하고 사용자가 확인한 뒤에만 노션에 업로드한다.

## 글의 성격 (고정)

이 게시판은 **학원 선생님끼리만 보는 내부 게시판**이다. 따라서 작성되는 모든 글의 톤은 다음과 같이 고정된다:

- **독자**: 동료 선생님 (학부모/학생/외부인 아님)
- **톤**: 공적이면서도 동료 간 실무 공유 톤 — 격식은 과하지 않고, 존댓말은 유지하되 딱딱하지 않게
- **금지**: 학부모 대상 문구("학부모님께 안내드립니다" 등), 학생 대상 문구("여러분~" 등)
- **권장**: "선생님들께", "공유드립니다", "확인 부탁드립니다", "참고해주세요" 같은 동료 간 표현

제목과 본문 정리 단계에서 이 톤을 반드시 적용할 것.

## 대상 DB 정보

config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치 — `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장. 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내)

- **게시판 data_source_id**: `notion.data_sources.boards` (없으면 즉시 중단 + `/노션-초기설정` 또는 `/노션-템플릿연결` 안내)
- **게시판 database_id**: `notion.boards_database_id` (post-page parent에는 data_source_id가 들어가므로 게시판 database_id는 retrieve가 필요한 경우에만 사용)
- **선생님 data_source_id**: `notion.data_sources.teachers`

게시판 필드:

| 필드 | 타입 | 처리 |
|---|---|---|
| 제목 | title | 사용자에게 물어봄 |
| 내용 | rich_text | 본문 전체가 여기 들어감 |
| 글 작성자 | relation → 선생님 DB | 선생님 목록에서 선택 |
| 글 유형 | select | DB 스키마에서 동적으로 옵션 읽어 선택 |
| 작성일 | date | 오늘 날짜 기본값, 변경 가능 |

## 절차

### 1. 필요 정보 수집 (한 번에 한 질문씩)

#### 1-1. 글 유형 선택

- `mcp__notionApi__API-retrieve-a-data-source` 호출 (data_source_id: `config.notion.data_sources.boards`)
- 응답에서 `properties["글 유형"].select.options` 의 `name` 목록을 추출
- 사용자에게 옵션 목록을 번호 매겨 보여주고 선택받기
  - 예: "글 유형을 선택해주세요: 1) 공지사항"
- 옵션이 1개뿐이면 그래도 확인차 물어봄

#### 1-2. 글 작성자 선택

- `mcp__notionApi__API-query-data-source` 호출 (data_source_id: `config.notion.data_sources.teachers`)
- 결과에서 각 페이지의 `properties["선생님이름"].title[0].plain_text` 와 `id` 를 추출
- 이름 목록을 번호 매겨 보여주고 선택받기
- 선택한 페이지의 `id` 를 relation 값으로 기억해둠

#### 1-3. 작성일

- 환경 context의 `currentDate` (오늘 날짜, 예: `2026-04-11`)를 기본값으로 제시
- 사용자에게: "작성일은 오늘 날짜 `YYYY-MM-DD` 로 넣을게요. 바꿀 거면 알려주세요."
- 사용자가 다른 날짜를 주면 그 값 사용, 그냥 넘어가면 오늘 날짜 사용

#### 1-4. 제목 (러프 입력 → 다듬기)

- 사용자에게 "글의 제목 또는 주제를 대략 알려주세요. 제가 3개 후보로 다듬어드릴게요." 라고 요청
- 사용자가 러프한 제목/주제/키워드를 주면
- **선생님 간 내부 공지 톤**으로 제목 후보 3개를 제안
  - 짧고 명확할 것 (일반적으로 25자 이내)
  - 핵심 키워드 포함, 모호한 표현 지양
  - 예시 패턴: "[카테고리] 핵심내용 안내", "○○ 관련 공유", "○○ 일정 변경 안내"
- 사용자가 번호로 고르거나 "1번인데 ○○ 로 바꿔" 같은 수정을 요청하면 반영
- "더 많은 후보를 원해" 라고 하면 추가로 3개 더 생성
- 확정되면 다음 단계로

#### 1-5. 내용 내용 초안 수집 (러프 입력 → 정리)

- "글에 들어갈 내용을 대략 알려주세요. 키워드/메모/짧은 문장 뭐든 OK. 제가 선생님 간 내부 공지 톤으로 정리해드릴게요." 라고 요청
- 사용자가 자료를 주면 다음 단계(2번)에서 적극적으로 정리

### 2. 본문 정리 (적극적으로 재작성)

- 사용자가 준 내용 초안를 **선생님 간 내부 공지 톤**으로 적극적으로 재작성한다
  - 누락된 인사말/마무리 멘트를 자연스럽게 추가 ("선생님들께 공유드립니다", "확인 부탁드립니다" 등)
  - 키워드 나열만 받았다면 완성된 문장으로 확장
  - 문장을 학원 실무 톤으로 다듬음 (존댓말 유지, 과한 격식 지양)
  - 논리적 순서로 재배치
- 내용이 충분히 길어서 여러 항목이 나뉘면 목차를 만들어 구조를 준다 (짧은 글은 목차 생략)
- 내용 초안가 너무 짧거나 모호해서 추측이 필요한 부분은 **미리보기에서 "이렇게 해석했습니다"라고 짧게 표시**해서 사용자가 의도 차이를 확인할 수 있게 한다
- **rich_text 필드에 들어갈 최종 텍스트**로 포맷 (Notion DB 속성 rich_text는 페이지 블록이 아니므로 헤딩/토글/체크박스는 못 씀):
  - 구조 예시:
    ```
    【목차】
    1. 인사말
    2. 주요 안내
    3. 참고사항

    ─────────────

    【1. 인사말】
    (본문 내용)

    【2. 주요 안내】
    (본문 내용)

    【3. 참고사항】
    (본문 내용)
    ```
  - 서식은 rich_text annotations로 bold/italic/underline/color 정도만 적용 가능
  - 단순 줄바꿈과 기호(`【】`, `─`, `•`)로 구조감을 표현

- 정리한 최종 결과를 사용자에게 **미리보기로** 보여준다 (제목, 글 유형, 작성자, 작성일, 내용 전체)
- "이대로 업로드할까요? 수정할 부분 있으면 알려주세요." 라고 확인을 받는다
- 사용자가 수정 요청하면 반영 후 다시 미리보기 → 확인 루프

### 3. 업로드

- 사용자가 명시적으로 "업로드", "올려줘", "좋아 올려" 등 확정 신호를 줄 때만 실행
- `mcp__notionApi__API-post-page` 호출
  - `parent`: `{ "type": "data_source_id", "data_source_id": "<config.notion.data_sources.boards>" }`
  - `properties`:
    ```json
    {
      "제목": { "title": [{ "type": "text", "text": { "content": "<제목>" } }] },
      "내용": { "rich_text": [{ "type": "text", "text": { "content": "<본문전체>" } }] },
      "글 작성자": { "relation": [{ "id": "<선생님페이지id>" }] },
      "글 유형": { "select": { "name": "<선택한옵션>" } },
      "작성일": { "date": { "start": "<YYYY-MM-DD>" } }
    }
    ```
- **rich_text 2000자 제한 처리**: 본문이 2000자를 넘으면 `rich_text` 배열을 여러 세그먼트로 분할 (각 세그먼트 content ≤ 2000자, 줄바꿈 경계에서 자르기 선호)
- 업로드 성공 시 응답의 `url` 을 사용자에게 알려준다

## 주의사항

- 업로드는 **반드시 사용자 확인 후**에만 실행. 미리보기 없이 즉시 업로드 금지.
- 필요한 MCP 툴 스키마가 아직 로드되지 않았으면 `ToolSearch` 로 먼저 로드한 뒤 호출:
  - `select:mcp__notionApi__API-retrieve-a-data-source,mcp__notionApi__API-query-data-source,mcp__notionApi__API-post-page`
- 노션 연결이 안 되면 (`mcp__notionApi__API-get-self` 로 검증 가능) 사용자에게 먼저 안내한다.
- 게시판 DB 스키마가 나중에 바뀌어 필드가 추가/삭제되면 이 파일도 업데이트 필요. 특히 필수 필드가 늘어나면 1단계에 수집 질문을 추가할 것.
