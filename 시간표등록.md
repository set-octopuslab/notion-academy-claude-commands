---
description: 매주 반복되는 수업 시간표 등록. 학생-반-요일-시간 row N개 일괄 생성 (주N회 처리, 트랜잭션 일관성)
---

# 시간표등록

원장이 `/시간표등록` 을 입력하면, **학생 → 반(수강 등록 매칭) → 주N회 → N번 반복(요일·시간) → 미리보기 → 확인(네/아니오)** 후 노션 수업 시간표 DB에 row N건을 일괄 생성한다.

학생이 수강 등록 DB에 매핑된 반만 후보로 노출 (수강료 누락 방지). 매주 반복되는 모든 수업(정규반·특강반)이 이 명령어로 등록됨. 단발성 보강은 `/보강시간표등록` 별도.

> ⚠️ **Claude 동작 가드 (반드시 준수)**
>
> 1. **AskUserQuestion 도구 사용 금지**. 이 명령어의 모든 질문은 일반 텍스트로 출력하고 사용자 자유 답변(텍스트·번호) 받기. AskUserQuestion은 옵션 4개 제한이 있어 7개 요일을 한 번에 못 표시함.
> 2. **자의적 부연 설명 금지**. .md에 정의되지 않은 안내문(예: "주말도 가능?", "추가 요일 있나요?") 노출 금지. 정의된 텍스트만 그대로 출력.
> 3. **요일은 항상 7개 모두 한 줄 노출**. 부분 노출 금지. "월·화·수·목·금·토·일" 7개를 한 화면에 표시하고 1개 번호만 받기.
> 4. **주N회면 N번 정확히 반복**. K번째 요일 입력 → K번째 시작 시간 입력 → K번째 종료 시간 입력 → K+1번째 요일 입력 → ... 순서. K번째 row의 3개 입력이 끝나야 K+1번째로 넘어감.

## 범위

**입력받을 정보**
- 학생 (수강 등록된 학생만)
- 반 (학생의 수강 등록 반 중 선택)
- 주N회 (수강 등록 월간 횟수에서 자동 추정 + 사용자 확인)
- N번 반복: 수업요일 + 수업 시작 시간 + 수업 종료 시간

**자동 생성**
- 시간표라벨 = `"{학생이름} - {반이름} ({요일}{시작시간})"`

**자동 채워짐 (노션이 처리)**
- 선생님 (rollup), 정규수업시간 (formula), 수강생(수식)

**제외 (별도 명령어)**
- 보강수업 시간표 row — `/보강시간표등록`
- 수업계획서 — `/수업계획`
- 출결 — `/출석`, `/결석`

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-post-page,mcp__notionApi__API-patch-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.students`
   - `notion.data_sources.classes`
   - `notion.data_sources.class_enrollments`
   - `notion.data_sources.regular_schedule` (수업 시간표 DB)

## 절차

### 1. 학생 검색·선택

```
[1/?] 어떤 학생의 시간표를 등록하나요? (이름 일부만 입력해도 됨)

> 
```

쿼리: 학생 DB
- `학생이름 contains <입력>` AND `등록현황 == "등록"`

분기:
- 0건 → "학생 DB에 없습니다. /학생-신규등록 먼저 호출해주세요." 종료
- 1건 → 자동 선택 + 확인 ("`{학생이름}` 학생이 맞나요? 네/아니오")
- 2건+ → 학교·학년으로 구분 표시 후 번호 선택

### 2. 학생의 수강 등록 반 조회

쿼리: 수강 등록 DB
- `학생 == <학생 page_id>`

#### 2-1. 0건 → 차단

```
❌ {학생이름} 학생은 수강 등록된 반이 없습니다.

💡 시간표를 만들기 전에 학생을 반에 등록해야 합니다.
   /수강등록 으로 학생-반 매핑 + 수강료 설정 후 다시 시도해주세요.

종료 (노션 변경 없음).
```

#### 2-2. 1건+ → 다음 단계

각 등록 row의 `반` relation에서 클래스 정보(이름, 클래스타입, 선생님), `월간 수업 횟수` 함께 retrieve.

### 3. 반 선택

#### 3-1. 학생 1건 + 반 1건 → 통합 확인 (한 번만 묻기)

1단계에서 학생이 자동 선택됐고(1건), 2단계에서 그 학생의 등록 반도 1개라면 학생·반을 묶어 한 번에 확인:

```
✅ 검색 결과
- 학생: {학생이름} ({학교} {학년}학년)
- 반:   [{클래스타입}] {반이름} (담당: {선생님이름}, 월 {월간횟수}회)

이 학생을 이 반에 시간표 등록할까요? (네/아니오)
```

긍정 → 4단계. 부정 → 종료.

> 학생 확인 + 반 확인을 따로 두 번 묻지 않음. 둘 다 1건일 때 흐름 단축.

#### 3-2. 학생 2건+ 후 반 1건 → 반은 자동 + 확인

1단계에서 학생을 사용자가 직접 선택했다면(2건+에서 선택), 반이 1개여도 다시 한 번 확인:

```
✅ 선택한 학생의 등록 반이 1개입니다.
- [{클래스타입}] {반이름} (담당: {선생님이름}, 월 {월간횟수}회)

이 반의 시간표를 만들까요? (네/아니오)
```

#### 3-3. 반 2건+ → 목록 선택

```
[2/?] 어느 반의 시간표를 만들까요?
  1. [정규반] 파이썬 초등 A (담당: 김선생, 월 4회)
  2. [특강반] 정보올림피아드 특강반 (담당: 박선생, 월 4회)
번호 선택:
```

수강 등록 row의 월간 수업 횟수 함께 표시 (주N회 추정 도움).

### 4. 주N회 결정

#### 4-1. 자동 추정

선택한 수강 등록 row의 월간 수업 횟수 기준:
- 4 → 주1회 추천
- 8 → 주2회 추천
- 12 → 주3회 추천
- 그 외 → 직접 입력

#### 4-2. 안내문

```
[3/?] 주 몇 회 수업인가요?
       (수강 등록상 월 {월간횟수}회 → 주{추천N}회 추천)

  1. 주1회 (월 4회)
  2. 주2회 (월 8회)
  3. 주3회 (월 12회)
  4. 직접 입력 (특수 케이스)

번호 선택:
```

#### 4-3. 직접 입력 시

```
[?/?] 주 몇 회 수업인가요? (1~7 사이 정수)

> 
```

1~7 외 입력 → 재입력.

#### 4-4. 추정과 다른 N 입력 시 경고

```
⚠️ 수강 등록상 이 학생은 월 {월간횟수}회 (주{추천N}회)로 되어 있는데,
   주{입력N}회로 입력하셨습니다.

   이대로 진행하면 시간표 row {입력N}개가 만들어지지만,
   수강 등록의 월간 수업 횟수와 맞지 않을 수 있습니다.

   계속 진행할까요? (네/아니오)
```

긍정 → 진행. 부정 → 4단계 재입력.

### 5. N번 반복 — 요일·시간 입력 (주N회면 정확히 N번 반복)

각 시간표 row마다 3단계 (요일 → 시작시간 → 종료시간). K = 1, 2, ..., N.

> ⚠️ **반복 흐름 명확화**:
> - 주1회 → K=1 한 번만 (요일·시간 1세트)
> - 주2회 → K=1, K=2 두 번 (요일·시간 2세트)
> - 주3회 → K=1, K=2, K=3 세 번 (요일·시간 3세트)
> - K번째 row의 [요일 → 시작 → 종료] 3개 입력이 모두 끝나야 K+1번째 row로 넘어감
> - K번째 입력 도중에 K+1번째 정보를 미리 묻지 말 것

#### 5-1. K번째 시간표 입력 (요일)

```
[?/?] {K}번째 시간표 — 수업요일?
       (월·화·수·목·금·토·일 7개 중 1개만 선택)

  1. 월
  2. 화
  3. 수
  4. 목
  5. 금
  6. 토
  7. 일

번호 선택 (1~7):
```

> 7개 요일 모두 한 번에 노출. 부분 노출 금지. AskUserQuestion 도구 사용 금지(4개 제한 우회).

#### 5-1-1. 시작 시간 입력 (K번째 요일 선택 후 즉시)

```
[?/?] {K}번째 시간표 — 수업 시작 시간? (HH:MM, 24시간제)
       (예: 16:00, 17:30)

> 
```

#### 5-1-2. 종료 시간 입력 (K번째 시작 시간 입력 후 즉시)

```
[?/?] {K}번째 시간표 — 수업 종료 시간? (HH:MM, 24시간제)

> 
```

K번째 [요일·시작·종료] 3개 모두 받은 후 K+1번째로 넘어감 (N번 반복 종료까지).

#### 5-2. 시간 형식 검증

- 정규식: `^([01]?[0-9]|2[0-3]):[0-5][0-9]$` (00:00 ~ 23:59)
- 단일 자릿수 시간 → 자동 0 패딩 (`9:00` → `09:00`)
- 형식 오류 → "HH:MM 24시간제로 입력해주세요. (예: 09:00, 16:00)" 재입력

#### 5-3. 시작 < 종료 검증

`시작 시간 ≥ 종료 시간` → "시작 시간이 종료 시간보다 늦거나 같습니다. 다시 입력해주세요." 재입력.

#### 5-4. K번째 row 내부에서 즉시 충돌 검사

K=2 이상이면, 1~K-1번째 입력과 같은 요일·시간대 중복 차단:
```
⚠️ 이전 {K-1}번째 시간표와 같은 요일·시간대입니다. 다시 입력해주세요.
```

이 시점은 노션 변경 전이므로 입력만 재요청.

### 6. 같은 학생-기존 시간표 충돌 검사

5단계 입력 완료 후, 학생의 **기존 수업 시간표 row 전체**와 시간 충돌 검사.

쿼리: 수업 시간표 DB
- `수강생 == <학생 page_id>`

#### 6-1. 충돌 정의

같은 요일이고 시간대가 겹치면 충돌:
- 새: 16:00 ~ 17:30
- 기존: 17:00 ~ 18:00 → 충돌 (17:00~17:30 겹침)

겹침 판정 공식: `start_new < end_existing AND start_existing < end_new`.

#### 6-2. 충돌 시 차단

```
━━━━━━━━━━━━━━━━━━━━━━
❌ 시간 충돌 발견.
━━━━━━━━━━━━━━━━━━━━━━

[충돌 정보]
- 새 시간표:    {요일} {시작}~{종료}
- 기존 시간표:  {요일} {기존시작}~{기존종료} ({기존 반이름})
- 노션 링크:    https://notion.so/<기존 page-id>

💡 한 학생이 같은 시간대에 두 수업을 동시에 받을 수 없습니다.
   다음 중 하나로 처리해주세요:

  1. 시간 변경하여 다시 시도
     → 명령어 다시 호출하고 다른 시간 입력

  2. 기존 시간표 변경
     → 위 노션 링크에서 기존 시간 수정 후 명령어 다시 호출

  3. 기존 시간표 삭제 후 재등록
     → 위 노션 링크에서 기존 row 삭제 후 명령어 다시 호출

━━━━━━━━━━━━━━━━━━━━━━
종료 (노션 변경 없음).
```

> 다른 반과의 시간 충돌도 차단. 한 학생이 같은 시간 두 수업 받을 수 없음.

### 7. 같은 학생-같은 반-같은 요일-같은 시간 중복 차단

기존 시간표 중 같은 반·요일·시간이 정확히 일치하는 row가 있으면 차단:

```
━━━━━━━━━━━━━━━━━━━━━━
❌ 같은 학생-같은 반-같은 요일-같은 시간의 시간표가 이미 있습니다.
━━━━━━━━━━━━━━━━━━━━━━

[기존 row]
- {학생이름} / {반이름} / {요일} {시작}~{종료}
- 노션 링크: https://notion.so/<기존 page-id>

💡 중복 등록할 수 없습니다.
   노션에서 확인 후 다른 시간으로 시도해주세요.

━━━━━━━━━━━━━━━━━━━━━━
종료 (노션 변경 없음).
```

### 8. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 시간표 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생-반]
- 학생:        {학생이름}
- 반:          {반이름} ({클래스타입})
- 담당 선생님: {선생님이름}
- 수강 등록:   월 {월간횟수}회

[등록할 시간표 — 주{N}회]
  1. {요일1} {시작1} ~ {종료1}
  2. {요일2} {시작2} ~ {종료2}
  ...
  {N}. {요일N} {시작N} ~ {종료N}

[처리]
- 동작:        수업 시간표 DB에 row {N}건 일괄 생성
- 후속 작업:   /수업계획, /출석·/결석

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

부정 또는 "취소" → 즉시 종료, 노션 변경 0.

### 9. 노션 row 일괄 생성 (트랜잭션 일관성)

#### 9-1. K번째 row post-page (K = 1, 2, ..., N)

```json
{
  "parent": {"type": "database_id", "database_id": "<수업 시간표 database_id>"},
  "properties": {
    "시간표라벨": {"title": [{"text": {"content": "{학생이름} - {반이름} ({요일}{시작시간})"}}]},
    "수업요일": {"select": {"name": "<요일>"}},
    "수업 시작 시간": {"rich_text": [{"text": {"content": "<HH:MM>"}}]},
    "수업 종료 시간": {"rich_text": [{"text": {"content": "<HH:MM>"}}]},
    "수업선택": {"relation": [{"id": "<반 page_id>"}]},
    "수강생": {"relation": [{"id": "<학생 page_id>"}]}
  }
}
```

각 응답에서 `id` 추출 → 결과 보고 URL에 사용.

#### 9-2. 트랜잭션 일관성 (롤백)

K번째 row 생성 실패 시 → 1~K-1번째 row 모두 archive(`API-patch-page` with `archived: true`)로 롤백.

```
❌ {K}번째 시간표 생성 실패. 이전 {K-1}건 모두 archive로 롤백합니다.

원인: <오류 메시지>

종료 (노션은 archive된 row가 N-1건 남아있을 수 있음 — 휴지통 확인 권장).
```

> 노션 API는 트랜잭션 미지원. 명령어 측에서 부분 성공 시 archive로 정리.

### 10. 결과 보고

```
✅ 시간표 등록 완료 — 주{N}회 row {N}건

[학생-반]
- 학생:    {학생이름}
- 반:      {반이름}
- 선생님:  {선생님이름}

[등록된 시간표]
1. {요일1} {시작1}~{종료1}  → https://notion.so/<page-id-1>
2. {요일2} {시작2}~{종료2}  → https://notion.so/<page-id-2>
...

[다음 작업 추천]
1. /수업계획 → 수업 전 수업계획·수업요약 DB 입력
2. 다른 반 시간표가 더 있다면 /시간표등록 다시 호출
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 학생 검색 0건 | "/학생-신규등록 먼저" 종료 |
| 학생 동명이인 | 학교·학년 표시 후 선택 |
| 수강 등록 0건 | "/수강등록 먼저" 종료 |
| 수강 등록 반이 1개만 | 자동 선택 + 확인 |
| 수강 등록 반이 N개 | 클래스타입·월간 횟수 함께 표시 후 선택 |
| 주N회 자동 추정과 다른 입력 | ⚠️ 경고 + 진행 확인 |
| 주N회 1~7 외 입력 | 재입력 |
| 시간 형식 오류 | 재입력 |
| 시간 시작 ≥ 종료 | 재입력 |
| 같은 입력 내 K번째가 K-1번째와 충돌 | 즉시 재입력 (노션 변경 전) |
| 기존 시간표와 시간 충돌 (다른 반 포함) | 차단 + 3가지 행동 가이드 후 종료 |
| 같은 학생-같은 반-같은 요일-같은 시간 중복 | 차단 + 노션 링크 안내 후 종료 |
| 미리보기에서 부정 답변 | 즉시 종료, 노션 변경 0 |
| K번째 row 생성 실패 | 1~K-1번째 모두 archive 롤백 |
| 어느 단계든 "취소"·"q" | 즉시 종료 |

## 주의사항

- **확인 없는 자동 쓰기 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 노션에 절대 POST 금지
- **수강 등록 cross-check 강제**: 수강 등록 없는 학생·반은 시간표 등록 차단. 청구 누락 방지
- **시간 충돌은 다른 반과도 차단**: 한 학생이 같은 시간 두 수업 받을 수 없음
- **트랜잭션 롤백**: 부분 성공 시 archive 처리. 노션 휴지통 확인 권장
- **시간 형식 24시간제**: 노션 정규수업시간 formula가 HH:MM 24시간제 가정
- **단일 자릿수 자동 패딩**: `9:00` 입력 시 `09:00`으로 자동 변환
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (메모리 `feedback_yes_no_korean`)
- **터미널 default**: 엔터 사용 안 함. 번호 선택·키워드만 사용 (메모리 `feedback_terminal_default_input`)

## 예시

**입력**
- 학생: 추득만 (목동초 5학년)
- 수강 등록: 파이썬 초등 A (월 8회)
- 반: 파이썬 초등 A
- 주2회 (월 8회 → 주2회 자동 추정)
- 1번째: 월 16:00 ~ 17:30
- 2번째: 수 16:00 ~ 17:30

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📋 시간표 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생-반]
- 학생:        추득만
- 반:          파이썬 초등 A (정규반)
- 담당 선생님: 김선생
- 수강 등록:   월 8회

[등록할 시간표 — 주2회]
  1. 월 16:00 ~ 17:30
  2. 수 16:00 ~ 17:30

[처리]
- 동작:        수업 시간표 DB에 row 2건 일괄 생성
- 후속 작업:   /수업계획, /출석·/결석

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

**노션 결과**
- 수업 시간표 DB에 row 2건 추가
- 시간표라벨 자동: "추득만 - 파이썬 초등 A (월16:00)", "추득만 - 파이썬 초등 A (수16:00)"
- 클래스의 `시간표` rollup·정규수업시간 formula 자동 갱신
