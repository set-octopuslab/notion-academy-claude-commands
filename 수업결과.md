---
description: 수업 후 일일피드백 DB의 13개 피드백 필드 입력 (학생 1명씩, 필수 3개 + 옵션 분기)
---

# 수업결과

원장이 `/수업결과` 를 입력하면, **학생 검색 → 오늘 row 매칭 → 보호 판정 → 필수 3개 입력 → 옵션 분기(과제·테스트·특이사항) → 미리보기 → 확인(네/아니오)** 후 일일피드백 DB row를 patch한다.

수업이 끝난 후 학생별 피드백 입력. 학부모 발송 명령어(`/일일피드백발송`)의 본문이 되는 데이터. 출결은 절대 변경 안 함 (`/출석`·`/결석` 책임).

> ⚠️ **Claude 동작 가드 (반드시 준수)**
>
> 1. **AskUserQuestion 도구 사용 금지**. 모든 질문은 일반 텍스트 출력 + 자유 답변 받기.
> 2. **자의적 부연 설명 금지**. .md에 정의되지 않은 안내문 노출 금지.
> 3. **출결 필드는 절대 patch 금지**. /출석·/결석의 책임. 이 명령어는 피드백 필드만 처리.
> 4. **의견 3개는 raw + AI 둘 다 patch**. 일일피드백 발송용 formula가 AI 필드를 참조하므로 AI 필드를 반드시 채워야 발송 본문이 정상 출력됨. raw에 사용자 입력 원본, AI에 최종 채택 텍스트 (다듬은 버전 또는 원본).
> 5. **사진 3개는 입력 X**. 노션 UI에서 직접 업로드.
> 5. **의견 3개 필드는 다듬기 패턴 강제**. 수업 내용 선생님 의견·과제 관련 선생님 의견·테스트 관련 선생님 의견은 학부모 발송 본문이므로 학부모 친근체 다듬기 + 사용자 확인/수정/원본 분기 거침. 다듬기 후 확인 없이 다음 단계로 넘어가지 말 것.
> 6. **다듬기 수정 무제한**. 사용자가 "이대로" 또는 "원본 그대로" 선택할 때까지 다듬기 반복.
> 7. **필수는 2개만**: 수업태도 + 수업 내용 선생님 의견. 과제완수도는 과제 분기로 이동 (과제 있는 날만 입력).

## 범위

**입력받을 필드**

필수 2개:
- 수업태도 (select: 상/중/하)
- 수업 내용 선생님 의견 (rich_text) ⭐ raw + AI 둘 다 patch

옵션 — 과제 분기 (과제 있는 날만):
- 과제완수도 (select: 상/중/하)
- 과제 점수 (number)
- 과제 제출여부 (checkbox)
- 지난과제내용 (rich_text)
- 과제 관련 선생님 의견 (rich_text) ⭐ raw + AI 둘 다 patch

옵션 — 테스트 분기:
- 테스트 점수 (number)
- 테스트주제 (rich_text)
- 테스트 관련 선생님 의견 (rich_text) ⭐ raw + AI 둘 다 patch

옵션 — 내부 메모:
- 내부 특이사항 메모 (rich_text)

**AI 필드 동시 patch (의견 3개)**
- 수업 내용 선생님 의견AI ← 수업 의견 다듬은 결과
- 과제 관련 선생님 의견AI ← 과제 의견 다듬은 결과
- 테스트 관련 선생님 의견AI ← 테스트 의견 다듬은 결과

> 이유: 일일피드백 발송용 formula는 AI 필드 3개를 참조해 본문을 만듦. 명령어가 AI 필드를 채우지 않으면 학부모 발송 본문이 빈 칸으로 출력됨.

**제외**
- 출결 (`/출석`·`/결석` 책임)
- 사진 3개 — 노션 UI에서 직접

## 사전 조건

- 학생 DB에 학생 row가 등록현황 == "등록"으로 존재
- 일일피드백 DB에 그 학생의 오늘 row가 이미 존재 (`/출석` 또는 `/결석`이 생성)

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-patch-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.students`
   - `notion.data_sources.lesson_results`
3. 현재 날짜 기록 (`today`)

## 절차

### 1. 학생 검색·선택

```
[1/?] 어떤 학생의 수업결과를 입력하나요? (이름 일부만 입력해도 됨)

> 
```

쿼리: 학생 DB
- `학생이름 contains <입력>` AND `등록현황 == "등록"`

분기:
- 0건 → "/학생-신규등록 먼저" 종료
- 1건 → 자동 선택 + 확인 ("`{학생이름}` 학생 맞나요? 네/아니오")
- 2건+ → 학교·학년 표시 후 번호 선택

### 2. 학생의 오늘 일일피드백 row 검색

쿼리: 일일피드백 DB
- `학생 직접 == <학생 page_id>` AND `수업일자 == <today>`

#### 2-1. 0건 → 차단

```
❌ {학생이름} 학생의 오늘 일일피드백 row가 없습니다.

💡 수업결과를 입력하기 전에 출결 처리가 먼저 필요합니다.
   /출석 (학생이 학원에 왔을 때) 또는
   /결석 (학생이 안 왔을 때) 으로 출결을 먼저 기록해주세요.

종료 (노션 변경 없음).
```

#### 2-2. 1건 → 다음 단계 (자동 선택)

#### 2-3. 2건+ (드문 케이스) → 목록 선택

```
[?/?] {학생이름} 학생의 오늘 row가 여러 개입니다 (정규+보강 동시 등):
  1. 정규수업 (파이썬 초등 A) - 출결: 출석
  2. 보강수업 (파이썬 초등 A) - 출결: 출석
번호 선택:
```

### 3. 보호 판정 (덮어쓰기 확인)

선택한 row의 13개 피드백 필드 검사. 하나라도 채워져 있으면 보호 발동.

채워짐 기준:
- select 필드 (수업태도·과제완수도) → not null
- number 필드 (과제 점수·테스트 점수) → not null
- checkbox (과제 제출여부) → true
- rich_text 필드 (의견·메모·주제 등) → 비어있지 않음

#### 3-1. 모두 비어있음 → 4단계로

#### 3-2. 하나라도 채워짐 → 보호 안내

```
⚠️ 이미 일부 피드백이 작성되어 있습니다:

  [출결]                  {기존 출결}
  - 수업태도:             {기존 또는 (없음)}
  - 과제완수도:           {기존 또는 (없음)}
  - 과제 점수:            {기존 또는 (없음)}
  - 수업 내용 선생님 의견: {기존 텍스트 앞 50자... 또는 (없음)}
  - 테스트 점수:          {기존 또는 (없음)}
  - 내부 특이사항 메모:   {기존 또는 (없음)}

새로 입력해서 덮어쓸까요?
  1. 네, 모두 덮어쓰기 (기존 내용 사라짐)
  2. 아니오, 종료 (수정은 노션에서 직접)
번호 선택:
```

부정 → 종료. 긍정 → 4단계.

### 4. 필수 2개 입력

#### 4-1. 수업태도

```
[필수 1/2] 수업태도?
  1. 상
  2. 중
  3. 하
번호 선택:
```

분기:
- "1" → "상", "2" → "중", "3" → "하"
- 그 외 → 재입력

#### 4-2. 수업 내용 선생님 의견 (학부모 친근체로 다듬기)

> 학부모 발송 본문이 되는 가장 중요한 필드. 다듬기 패턴 강제.

##### 4-2-1. raw 입력

```
[필수 2/2] 수업 내용 선생님 의견을 입력해주세요.
       대략적으로 적어주시거나 "수업계획 보고 작성해줘" 같이 요청하시면
       학부모 발송에 적합한 친근한 문체로 다듬어/작성해드립니다.
       (한 메시지로 여러 줄 가능)

> 
```

사용자 입력 처리:
- 직접 적은 raw → 그 텍스트 다듬기
- "작성해줘"·"수업계획 보고 작성해줘" 등 요청 → 그 학생의 오늘 수업계획 row의 `수업내용` 필드 retrieve해서 참고로 작성

##### 4-2-2. Claude가 학부모 친근체로 다듬어/작성해 제시

다듬기 가이드라인:
- 학부모가 읽는다는 전제 — 친근하고 따뜻한 톤
- 학생 칭찬·격려 톤 (`/수업계획`은 수업 안내, 이쪽은 학생 평가)
- 학습 내용·용어 정확히 유지
- 영어 용어 옆에 한글 설명 자연스럽게 추가
- 과도한 미사여구 금지

```
📝 다듬은 버전 (학부모용 친근체):
{다듬은/작성된 텍스트}

이대로 진행할까요?
  1. 네, 이 버전으로 저장
  2. 수정할 부분이 있어요 (의견 입력)
  3. 원본 그대로 저장 (다듬기 안 함, raw 텍스트가 있을 때만)
번호 선택:
```

##### 4-2-3. 분기

- "1" → 다듬은 버전 채택, 다음 단계로
- "2" → 수정 의견 입력 (4-2-4)
- "3" → 원본 채택 (raw가 있는 경우만)
- 그 외 → 재입력

##### 4-2-4. 수정 의견 입력 (선택지 2)

```
[?/?] 어떤 부분을 수정할까요?
       (예: "더 짧게", "○○ 부분 추가", "더 격식있게", "구체 예시 빼주세요")

> 
```

수정 의견 받고 다시 다듬어 4-2-2 형식으로 제시 → 분기 반복. 무제한.

##### 4-2-5. 원본 빈 칸 입력 시 (raw 자체 비어있음)

```
⚠️ 의견이 비어있습니다. 그래도 진행할까요? (네/아니오)
```
긍정 → 빈 칸 저장 (다듬기 단계 건너뜀). 부정 → 재입력.

### 5. 옵션 분기 — 과제

```
[과제] 오늘 과제 있었나요? (네/아니오)
```

부정 → 6단계로. 긍정 → 5-1 ~ 5-5 입력.

#### 5-1. 과제완수도

```
[과제 1/5] 과제완수도?
  1. 상
  2. 중
  3. 하
번호 선택:
```

분기:
- "1" → "상", "2" → "중", "3" → "하"
- 그 외 → 재입력

#### 5-2. 과제 점수

```
[과제 2/5] 과제 점수? (0~100, 점수 없으면 "없음")

> 
```

- 숫자 → 0~100 범위 검증
- "없음" → null
- 그 외 → 재입력

#### 5-3. 과제 제출여부

```
[과제 3/5] 과제를 제출했나요? (네/아니오)
```

긍정 → checkbox true. 부정 → checkbox false.

#### 5-4. 지난과제내용

```
[과제 4/5] 지난과제내용을 입력해주세요. (없으면 "없음")

> 
```

- 텍스트 → rich_text 저장
- "없음" → omit (빈 칸 유지)

#### 5-5. 과제 관련 선생님 의견 (학부모 친근체로 다듬기)

> 학부모 발송 본문 일부. 다듬기 패턴 강제 (4-2와 동일).

##### 5-5-1. raw 입력

```
[과제 5/5] 과제 관련 선생님 의견을 입력해주세요. (없으면 "없음")
       대략적으로 적어주시면 학부모 발송에 적합한 친근한 문체로 다듬어드립니다.

> 
```

- "없음" → omit (빈 칸 유지, 다듬기 건너뜀)
- 일반 텍스트 → 다듬기 단계로

##### 5-5-2. Claude가 학부모 친근체로 다듬어 제시

```
📝 다듬은 버전 (학부모용 친근체):
{다듬은 텍스트}

이대로 진행할까요?
  1. 네, 이 버전으로 저장
  2. 수정할 부분이 있어요 (의견 입력)
  3. 원본 그대로 저장
번호 선택:
```

##### 5-5-3. 분기 (4-2-3과 동일)

- "1" → 채택, 다음 단계로
- "2" → 수정 의견 입력 → 다시 다듬기 → 분기 반복 (무제한)
- "3" → 원본 채택, 다음 단계로

### 6. 옵션 분기 — 테스트

```
[테스트] 오늘 테스트 있었나요? (네/아니오)
```

부정 → 7단계로. 긍정 → 6-1, 6-2, 6-3 입력.

#### 6-1. 테스트 점수

```
[테스트 1/3] 테스트 점수? (0~100, 점수 없으면 "없음")

> 
```

#### 6-2. 테스트주제

```
[테스트 2/3] 테스트주제? (없으면 "없음")

> 
```

#### 6-3. 테스트 관련 선생님 의견 (학부모 친근체로 다듬기)

> 학부모 발송 본문 일부. 다듬기 패턴 강제 (4-2·5-5와 동일).

##### 6-3-1. raw 입력

```
[테스트 3/3] 테스트 관련 선생님 의견을 입력해주세요. (없으면 "없음")
       대략적으로 적어주시면 학부모 발송에 적합한 친근한 문체로 다듬어드립니다.

> 
```

- "없음" → omit (빈 칸 유지, 다듬기 건너뜀)
- 일반 텍스트 → 다듬기 단계로

##### 6-3-2. Claude가 학부모 친근체로 다듬어 제시

```
📝 다듬은 버전 (학부모용 친근체):
{다듬은 텍스트}

이대로 진행할까요?
  1. 네, 이 버전으로 저장
  2. 수정할 부분이 있어요 (의견 입력)
  3. 원본 그대로 저장
번호 선택:
```

##### 6-3-3. 분기 (4-2-3과 동일)

- "1" → 채택, 다음 단계로
- "2" → 수정 의견 입력 → 다시 다듬기 → 분기 반복 (무제한)
- "3" → 원본 채택, 다음 단계로

### 7. 옵션 분기 — 내부 특이사항 메모

```
[특이사항] 학부모에게는 비공개로 적을 내부 메모 있나요? (네/아니오)
```

부정 → 8단계로. 긍정 → 입력 받기.

```
[메모] 내부 특이사항 메모를 입력해주세요.

> 
```

### 8. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 수업결과 입력 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생-수업]
- 학생:        {학생이름}
- 출결:        {기존 출결} (변경 없음)
- 수업일자:    {today}
- 클래스:      {클래스 — rollup}
- 정규/보강:   {정규수업/보강수업 — rollup}

[필수 입력]
- 수업태도:               {입력값}
- 수업 내용 선생님 의견:
{여러 줄 텍스트}

(과제 분기 입력 시)
[과제]
- 과제완수도:             {값}
- 과제 점수:              {값 또는 (없음)}
- 과제 제출여부:          {네/아니오}
- 지난과제내용:           {텍스트 또는 (없음)}
- 과제 관련 선생님 의견:  {텍스트 또는 (없음)}

(테스트 분기 입력 시)
[테스트]
- 테스트 점수:            {값 또는 (없음)}
- 테스트주제:             {텍스트 또는 (없음)}
- 테스트 관련 선생님 의견: {텍스트 또는 (없음)}

(내부 메모 입력 시)
[내부 특이사항 메모]
{텍스트}

[처리]
- 동작:        일일피드백 DB row patch
- 보호 판정:   {신규 / 덮어쓰기}
- 후속 작업:   /일일피드백발송 (학부모 안내 — 향후 구현)

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

부정 또는 "취소" → 즉시 종료, 노션 변경 0.

### 9. 노션 patch

#### 9-1. row patch (`API-patch-page`)

입력받은 필드만 properties에 포함. 입력 안 한 옵션 필드는 omit (기존 값 유지).

**의견 3개 필드는 raw + AI 둘 다 patch**:

```json
{
  "page_id": "<일일피드백 page_id>",
  "properties": {
    "수업태도": {"select": {"name": "<상/중/하>"}},
    "수업 내용 선생님 의견":   {"rich_text": [{"text": {"content": "<raw 원본>"}}]},
    "수업 내용 선생님 의견AI": {"rich_text": [{"text": {"content": "<최종 채택 텍스트>"}}]}

    // 과제 분기 시:
    , "과제완수도": {"select": {"name": "<상/중/하>"}}
    , "과제 점수": {"number": <값>}
    , "과제 제출여부": {"checkbox": true}
    , "지난과제내용": {"rich_text": [{"text": {"content": "<텍스트>"}}]}
    , "과제 관련 선생님 의견":   {"rich_text": [{"text": {"content": "<raw 원본>"}}]}
    , "과제 관련 선생님 의견AI": {"rich_text": [{"text": {"content": "<최종 채택 텍스트>"}}]}

    // 테스트 분기 시:
    , "테스트 점수": {"number": <값>}
    , "테스트주제": {"rich_text": [{"text": {"content": "<텍스트>"}}]}
    , "테스트 관련 선생님 의견":   {"rich_text": [{"text": {"content": "<raw 원본>"}}]}
    , "테스트 관련 선생님 의견AI": {"rich_text": [{"text": {"content": "<최종 채택 텍스트>"}}]}

    // 내부 메모 분기 시:
    , "내부 특이사항 메모": {"rich_text": [{"text": {"content": "<텍스트>"}}]}
  }
}
```

**raw vs AI 필드 값 결정 로직**:
- 사용자가 "1. 다듬은 버전 채택" → raw=원본 입력, AI=다듬은 텍스트
- 사용자가 "3. 원본 그대로 저장" → raw=원본 입력, AI=원본 입력 (동일)
- 결국 **AI 필드에는 항상 발송될 최종 텍스트**가 들어감

> ⚠️ **출결 필드는 절대 patch 안 함**. /출석·/결석의 책임.

### 10. 결과 보고

```
✅ 수업결과 입력 완료

[학생]
- 이름:    {학생이름}
- 출결:    {기존 출결} (변경 없음)

[입력된 피드백]
- 수업태도: {값}
- 수업 내용 선생님 의견: (입력됨)
{과제(과제완수도 포함)·테스트·특이사항 — 입력했다면 표시}

[노션 URL]
https://notion.so/<page-id>

[다음 작업 추천]
1. 사진은 노션에서 직접 업로드 (수업사진·과제사진·테스트사진)
2. /일일피드백발송 으로 학부모에게 안내

ℹ️ AI 의견 필드 3개도 다듬은 텍스트로 함께 채워졌으므로,
   /일일피드백발송이 발송 본문(formula)을 정상 출력합니다.
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 학생 검색 0건 | "/학생-신규등록 먼저" 종료 |
| 학생 동명이인 | 학교·학년 표시 후 선택 |
| 오늘 row 0건 | "/출석·/결석 먼저" 종료 |
| 오늘 row 2건+ (정규+보강 동시) | 목록 선택 |
| 보호 발동 (이미 채워짐) | 덮어쓰기 확인 → 부정 시 종료 |
| 수업태도 1·2·3 외 입력 | 재입력 |
| 수업 의견 빈 칸 | "비었음, 진행?" 확인 |
| 과제 분기 진입 시 과제완수도 1·2·3 외 입력 | 재입력 |
| 과제 점수 0~100 외 또는 비숫자 | 재입력 |
| 테스트 점수 0~100 외 | 재입력 |
| "없음" 키워드 | 빈 칸으로 처리 |
| 미리보기 부정 답변 | 즉시 종료, 노션 변경 0 |
| 어느 단계든 "취소"·"q" | 즉시 종료 |

## 주의사항

- **확인 없는 자동 쓰기 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 노션에 절대 patch 금지
- **출결 절대 patch 금지**: /출석·/결석의 책임. 이 명령어는 피드백 13개 필드만 처리
- **옵션 미입력 = 기존 값 유지**: 과제·테스트·특이사항 분기에서 "아니오" 시 해당 필드들은 omit. 노션 기존 값 유지
- **사진 입력 X**: 사진은 노션 UI에서 직접
- **AI 필드 patch 필수**: 일일피드백 발송용 formula가 AI 필드 3개(수업 의견AI·과제 의견AI·테스트 의견AI)를 참조해 학부모 발송 본문을 만듦. 명령어가 AI 필드를 안 채우면 발송 본문 빈 칸 → raw + AI 둘 다 patch
- **raw vs AI 값 결정**: 사용자가 "다듬은 버전" 선택 시 raw=원본·AI=다듬은 텍스트. "원본 그대로" 선택 시 raw=AI=원본 입력. AI 필드는 항상 발송될 최종 텍스트
- **의견 3개 다듬기 패턴 강제**: 수업 의견·과제 의견·테스트 의견은 학부모 발송 본문이므로 다듬기 + 확인/수정/원본 분기 거침. 다듬기 후 사용자 확인 없이 다음 단계 넘어가지 말 것
- **다듬기 수정 무제한**: "이대로" 또는 "원본 그대로" 선택할 때까지 반복
- **수업계획 참고 작성 지원**: 사용자가 "수업계획 보고 작성해줘" 같은 요청 시 그 학생의 오늘 수업계획 row의 `수업내용` retrieve해서 참고로 작성
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (메모리 `feedback_yes_no_korean`)
- **터미널 default**: 엔터 사용 안 함. 번호 선택 또는 "없음" 키워드 (메모리 `feedback_terminal_default_input`)

## 예시

**입력**
- 학생: 추득만
- 출결: 출석 (이미 처리됨)
- 수업태도: 1 (상)
- 수업 의견: "오늘 변수 개념 잘 이해함. 자료형 차이도 정확히 구분."
- 과제: 네 → 과제완수도 2 (중), 점수 80, 제출 네, 지난과제 "변수 실습", 의견 "잘 했음"
- 테스트: 아니오
- 특이사항: 아니오

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📋 수업결과 입력 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생-수업]
- 학생:        추득만
- 출결:        출석 (변경 없음)
- 수업일자:    2026-05-10
- 클래스:      파이썬 초등 A
- 정규/보강:   정규수업

[필수 입력]
- 수업태도:               상
- 수업 내용 선생님 의견:
오늘 변수 개념 잘 이해함. 자료형 차이도 정확히 구분.

[과제]
- 과제완수도:             중
- 과제 점수:              80
- 과제 제출여부:          네
- 지난과제내용:           변수 실습
- 과제 관련 선생님 의견:  잘 했음

[처리]
- 동작:        일일피드백 DB row patch
- 보호 판정:   신규
- 후속 작업:   /일일피드백발송 (학부모 안내 — 향후 구현)

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

**노션 결과**
- 일일피드백 row의 7개 필드 patch (수업태도·수업 의견·과제완수도·과제 점수·과제 제출여부·지난과제내용·과제 의견)
- 출결·테스트·특이사항 필드는 변경 없음
- AI 의견 3개는 노션 자동화로 자동 생성될 예정
