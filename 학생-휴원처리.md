---
description: 재원 중인 학생을 휴원 상태로 변경 (등록현황 + 이탈일·이탈사유 patch)
---

# 학생-휴원처리

원장이 `/학생-휴원처리` 를 입력하면, 학생 이름을 검색해서 1명 선택하고, **사유·시작일을 받은 뒤 미리보기 → 확인(네/아니오)**, 그 학생의 학생 DB row 1개를 `등록현황 = 휴원` 으로 patch한다. 시간표·청구서·피드백은 전혀 건드리지 않음 (모두 `등록현황 == 등록` 필터로 자동 제외).

집/학원/서버 어디서든 실행 가능.

## 범위

**포함**
- 학생 검색 (이름 부분 일치 + 등록 상태)
- `등록현황` → "휴원" patch
- (선택) `이탈일` patch
- (선택) `이탈사유` patch (rich_text)

**제외**
- 시간표 row 수정
- 학부모 알림 발송
- 미납 정산

## 사전 조건

- 학생 DB의 `이탈사유` 필드가 **rich_text** (텍스트 타입). 만약 select 타입이면 사용자에게 노션 UI에서 텍스트로 변경 안내.

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-patch-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.학생` ID 확인
3. G드라이브 마운트 확인

## 절차

### 1. 학생 검색

```
[1/3] 휴원 처리할 학생 이름은? (일부만 입력해도 됨)
```

쿼리:
- `학생이름 contains <입력>` AND `등록현황 == 등록`

분기:
- 0건 → `❌ 재원생 중에 '<입력>' 이름 없음` 종료
- 1건 → 자동 선택 + 정보 표시:
  ```
  ✅ 1건 찾음
  - {학생이름} ({학교} {학년}학년, 등록현황: 등록)
  맞나요? (네/아니오)
  ```
  답변 인식: 메모리 `feedback_yes_no_korean` 규칙 따름
- 2건+ → 목록 번호 매겨 보여주고 선택받음

### 2. 휴원 사유

```
[2/3] 휴원 사유? (자유 입력, 모르거나 안 받았으면 엔터)
```

- 자유 입력. rich_text 필드라 어떤 텍스트도 OK
- 엔터/빈 값이면 필드 비움

### 3. 휴원 시작일

```
[3/3] 휴원 시작일? (YYYY-MM-DD, 엔터 시 오늘)
```

- "오늘"·엔터 → 오늘 날짜 자동 적용
- "내일"·"다음주 월요일" 같은 자연어 → YYYY-MM-DD로 정규화 후 사용자 확인
- 형식 오류 시 재입력
- 미래 날짜 OK
- 과거 날짜는 경고 후 확인

### 4. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📌 휴원 처리 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:      {학생이름}
- 학교/학년: {학교} {학년}학년
- 현재 상태: 등록

[변경 사항]
- 등록현황:  등록 → 휴원
- 이탈일:    {시작일 or "(없음)"}
- 이탈사유:  {사유 or "(없음)"}

(학생 row 1개만 patch. 시간표·청구서 변경 없음)
━━━━━━━━━━━━━━━━━━━━━━

⚠️ 노션 학생 DB의 row 1개가 업데이트됩니다.
진행할까요? (네/아니오)
```

### 5. 노션 patch

`API-patch-page` 1회 호출:

```json
{
  "page_id": "<학생 page_id>",
  "properties": {
    "등록현황": {"select": {"name": "휴원"}},
    "이탈일": {"date": {"start": "<시작일>"}},                                    // 입력 있을 때만
    "이탈사유": {"rich_text": [{"text": {"content": "<사유>"}}]}                  // 입력 있을 때만
  }
}
```

빈 값은 properties에서 키 omit.

### 6. 결과 보고

```
✅ 휴원 처리 완료
- 학생: {학생이름} (https://notion.so/<page-id>)
- 등록현황: 등록 → 휴원
- 이탈일: {시작일 or "(없음)"}
- 이탈사유: {사유 or "(없음)"}

복귀 시: 노션 UI에서 등록현황을 "등록"으로 직접 변경 (또는 향후 `/학생-복귀처리` 명령어 추가 예정)
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 학생 못 찾음 | "재원생 중에 '<입력>' 이름 없음" 안내 후 종료 |
| 동명이인 (재원생만 2건+) | 목록 번호 매겨 선택 |
| 이미 휴원/퇴원 상태 | 검색 필터(`등록현황 == 등록`)에서 제외되어 안 잡힘 |
| 휴원 시작일 형식 오류 | 재입력 |
| 휴원 시작일 과거 날짜 | "과거 날짜인데 맞나요? (네/아니오)" 확인 |
| 자연어 날짜 입력 ("오늘"·"내일") | YYYY-MM-DD 정규화 후 사용자 확인 |
| "취소"·"q" 또는 부정 답변 | 즉시 종료, 노션 변경 0 |

## 답변 인식 (네/아니오)

메모리 규칙 `feedback_yes_no_korean` 적용:
- 긍정: 네·예·응·맞아·맞습니다·이대로·진행·오케이·OK·y·Y·yes·좋아·좋습니다·해줘
- 부정: 아니오·아니·아뇨·아냐·잘못·취소·다시·n·N·no·안할래·취소해·중지
- 모호하면 한 번 더 명시적으로 묻기

## 주의사항

- **확인 없는 자동 patch 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 노션에 절대 PATCH 금지
- **빈 값 처리**: properties에서 키 omit
- **이탈사유 필드 타입**: 본 명령어는 rich_text 가정. select 타입이면 사용자에게 노션 UI 변경 안내 (스키마 변경은 사용자 직접)
- **휴원 ≠ 퇴원**: 등록현황만 "휴원". `/학생-퇴원처리`는 별도 명령어
- **결제선생·일일피드백 영향**: 휴원 처리 즉시 다음 발송부터 자동 제외됨 (등록현황 필터)

## 예시

**입력**
- 학생 이름: 추득만
- 휴원 사유: 포경수술로 인해 휴원
- 휴원 시작일: 2026-05-06 (오늘)

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📌 휴원 처리 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:      추득만
- 학교/학년: 목동초등학교 6학년
- 현재 상태: 등록

[변경 사항]
- 등록현황:  등록 → 휴원
- 이탈일:    2026-05-06
- 이탈사유:  포경수술로 인해 휴원

(학생 row 1개만 patch. 시간표·청구서 변경 없음)
━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

**노션 결과**
- 학생 row 1개 patch
- `등록현황` → 휴원 (회색)
- `이탈일` → 2026-05-06
- `이탈사유` → "포경수술로 인해 휴원"
