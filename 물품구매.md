---
description: 학원 운영 물품구매 내역을 노션 운영 물품구매 DB에 기록 (구매자·물건·카테고리·금액·결제방식·구매처·구매일자 입력)
---

# 물품구매 (운영 물품구매 DB 기록)

원장 또는 선생님이 학원 운영을 위해 구매한 물품을 노션 **운영 물품구매 DB**에 기록합니다. 대화식 입력 → 미리보기 → 확인(y/n) → row 생성.

영수증 사진은 명령어로 첨부하지 않습니다. row 생성 후 노션에서 직접 첨부하세요 (안내 메시지 출력).

## 범위

이 명령어는 운영 물품구매 DB row **1건 생성**만 다룹니다. 통계·정산·디스코드 알림 등 부가 기능은 포함하지 않습니다 (메모리 `feedback_simple_commands` 준수).

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-post-page
   ```
2. config 읽기: `G:/내 드라이브/ClaudeSetting/notion-academy-config.json`
   - `notion.data_sources.supplies` 확인 (없으면 즉시 중단 + `/노션-초기설정` 안내)
   - `notion.data_sources.teachers` 확인 (구매자 선택용)

## 입력 항목 (대화식)

순서대로 하나씩 물어서 받습니다. 메모리 `feedback_command_design_dialogue` 준수 — AskUserQuestion 1개씩, 자유 입력은 추천 형식 제안 후 사용자 결정.

### 1. 구매한 물건 (필수)

자유 입력. 예: "화이트보드 마커 10개", "학생 간식 음료", "청소기".

### 2. 카테고리 (필수)

선택지: **교구 / 소모품 / 사무용품 / 간식 / 시설**

판단 가이드:
- 교구: 수업에 직접 쓰는 도구 (마인크래프트 라이선스·코딩 보드·로봇 부품 등)
- 소모품: 자주 다시 사는 일반 물품 (펜·종이·휴지 등)
- 사무용품: 사무 운영용 (프린터 잉크·파일 등)
- 간식: 학생·선생님 간식
- 시설: 학원 시설 관련 (가구·청소기·에어컨 등)

### 3. 구매자 (필수, 선생님 DB에서 선택)

선생님 DB(`config.notion.data_sources.teachers`)를 query해 등록된 선생님 목록을 조회·표시 → 사용자에게 번호로 선택받음.

**중요**: 원장도 선생님 DB에 등록되어 있어야 함. 없으면 사용자에게 안내 후 중단.

### 4. 금액 (필수, 원 단위)

숫자만 입력. 콤마·"원"·"만원" 등 표기 허용해서 파싱.

예시 입력 → 정규화:
- "12000" → 12000
- "12,000" → 12000
- "1만원" → 10000
- "1.2만원" → 12000

음수·0 입력 시 오류 출력 후 재입력 요청.

### 5. 결제방식 (필수)

선택지: **카드 / 현금 / 계좌이체 / 지역상품권**

### 6. 구매일자 (선택, default = 오늘)

자연어 일시 파싱:
- 빈 입력 또는 "오늘" → 오늘
- "어제" → 어제
- "5월 17일" / "2026-05-17" / "5/17" → 해당 날짜

자유 입력. Claude가 파싱 시도 → 모호하면 ISO 형식(YYYY-MM-DD)으로 재확인.

### 7. 구매처 (선택, 자유 텍스트)

비워둘 수 있음. 예: "쿠팡", "다이소 목동점", "네이버 스마트스토어 OOO".

## 절차

### 1. 입력 받기
위 7개 항목을 1~7 순서대로 받음. 필수 4개(물건·카테고리·구매자·금액·결제방식) 누락 시 재입력 요청.

### 2. 미리보기 출력

```
━━━━━━━━━━━━━━━
📦 물품구매 미리보기

구매한 물건  : {입력값}
카테고리     : {선택값}
구매자       : {선생님 이름}
금액         : {입력값}원
결제방식     : {선택값}
구매일자     : {YYYY-MM-DD}
구매처       : {입력값 또는 "(없음)"}

생성할 위치: 운영 물품구매 DB
영수증     : row 생성 후 노션에서 직접 첨부 필요
━━━━━━━━━━━━━━━

이대로 생성할까요? (네/아니오)
```

### 3. 확인 분기

메모리 `feedback_yes_no_korean` 준수 — (네/아니오) 표기, 자유 답변도 의미로 해석.

- 네/yes/y/응/오케이 등 긍정 → 다음 단계
- 아니오/no/n/취소 등 부정 → 중단 후 "취소되었습니다" 출력
- 모호한 답 → 재질문

### 4. row 생성 (`API-post-page`)

parent: `{"type": "data_source_id", "data_source_id": "<supplies>"}`

properties:
- 구매한 물건 (title): {입력값}
- 카테고리 (select): {선택값}
- 구매자(담당선생님) (relation): [{id: <선생님 page_id>}]
- 금액 (number): {입력값}
- 결제방식 (select): {선택값}
- 구매일자 (date): {start: YYYY-MM-DD}
- 구매처 (rich_text): {입력값} ← 비어있으면 omit

### 5. 결과 보고

```
✅ 운영 물품구매 DB에 row 생성 완료

구매한 물건  : {입력값}
금액         : {입력값}원
생성된 page : {url}

📷 영수증 첨부:
노션에서 위 row 열기 → "영수증" 필드 클릭 → 파일/사진 업로드
```

## 엣지케이스

- **선생님 DB에 등록된 사람이 없음** → "선생님 DB가 비어있습니다. 먼저 선생님을 등록하세요"로 중단
- **금액 파싱 실패** → 재입력 요청 ("숫자만 또는 '1만원' 같이 입력해주세요")
- **카테고리·결제방식 select option이 노션에서 변경됨** → query로 현재 option 목록 가져와 동적 표시 권장
- **사용자가 중간에 '취소'** → 즉시 중단, "취소되었습니다" 출력
- **영수증 첨부를 명령어에서 받고 싶다는 요청** → 노션 UI에서 직접 첨부하라고 안내 (명령어는 row 생성만)

## 답변 인식 (네/아니오)

메모리 `feedback_yes_no_korean`:
- 표시: `(네/아니오)`
- 자유 답변 받기: y / yes / 네 / 응 / 오케이 → 긍정 / n / no / 아니 / 취소 → 부정
- 모호하면 재질문

## 주의사항

- **영수증은 명령어에서 받지 않음**. row 생성 후 노션 UI에서 직접 첨부.
- 메모리 `feedback_simple_commands` 준수 — 통계·정산·알림 등 부가 기능 추가 금지. 본질(1 row N 필드 생성)만 다룸
- 구매자는 선생님 DB에 등록된 사람만 선택 가능 (relation 무결성). 외부 구매자(학부모·외주 등)는 본 명령어 범위 밖
- **확인 없이 생성 금지**: 사용자의 명시적 (네) 응답 없이 노션에 절대 쓰지 않음
- 다른 학원에 배포될 때 학원 설정 DB와 무관 (이 명령어는 노션 DB ID만 의존)

## 예시 흐름

```
사용자: /물품구매

Claude: 1. 구매한 물건? (자유 입력)
사용자: 화이트보드 마커 5색 세트

Claude: 2. 카테고리? (교구/소모품/사무용품/간식/시설)
사용자: 소모품

Claude: 3. 구매자? (선생님 DB)
        1. 이승환
        2. 김동환
        3. 문성준
사용자: 1

Claude: 4. 금액? (숫자만 또는 "1만원" 같이)
사용자: 12000

Claude: 5. 결제방식? (카드/현금/계좌이체/지역상품권)
사용자: 카드

Claude: 6. 구매일자? (오늘/어제/날짜 직접 — 빈 입력 시 오늘)
사용자: 오늘

Claude: 7. 구매처? (선택, 빈 입력 가능)
사용자: 다이소

[미리보기 출력]

Claude: 이대로 생성할까요? (네/아니오)
사용자: 네

[row 생성 + 결과 보고]
```
