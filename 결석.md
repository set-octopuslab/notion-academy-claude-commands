---
description: 학생이 안 온 게 확인됐을 때 결석을 노션에 기록 (일일피드백 row 생성·patch, 시간 비교 없음)
---

# 결석

원장이 `/결석 <학생이름>` 또는 `/결석` 으로 입력하면, 학생을 검색해 오늘 수업을 자동 매칭한 뒤 일일피드백 DB에 결석 row를 신규 생성하거나 기존 row의 출결을 결석으로 patch한다. 피드백 작성된 row는 🔒 보호.

`/출석`과 핵심 로직 동일. **차이점**: 출결 값 = "결석" 고정, 시간 비교 단계 없음.

집/학원/서버 어디서든 실행 가능.

## 범위

**포함**
- 학생 검색 (등록 + 휴원)
- 오늘 정규+보강 시간표 매칭
- 클래스 결정 (자동 매핑 or fallback 선택 + 보너스 시간표 patch)
- 수업계획서 검색 (없으면 ⚠️ 경고 + 자동 생성)
- 일일피드백 row 신규 생성 또는 출결 patch (결석)

**제외**
- 결석 사유 입력 (v1에서는 받지 않음, 운영 중 필요하면 v2)
- 피드백 내용 입력
- 학부모 발송 (`/일일피드백` 별도)

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-post-page,mcp__notionApi__API-patch-page
   ```
2. config 읽기: `G:/내 드라이브/ClaudeSetting/notion-academy-config.json`
   - `notion.data_sources.students`
   - `notion.data_sources.regular_schedule`
   - `notion.data_sources.makeup_schedule`
   - `notion.data_sources.lesson_plans`
   - `notion.data_sources.classes`
   - `notion.data_sources.lesson_results`

## 절차

### 1. 학생 검색

호출 시 학생 이름이 인자로 들어왔으면 그대로 사용. 안 들어왔으면 질문:
```
어떤 학생인가요? (이름 일부만 입력해도 됨)
```

쿼리: 학생 DB
- `학생이름 contains <입력>` AND `등록현황 in (등록, 휴원)`

분기:
- 0건 → `❌ '<입력>' 이름의 등록·휴원 학생 없음` 종료
- 1건 → 자동 선택 + 정보 표시:
  ```
  ✅ {학생이름} ({학교} {학년}학년, 등록현황: {등록 or 휴원})
  ```
- 2건+ → 목록 번호 매겨 선택

### 2. 오늘 수업 매칭

오늘 = 호출 시각의 날짜 + 요일.

**2-1. 정규수업 시간표 검색**
- `수강생 == <학생 page_id>` AND `수업요일 == <오늘 요일>`

**2-2. 보강수업 시간표 검색**
- `수강생 == <학생 page_id>` AND `보강수업 날짜 == <오늘 날짜>`

**2-3. 합산 분기**

| 매칭 | 처리 |
|---|---|
| 0건 | `❌ 오늘 {학생이름}의 정규·보강 수업 없음` 종료 |
| 1건 | 자동 선택 |
| 2건+ | 목록 번호 매겨 선택 |

### 3. 클래스 결정

선택된 시간표 row의 `수업선택` (클래스 relation) 확인.

**3-1. 자동 매핑 (정상)**
- `수업선택` 에 클래스 link 있음 → 그 page_id 사용
- `retrieve-a-page` 로 클래스 title 추출

**3-2. Fallback (link 빠짐)**
- `수업선택` 비어있음 → 클래스 DB 전체 query → title 번호 매겨 표시 → 사용자 선택
- 선택 후, 시간표에도 저장할지 친절하게 안내:
  ```
  ━━━━━━━━━━━━━━━━━━━━━━
  ℹ️ 잠깐, 한 가지 더

  방금 선택하신 "{클래스이름}" 정보를
  노션의 "{정규수업 or 보강수업} 시간표" DB에도
  함께 저장해둘까요?

  지금 이 학생의 {정규수업 or 보강수업} 시간표에는
  어느 반에 속하는지가 비어있는 상태예요.
  한 번 채워두면 다음에 같은 학생 출결 처리할 때
  이 단계(클래스 선택)를 거치지 않고 자동으로 처리됩니다.

    1. 네, "{정규수업 or 보강수업} 시간표" DB에도 저장 
       ← 추천 (다음부터 자동으로 풀려서 빠름)
    2. 아니오, 이번 처리에만 사용 
       (시간표는 그대로 비워둠. 다음 호출 시 다시 묻습니다)

  번호 선택:
  ━━━━━━━━━━━━━━━━━━━━━━
  ```
- 1번 선택 → 해당 시간표 row의 `수업선택` relation에 클래스 patch. 다음 호출부터 자동 매핑
- 2번 선택 → 시간표 DB 그대로 둠. 다음 호출 시 또 fallback 거침

### 4. 수업계획서 검색·자동 생성

쿼리: 수업계획서 DB
- `학생 == <학생 page_id>` AND `수업 날짜 == <오늘>` AND `수업타입 == <정규수업 or 보강수업>`

분기:
- 1건 → 그 page_id 사용
- 0건 → ⚠️ 경고 + 자동 생성 (`API-post-page`):
  ```
  ⚠️ 오늘 {학생이름}의 {수업타입} 수업계획서가 없어 자동 생성합니다.
     수업내용은 빈 칸 — 수업 끝난 뒤 직접 채워주세요.
  ```
  ```json
  {
    "parent": {"type": "database_id", "database_id": "<lesson_plans database_id>"},
    "properties": {
      "수업명": {"title": [{"text": {"content": "{YYYY년 M월 D일} ({요일})\n{학생이름} {클래스이름} 수업계획"}}]},
      "수업 날짜": {"date": {"start": "<오늘>"}},
      "수업 요일": {"select": {"name": "<오늘 요일>"}},
      "수업타입": {"select": {"name": "<정규수업 or 보강수업>"}},
      "학생": {"relation": [{"id": "<학생 page_id>"}]},
      "클래스": {"relation": [{"id": "<클래스 page_id>"}]}
    }
  }
  ```
- 2건+ → 가장 최근 row 사용 + 알림

### 5. 기존 일일피드백 row 검색

쿼리: 일일피드백 DB
- `학생 직접 == <학생 page_id>` AND `수업일자 == <오늘>` AND `수업계획 선택 contains <수업계획서 page_id>`

분기:
- 0건 → 7단계로 (신규 생성)
- 1건 → 5-1 보호 판정
- 2건+ → 첫 번째 사용 + 알림

**5-1. 피드백 작성 여부 판정**

다음 13개 필드 중 **하나라도 채워졌으면 보호 발동**:
- `수업태도`·`과제완수도`·`과제 제출여부`·`과제 점수`·`테스트 점수`
- `테스트주제`·`지난과제내용`·`내부 특이사항 메모`
- `수업 내용 선생님 의견`·`수업 내용 선생님 의견AI`
- `과제 관련 선생님 의견`·`과제 관련 선생님 의견AI`
- `테스트 관련 선생님 의견`·`테스트 관련 선생님 의견AI`
- `수업사진,영상`·`과제사진`·`테스트사진`

(상세 기준은 `/출석.md` 와 동일)

**5-2. 분기**

🔒 **피드백 작성됨** (보호):
```
🔒 이미 피드백까지 작성된 row입니다.
   {학생이름} / {수업일자} / 출결: {기존 출결}
   변경하려면 노션에서 해당 row를 직접 수정하거나 삭제 후 다시 호출해주세요.
종료 (노션 변경 없음).
```

ℹ️ **출결만 들어감** (변경 가능):
```
ℹ️ 이미 출결이 입력되어 있습니다.
   현재: {기존 출결}
   변경: 결석
바꿀까요? (네/아니오)
```
긍정 → 7단계 patch. 부정 → 종료.

### 6. 출결 값

`/결석` 명령어는 시간 비교 없음. **출결 값 = "결석" 고정**.

### 7. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📌 결석 처리 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:      {학생이름}
- 학교/학년: {학교} {학년}학년

[수업]
- 수업타입:  {정규수업 or 보강수업}
- 클래스:    {클래스이름}
- 수업시간:  {시작} ~ {종료}
- 수업일자:  {오늘}

[처리]
- 출결:      결석
- 동작:      {신규 row 생성 or 기존 row 출결 patch}

⚠️ 변동 사항:
{ 수업계획서 자동 생성 시: "- 수업계획서 자동 생성됨 (수업내용 빈 칸)" }
{ 시간표 클래스 link 보너스 patch 시: "- 시간표 row에 클래스 link 추가" }
━━━━━━━━━━━━━━━━━━━━━━

진행할까요? (네/아니오)
```

### 8. 노션 patch 또는 생성

**8-1. 신규 일일피드백 row** (`API-post-page`):
```json
{
  "parent": {"type": "database_id", "database_id": "<lesson_results database_id>"},
  "properties": {
    "피드백라벨": {"title": [{"text": {"content": "{YYYY-MM-DD} {학생이름} {클래스이름} 수업결과 ({정규 or 보강})"}}]},
    "수업일자": {"date": {"start": "<오늘>"}},
    "학생 직접": {"relation": [{"id": "<학생 page_id>"}]},
    "수업계획 선택": {"relation": [{"id": "<수업계획서 page_id>"}]},
    "시간표": {"relation": [{"id": "<정규시간표 page_id>"}]},   // 정규일 때만 (보강이면 omit)
    "출결": {"select": {"name": "결석"}}
  }
}
```

**8-2. 기존 row 출결 patch** (`API-patch-page`):
```json
{
  "page_id": "<일일피드백 page_id>",
  "properties": {
    "출결": {"select": {"name": "결석"}}
  }
}
```

### 9. 결과 보고

```
✅ 결석 처리 완료
- 학생: {학생이름}
- 수업: {수업타입} {클래스이름} ({시간})
- 출결: {기존 → 결석 or 신규 결석}
- 일일피드백: https://notion.so/<page-id>
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 학생 못 찾음 | "학생 없음" 종료 |
| 동명이인 | 목록 선택 |
| 오늘 수업 0건 | "오늘 수업 없음" 종료 |
| 오늘 수업 2건+ | 목록 선택 |
| 시간표 클래스 link 빠짐 | 클래스 DB 나열 fallback + 보너스 patch |
| 수업계획서 0건 | ⚠️ 경고 + 자동 생성 |
| 일일피드백 row 0건 | 신규 생성 |
| 일일피드백 출결만 들어감 | "결석으로 변경?" 확인 후 patch |
| 일일피드백 피드백 작성됨 | 🔒 보호. 종료 |
| 휴원생 결석 호출 | 정상 처리 |
| "취소"·"q"·부정 | 즉시 종료, 변경 0 |

## 답변 인식 (네/아니오)

메모리 규칙 `feedback_yes_no_korean` 적용.

## 주의사항

- **확인 없는 자동 patch/생성 금지**: 미리보기 후 명시적 긍정 응답 없이 노션에 절대 쓰지 말 것
- **결석 사유 미수집**: v1에서는 받지 않음. 사유가 필요하면 노션에서 `내부 특이사항 메모` 등에 직접 기록
- **🔒 보호 발동 시 강제 변경 금지**: 노션에서 수동 처리 안내
- **`/출석` 과의 차이**: 본 명령어는 시간 비교 없음, 출결 값 = 결석 고정. 그 외 로직 동일

## 예시

**입력**: `/결석 추득만` (월요일, 추득만 정규수업 16:00~17:30 코딩 초등반, 학생 안 옴)

**미리보기**:
```
━━━━━━━━━━━━━━━━━━━━━━
📌 결석 처리 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:      추득만
- 학교/학년: 목동초등학교 6학년

[수업]
- 수업타입:  정규수업
- 클래스:    코딩 초등반
- 수업시간:  16:00 ~ 17:30
- 수업일자:  2026-05-07

[처리]
- 출결:      결석
- 동작:      신규 row 생성
━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

**결과**:
- 일일피드백 row 1개 생성
- `피드백라벨` → "2026-05-07 추득만 코딩 초등반 수업결과 (정규)"
- `출결` → 결석
