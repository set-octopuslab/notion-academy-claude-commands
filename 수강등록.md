---
description: 등록 중인 학생을 반에 매핑 + 수강료 설정 (수강 등록 DB row 1건 생성)
---

# 수강등록

원장이 `/수강등록` 을 입력하면, **학생 검색 → 반 타입 → 반 선택 → 시작일 → 수강료 → 월간 수업 횟수** 순서로 받고, **미리보기 → 확인(네/아니오)** 후 노션 수강 등록 DB에 row 1건을 생성한다.

학생 DB에 이미 등록된 학생만 대상. 학생 DB row가 없으면 `/학생-신규등록` 먼저.

이 row가 매월 청구라인 자동 생성의 기반이 되므로 수강료(월) / 월간 수업 횟수 / 회당 수강료 3개 필드를 함께 입력. 회당은 자동 계산 (수강료(월) ÷ 횟수, 소수점 절삭).

## 범위

**입력받을 필드 (5개)**
- 학생 (relation)
- 반 (relation)
- 수강시작일 (date)
- 수강료(월) (number, won)
- 월간 수업 횟수 (number)

**자동 계산**
- 회당 수강료 = `floor(수강료(월) ÷ 월간 수업 횟수)`

**비입력 (빈 칸)**
- 수강종료일 — 운영상 99% 케이스에 미정. 휴원·퇴원 시 별도 처리.

**제외 (별도 명령어)**
- 정규/보강 시간표 row 추가 — `/정규시간표등록`, `/보강시간표등록`
- 청구라인 — 노션 "다음달 청구 라인 추가" 버튼이 자동 처리
- 수강료 변경·일괄 조정 — `/수강료설정` (향후 ROADMAP 추가)

## 사전 확인

1. MCP 도구 로드:
   ```
   select:mcp__notionApi__API-query-data-source,mcp__notionApi__API-retrieve-a-page,mcp__notionApi__API-post-page
   ```
2. config 읽기: 학원 설정 파일 (`~/.claude/notion-academy-config.json` 또는 사용자 환경별 위치)
   - 위치는 `/노션-템플릿연결` 명령어가 처음 셋업 시 결정·저장
   - 파일이 없으면 `/노션-템플릿연결` 먼저 실행 안내
   - `notion.data_sources.students`
   - `notion.data_sources.classes`
   - `notion.data_sources.class_enrollments`
   - `tuition.patterns` (수강료 추천에 사용)
3. 현재 날짜 기록 (시작일 default "오늘" 키워드용)

## 절차

### 1. 학생 검색·선택

```
[1/5] 어떤 학생을 반에 등록하나요? (이름 일부만 입력해도 됨)

> 
```

쿼리: 학생 DB
- `학생이름 contains <입력>` AND `등록현황 == "등록"`

분기:
- 0건 → 1-1 종료
- 1건 → 1-2 자동 선택 + 확인
- 2건+ → 1-3 목록 선택

#### 1-1. 0건

```
❌ '<입력>' 이름의 등록 중인 학생이 없습니다.

💡 학생이 학생 DB에 등록되어 있어야 반 등록이 가능합니다.
   /학생-신규등록 으로 학생을 먼저 등록한 뒤 다시 시도해주세요.
   (이미 등록된 학생이라면 등록현황을 '등록' 으로 확인)

종료 (노션 변경 없음).
```

#### 1-2. 1건 (자동 선택)

```
✅ 1건 찾음
- {학생이름} ({학교} {학년}학년, 등록현황: 등록)
이 학생이 맞나요? (네/아니오)
```

긍정 → 다음 단계. 부정 → 종료.

#### 1-3. 2건+ (목록)

```
[1/5] 어느 학생인가요?
  1. 추득만 (목동초 5학년)
  2. 추득만 (양천중 1학년)
번호 선택:
```

동명이인 시 학교·학년으로 구분 표시.

### 2. 반 타입 선택

```
[2/5] 어떤 반에 등록하나요?
  1. 정규반 (매주 반복)
  2. 특강반 (기간한정·주제특화)
번호 선택:
```

분기:
- "1" → `target_type = "정규반"`
- "2" → `target_type = "특강반"`
- 그 외 → "1 또는 2를 입력해주세요" 재요청

### 3. 반 검색·선택

쿼리: 클래스관리 DB
- `클래스타입 == <target_type>`

분기:
- 0건 → 3-1 종료
- 1건 → 자동 선택 + 확인 ("`{반이름}` (담당: {선생님}) 맞나요? 네/아니오")
- 2건+ → 3-2 목록 선택

#### 3-1. 0건

```
❌ {정규반/특강반} 반이 클래스관리 DB에 없습니다.

💡 /클래스등록 으로 반을 먼저 만든 뒤 다시 시도해주세요.

종료 (노션 변경 없음).
```

#### 3-2. 2건+ (목록)

```
[3/5] 어느 {정규반/특강반}?
  1. 파이썬 초등 A (담당: 김선생)
  2. C언어 중등 B (담당: 이선생)
  3. 자바 고등 1 (담당: 박선생)
번호 선택:
```

각 반의 `선생님` rollup으로 담당 선생님 표시 (helpful context).

### 4. 같은 학생-같은 반 중복 검사

쿼리: 수강 등록 DB
- `학생 == <학생 page_id>` AND `반 == <반 page_id>`

#### 4-1. 0건 → 다음 단계 (5)

#### 4-2. 1건+ → 차단 + 행동 가이드 후 종료

```
━━━━━━━━━━━━━━━━━━━━━━
❌ {학생이름} 학생은 이미 '{반이름}' 반에 등록되어 있습니다.
━━━━━━━━━━━━━━━━━━━━━━

📍 기존 수강 등록 정보
   - 학생:        {학생이름}
   - 반:          {반이름}
   - 수강시작일:  {시작일}
   - 수강료:      {수강료(월)}원/월 ({월간 수업 횟수}회)
   - 노션 링크:   https://notion.so/<page-id>

💡 같은 학생을 같은 반에 중복 등록할 수 없습니다.
   아래 3가지 중 하나를 선택하시고 다시 시도해주세요.

  1. 다른 반에 등록 (정규+특강 동시 수강 등 정상 케이스)
     → 명령어 다시 호출하고 다른 반 선택

  2. 기존 수강료·시작일 변경 (현재 진행 중 row의 정보 수정)
     → 위 노션 링크 클릭 → 해당 필드 직접 수정

  3. 기존 row 삭제 후 재등록 (종료된 등록 정리)
     → 위 노션 링크 클릭 → 페이지 우상단 메뉴 → 'Delete'
     → 그 후 명령어 /수강등록 다시 호출

━━━━━━━━━━━━━━━━━━━━━━
종료 (노션 변경 없음).
```

> 같은 학생이 **다른 반**에 등록은 정상 (정규+특강 동시 수강 등) — 차단 안 함.

### 5. 수강시작일 입력

```
[4/5] 수강시작일을 입력해주세요. (YYYY-MM-DD)
       "오늘" 입력 시 오늘 날짜 ({YYYY-MM-DD}) 자동 적용

> 
```

#### 5-1. 입력 처리

- "오늘" → `start_date = <today>`
- "YYYY-MM-DD" 형식 → 검증 후 사용
- 그 외 → "날짜 형식이 올바르지 않습니다. YYYY-MM-DD 형식 또는 '오늘'을 입력해주세요." 재요청

#### 5-2. 날짜 검증 (±1년 이상 차이 시 경고)

| 조건 | 처리 |
|---|---|
| 형식 오류 | 재입력 |
| 너무 먼 미래 (오늘 + 1년 이상) | "⚠️ 시작일이 1년 이상 미래입니다. 맞나요? (네/아니오)" 부정 시 재입력 |
| 너무 먼 과거 (오늘 - 1년 이상) | "⚠️ 시작일이 1년 이상 과거입니다. 맞나요? (네/아니오)" 부정 시 재입력 |
| ±1년 이내 | 그대로 진행 |

### 6. 수강료(월) 입력

#### 6-1. 학생 학년대 추출 + 추천 패턴 결정

학생 row의 `초,중,고` 필드 (1단계에서 retrieve한 학생 정보 활용):
- "초" → config의 `초주1`, `초주2`
- "중", "고", "대" → config의 `중고주1`, `중고주2`
- 빈 값 → 추천 생략

#### 6-2. 입력 안내 (정규반 + 학년대 명확)

```
[5/5] 수강료(월)을 입력해주세요. (원, 숫자만)

💡 추천 (참고용 — 직접 입력해주세요)
   {학생이름} 학생은 {초/중/고}등생이네요. 학원 표준 패턴:
     · 주1회 정규수업 → {월수강료1}원 ({월기준1}회, 회당 {회당단가1}원)
     · 주2회 정규수업 → {월수강료2}원 ({월기준2}회, 회당 {회당단가2}원)

> 
```

#### 6-3. 입력 안내 (특강반 또는 학년대 미상)

```
[5/5] 수강료(월)을 입력해주세요. (원, 숫자만)

> 
```

#### 6-4. 입력 처리

- 숫자만 입력 → `tuition_monthly = int(<입력>)` (콤마·"원" 등 제거 후 숫자 변환)
- 0 또는 음수 → "0원 또는 음수 수강료는 불가합니다. 무료 체험은 별도 처리해주세요." 재요청
- 형식 오류 → 재요청

### 7. 월간 기준 수업 횟수 입력

```
[?/5] 월간 기준 수업 횟수를 입력해주세요.
       (월 수강료 계산 기준 수업 횟수)
       (예: 주1회 정규수업 → 4, 주2회 → 8)
       (보강수업은 이 횟수에 포함되지 않음)

> 
```

#### 7-1. 입력 처리

- 양의 정수 → `lesson_count = int(<입력>)`
- 0 또는 음수 → "0 이하는 불가합니다." 재요청
- 형식 오류 → 재요청

#### 7-2. 회당 수강료 자동 계산

`per_lesson = floor(tuition_monthly / lesson_count)` (소수점 절삭).

### 8. 미리보기

```
━━━━━━━━━━━━━━━━━━━━━━
📋 수강 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:        {학생이름}
- 학교/학년:   {학교} {학년}학년

[반]
- 이름:        {반이름}
- 타입:        {정규반/특강반}
- 담당 선생님: {선생님이름}

[수강 정보]
- 수강시작일:    {시작일}
- 수강료(월):    {수강료}원
- 월간 수업 횟수: {횟수}회 (월 수강료 계산 기준)
- 회당 수강료:   {회당}원 (자동 계산)
- 수강종료일:    (미정)

[처리]
- 동작:        수강 등록 DB에 row 1건 신규 생성
- 후속 작업:   /정규시간표등록 또는 /보강시간표등록

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

부정 또는 "취소" → 즉시 종료, 노션 변경 0.

### 9. 노션 row 생성

#### 9-1. 등록라벨 자동 생성

`등록라벨 = "{학생이름} - {반이름}"` (예: "추득만 - 파이썬 초등 A").

#### 9-2. post-page

```json
{
  "parent": {"type": "database_id", "database_id": "<class_enrollments database_id>"},
  "properties": {
    "등록라벨": {"title": [{"text": {"content": "<등록라벨>"}}]},
    "학생": {"relation": [{"id": "<학생 page_id>"}]},
    "반": {"relation": [{"id": "<반 page_id>"}]},
    "수강시작일": {"date": {"start": "<YYYY-MM-DD>"}},
    "수강료(월)": {"number": <tuition_monthly>},
    "월간 수업 횟수": {"number": <lesson_count>},
    "회당 수강료": {"number": <per_lesson>}
  }
}
```

`수강종료일`은 omit (빈 칸).

### 10. 결과 보고

```
✅ 수강 등록 완료

[학생-반 매핑]
- 학생:    {학생이름}
- 반:      {반이름} ({정규반/특강반})

[수강 정보]
- 시작일:   {시작일}
- 수강료:   {수강료}원/월 ({횟수}회, 회당 {회당}원)

[노션 URL]
https://notion.so/<page-id>

[다음 작업 추천]
1. /정규시간표등록 → 학생 요일·시간 추가 (정규반)
2. /보강시간표등록 → 보강수업 일정 추가 (필요 시)
```

## 엣지케이스

| 상황 | 처리 |
|---|---|
| 학생 검색 0건 | "/학생-신규등록 먼저" 안내 후 종료 |
| 학생 동명이인 | 학교·학년으로 구분 표시 |
| 반 타입 1/2 외 입력 | 재입력 요청 |
| 해당 타입 반 0건 | "/클래스등록 먼저" 안내 후 종료 |
| 같은 학생-같은 반 중복 등록 | 차단 + 3가지 행동 가이드 후 종료 |
| 같은 학생-다른 반 등록 | 정상 허용 (정규+특강 동시 수강 등) |
| 시작일 형식 오류 | 재입력 요청 |
| 시작일 너무 먼 미래/과거 (±1년 이상) | 경고 + 확인 |
| 수강료 0 또는 음수 | 재입력 요청 |
| 월간 수업 횟수 0 또는 음수 | 재입력 요청 |
| 수강료 ÷ 횟수 떨어지지 않음 | 회당 수강료 소수점 절삭 (미리보기에서 표시) |
| 미리보기에서 부정 답변 | 즉시 종료, 노션 변경 0 |
| 어느 단계든 "취소"·"q" | 즉시 종료 |

## 주의사항

- **확인 없는 자동 쓰기 금지**: 미리보기 후 사용자 명시적 긍정 응답 없이 노션에 절대 POST 금지
- **학생 등록현황 == "등록" 만**: 휴원·퇴원 학생은 검색에서 제외. 휴원 복귀 시 등록현황 변경 후 호출
- **같은 학생-다른 반은 정상**: 정규+특강 동시 수강 같은 케이스는 row 2건이 정상. 차단 안 함
- **회당 수강료 소수점 절삭**: 200,000 ÷ 3 = 66,666원. 학부모 안내 시 차이 1~2원 발생 가능
- **수강종료일 항상 빈 칸**: 종강일 정해진 단기 특강도 일단 빈 칸. 필요 시 노션에서 직접 입력
- **터미널 default 입력**: 시작일 default는 "오늘" 키워드. 엔터 사용 안 함 (메모리 `feedback_terminal_default_input`)
- **답변 인식**: (네/아니오) 자유 답변 의미로 해석 (메모리 `feedback_yes_no_korean`)

## 예시

**입력**
- 학생: 추득만 (목동초 5학년, 등록 중)
- 반 타입: 정규반
- 반: 파이썬 초등 A (김선생 담당)
- 시작일: 오늘
- 수강료: 200000
- 월간 수업 횟수: 4

**자동 계산**
- 회당 수강료: 50,000원

**미리보기**
```
━━━━━━━━━━━━━━━━━━━━━━
📋 수강 등록 미리보기
━━━━━━━━━━━━━━━━━━━━━━

[학생]
- 이름:        추득만
- 학교/학년:   목동초 5학년

[반]
- 이름:        파이썬 초등 A
- 타입:        정규반
- 담당 선생님: 김선생

[수강 정보]
- 수강시작일:    2026-05-10
- 수강료(월):    200,000원
- 월간 수업 횟수: 4회 (월 수강료 계산 기준)
- 회당 수강료:   50,000원 (자동 계산)
- 수강종료일:    (미정)

[처리]
- 동작:        수강 등록 DB에 row 1건 신규 생성
- 후속 작업:   /정규시간표등록 또는 /보강시간표등록

━━━━━━━━━━━━━━━━━━━━━━
진행할까요? (네/아니오)
```

**노션 결과**
- 수강 등록 DB row 1건 추가
- 등록라벨="추득만 - 파이썬 초등 A"
- 학생, 반 dual relation 자동 연결
